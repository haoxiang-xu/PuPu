---
case_id: 0000-0001-2026-0807
updated_at: 2026-08-07T17:05:00-07:00
---

# 裁定与授权

## R-0001 | 中止裁定 | 2026-08-07T17:05:00-07:00

- **裁定身份**: Chief Judge
- **记录类型**: TERMINATION
- **依据**: S-0002, S-0004, S-0005, S-0006
- **裁定**: 本案 **中止**，不进入议案裁定。理由不是议案不成立，而是 **本案的法定必到名单超出运行时可承载的传唤宽度**。14 名法定必到者并行出庭，9 个 instance 以同一签名死亡（600 秒无进展，watchdog 未恢复），其中 7 名为法定必到者。依 [`quorum.md` 第二节](../../../codex/lifecycle/quorum.md)，法定必到者缺席不得闭庭；继续补传只会以同样方式失败，因为成因与各角色的任务内容无关，只与并发宽度相关（见 [`adaptations.md` A-012](../../../codex/adaptations.md)）。

  待裁问题 Q1–Q10 **全部保留**，改由三个 case 分别承接。分案依据是 **必到名单的规模**，不是问题的大小或重要性 —— 名单规模是可机械导出的客观属性，与 [`tracks.md`](../../../codex/lifecycle/tracks.md) 拒绝按大小分档的原则不冲突。

- **强制回应**: 无（未进入裁定阶段，无 `Expert` 不成立意见与 `Dimension Owner` 反对意见待回应）
- **获准方案**: NOT_APPLICABLE
- **验收标准引用**: 无
- **获准 action**: 无
- **中止时的阶段**: 议案庭审（`phase: motion`），独立首轮提交进行中
- **已归档产出的处置**: **全部保留，不作废。** `record.md` 的 S-0001…S-0006 与 `evidence.md` 的 E-0001…E-0015 继续有效，后继 case 以跨 case 引用格式 `0000-0001-2026-0807#S-####` / `#E-####` 援引。两份完整的 `ASSESSMENT`（S-0005 `code-owner-runtime`、S-0006 `code-owner-chat-core`）是本案的主要实体产出，其中 S-0006 系书记员于中止后补录（`speaker-of-the-house` 已停止），其候选证据 CE-1…CE-8 **未经 `evidence-examiner` 验证、未分配 `E-####`**，引用者自行承担举证责任。
- **后继 case**: 0000-0002-2026-0807 · 0000-0003-2026-0807 · 0000-0004-2026-0807

### 本次中止暴露的程序缺陷（一并裁定，供 `codex` 归档）

1. **`记录类型` 枚举无法承载中止。** 中止权在宪法、`terminated` 在 `case.md` 格式，唯独记录形式缺失 —— 已补 `TERMINATION`（[A-011](../../../codex/adaptations.md)）
2. **运行时故障被迫记为阻塞记录。** 二者对闭庭效力相同但证明的东西相反，混计会把容量问题误读成组织过载、据此去拆一个健康的 owner —— 已分列（[A-012](../../../codex/adaptations.md)）
3. **传唤第一层连续两次静默漏人**：解析器要求围栏紧邻标题，丢掉 6 个 `Expert` 中的 5 个（[A-010](../../../codex/adaptations.md)）；实体抽取只收含 `/` 的字符串，丢掉 `code-owner-settings`，而它正是 Q2 的直接 owner。两次皆由后续传唤层捞回 —— **三层传唤的设计前提"第一层一定会写窄"得到实测确认**
4. **`speaker-of-the-house` 停止后归档无人承接。** S-0006 由书记员补录属程序偏离；证据灭失与程序纯洁性之间取前者。**该缺口尚未有条文，留待 `codex` 处置**
