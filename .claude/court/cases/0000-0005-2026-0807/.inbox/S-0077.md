#### S-0077 | ASSESSMENT | evidence-examiner → S-0065

- **阶段**: 议案庭审 · 承重证据复核 block 4/6（[证据规则第六节](../../../codex/lifecycle/evidence-rules.md)，[闭庭门禁第 11 项](../../../codex/lifecycle/speech-protocol.md#闭庭门禁)前置）

- **结论**: **25 条复核完毕：已验证 22 · 未验证 2（`E-0173` / `E-0174`，均维持 S-0060）· 相矛盾 1（`E-0154`，本块新增）。** 本块 25 条的每一处 `file:line`、每一段引文、每一条正向与负向 `grep`、每一次机械集合运算、以及 **`expert-ux` 的全部 15 个对比度数字**，本席在自钉 revision 上逐条重跑或按 WCAG 公式从头重算。**新增未通过一条**：`E-0154` 称 `chat_bubble.memory_v2_mount.test.js`「**既不经过 `isMemoryV2TraceBundle` 的真实实现**」—— 本席实测该陈述 **恰好相反**：`chat_bubble.js:10` 直接 import 真实实现、`:107-108` 调用它算出 `hasMemoryV2Audit`、`:109-110` 以它决定是否挂载；该测试的两个 mock 只替换了 `./lazy_trace_chain` 与 `./trace_chain`，**未替换 presenter**，且在该 fixture 下 `hasVisibleTraceActivity` 与 `hasTokenSummary` 均为假，**故真实的 `isMemoryV2TraceBundle` 是该测试唯一的挂载触发器 —— 它绿灯的全部原因就是那个门**（`E-0323`）。其结论（该测试察觉不到挂载门 **变宽**）经本席独立走通仍成立，但成立于它给出的 **另一个** 理由（fixture 只含白名单内的 `mode`）；依[证据规则第六节](../../../codex/lifecycle/evidence-rules.md)，**「是否仍能支持同一结论」是裁决者的判断，不是本席的**。**本席另有六项本块之外无人指出的发现，均在三问射程之内，逐条交出**：

  **(一) `E-0179` 的 light 对比度不可复现：实为 8.99:1，非 8.74:1，且成因可定位。** 本席按该条自己抬头声明的方法（`眼见值 = α·前景 + (1−α)·底色`，底色取 `detailsBackground` 叠在 `--pupu-background` 上）算得 **8.99:1**。穷举后 **8.74:1 只有一种产生方式**：把前景 `rgba(0,0,0,0.72)` 合成到 **纯 `#ffffff`** 上，却拿去与 `detailsBackground` 的合成底求比 —— **前景与底色被合成到两个不同的底上**。同一张表的 dark 行（`10.48:1`）**按一致方法可精确复现**，故这是 **同表内的方法不一致，不是方法性偏差**。偏差 2.8%、方向 **保守**（低估）、`SC 1.4.3（4.5:1）满足` 的结论不变（`E-0333`）。

  **(二) `E-0182` 的 `modeLabel` 指错 24 行。** 其 `定位` 称 `memory_v2_trace_presenter.js:355-357（modeLabel）`—— `:355-357` 实为 `predictedTokens`；`modeLabel: titleCase(mode) || "Off"` 在 **`:379`**。**其所述机制本席逐环独立走通并确认成立**（`:159 return mode || "off"` → `titleCase` `:150-151` → `:379` → `trace_chain.js:1944-1947` 的 span 槽），故判 **已验证**，只记指错。另 `titleCase` 的 `定位` 范围 `:148-151` 首尾各偏一行（实为 `:149-152`），而其行内注释所标的 `:150-151` **精确**（`E-0334`）。

  **(三) `E-0161` 的「唯一路径」句与它自己的端点枚举并存。** 该条同时 (a) 列出 `GET /debug/dom` 与 `GET /debug/state`，(b) 断言「读取渲染状态的 **唯一路径** 是 `POST /debug/eval`」。本席读原文：`test-api-debug.md:102` 逐字写着 `/debug/dom` 是 `Convenience wrapper for eval`，**故该句在文档自身的措辞下成立**；`GET /debug/state` 返回的是定长字段快照（`active_chat` / `modal_open` / `is_streaming` / `route` / …），**不含任何 trace 字段**。本席判 **射程可辩护，不判失真**，但记明：**该条支撑一项 `不成立`，而它的「唯一性」是靠一句文档措辞撑住的，不是靠实现**（`E-0326`）。

  **(四) `E-0176` 的消费者清单被呈为一条 `grep` 的输出，实为下界，至少漏 2。** 本席复跑得两条该条未列的 `var(--pupu-danger)` 消费者：`src/COMPONENTs/chat-bubble/artifact-summary/files_changed_card.js:97` · `src/COMPONENTs/toolkit/pages/plugin_detail_page.js:477`。**其 `toast_host.js:100` 一项经本席核对成立**（`semantic.danger || "#d64545"`，本席首轮 BRE 检索因 `?` 为字面量而漏掉，非该条之误）。漏列 **只增强** 其存在性主张，故判 **已验证**（`E-0330`）。

  **(五) `E-0171` 的「无任何 error / danger 键」按字面读及于整个 JSON 时为假。** `default_mini_theme.json:252` / `:568` 各有一个 `errorAccent`（`#DC3545` / `#F87171`），**二者均在 `modal` 块内，不在 `timeline` 块内**。该句的后半（「—— `timeline` 的状态词汇表只有 `done`/`active`/`pending` 三值」）已把射程指明，且 **`timeline` 块内确无 error / danger 键、确无 `pointColor`**，`timeline.js:742` 的 props 文档逐字如其所述。判 **已验证**，记射程（`E-0327`）。

  **(六) `E-0174` 的「六个自定义 `point` 元素」只在一种读法下自洽。** 本席复跑其 `grep`，**10 行逐行与其枚举完全一致**；其中字符串预设 3 处（`"loading"`×2 · `"end"`），**元素值 7 处**，**不同的自定义组件 5 个**（`AccentPoint` / `HammerPoint` / `SubagentPoint` / `toolPointEl` / `ErrorPoint`）。**「六」= 7 减 `ErrorPoint` 自身**（该条第 2 点单独论 `ErrorPoint`）—— 这一读法自洽，其余读法不成立（`E-0329`）。

  **本席对已有结论的两条不重开同一问题**，只作时效性维持并独立复现其两半：`E-0173` 的「整条 trace chain 不跟随用户自定义主题」经本席实测确属射程过宽 —— `Timeline` 的 `tl` 携带 `highlightColor: themeHighlightColor(theme)`（`timeline.js:756-762`），而 `highlightColor: accent` **正在** `applySemanticPaletteToTheme` 的覆写表内（`theme_semantic.js:233`），故 `status==="active"` 的点与线 **确实跟随主题**（`resolvePointColor:44` · `resolveLineColor:38`）；**而本案那一行的 `status` 只取 `"done"` 或 `"pending"`**（`trace_chain.js:1948-1949`），二者走的是 `pointDoneColor` / `pointPendingColor` 这两个固定 alpha 灰，**故「本案那一行上仍成立」经本席独立走通确认**（`E-0328`）。`E-0174` 同样维持 S-0060 的 **未验证**，其余各项经本席逐条复现均属实。**本席不就四形状、不就任何 `不成立`、不就严重度、不就对比度是否达标、不就本案任何实体争点作任何表态** —— 对比度达标与否属 `expert-ux` 的专业判断，本席只复核取值与算术。

- **依据**: 本案 `E-0150`~`E-0161` 本体（`evidence.md:1519-1827`）· `E-0170`~`E-0182` 本体（`evidence.md:2156-2421`，含 `:2156-2160` 的公共取证抬头与对比度复算方法声明）；`S-0064`（`SUMMARY` 草案，用于确认各条各自在撑什么）· `S-0065`（导出过程与偏离披露）· `S-0072` / `S-0074` / `S-0076`（block 1–3 结果与四格分块代价）；已有审查结论 `S-0060`（→`E-0173` / `E-0174`）· `S-0046`（→`E-0171` / `E-0172` / `E-0178` 的既往独立实测）· `S-0041`（→`E-0015`）· `S-0055`（→`E-0020`，仅取其已验证的 `:313`/`:386` 映射作为复核 `E-0160` 是否重复同一误读的对照）；援用处 `S-0023`（`expert-qa`）· `S-0027`（`expert-ux`）。**本席实测（2026-08-08，全程只读，未改任何产品代码，未 commit，未起应用，未渲染 DOM，未触发构建）**：PuPu `b2385d5dc7951887b6aeebd4001d17b4cd78af83`（分支 `dev`；`git status --porcelain -- src electron unchain_runtime public scripts package.json` **零行**，全部 54 行脏改动无一在 `.claude/` 之外）· unchain `a4e69f413c449c5768433ba4dddc5b60b8146991`（`git status --porcelain` **零行**）· `python3` 3.11.7 / PuPu `.venv` 3.12.3 / unchain `.venv` 3.12.11。逐条命令与输出见 `E-0320`~`E-0334`。程序依据 [证据规则](../../../codex/lifecycle/evidence-rules.md) 第三 · 四 · 五 · **六** 节、[发言协议 · 角色输出契约](../../../codex/lifecycle/speech-protocol.md#角色输出契约)、[A-012](../../../codex/adaptations.md)

---

## 逐条结论表（25 条）

### `expert-qa`（S-0023）12 条

| 证据 | 评估结论 | 一句话理由 |
|---|---|---|
| `E-0150` | **已验证** | sha256 `9778e5be…` 与 HEAD 逐字符复现，三产品目录 dirty 实测 0（本席在打开案卷前与跑完 pytest 后各测一次）。 |
| `E-0151` | **已验证** | 三目录 `*.test.js` 实测 **7 / 12 / 12 = 31**，文件名逐个吻合；`TOP_LEVEL_KEYS` 全 `src/` **2 行，全在 presenter 自身**（`:9` 定义 · `:127` 使用），零测试引用。 |
| `E-0152` | **已验证** | 须查类，本席依传唤书「不触发构建」**未复跑 jest**；但其运行时观察 **可静态对账且逐项吻合**：`--testPathPattern` 机械选出的套件集 **恰为 15 个、与 PASS 清单文件对文件相同**；用例数 **85 个静态调用点 + `memory_v2_trace_presenter.test.js:104` 的 `test.each` 3 行表 = 88**，与所载 `88 passed` **精确对上**（`E-0322`）。 |
| `E-0153` | **已验证** | `:29` `toMatchObject`、`:41` 负向断言、43 行、唯一 `test()` 均精确；`arbitrary_provider_payload` 经本席枚举确不在白名单（本席实数 **59** 项）；**一处偏一行**：`arbitrary_provider_payload` 实在 `:23`，条目记 `:24`。 |
| `E-0154` | **相矛盾** | 「该测试**不经过** `isMemoryV2TraceBundle` 的真实实现」**与事实恰好相反** —— 该门是它唯一的挂载触发器；详见下方完整四项。 |
| `E-0155` | **已验证** | `:132-141` 的 `bind_pupu_context_module(…partial_attempt_sink…)` **逐行精确**，`:873` / `:876` 第二实例 **精确**；**一处偏一行**：引文首行 `assert len(partials) == 1` 实在 `:789`，条目记 `:790`。 |
| `E-0156` | **已验证** | 引用代码 **逐字属实**，三条断言（注入 raise · `journal_status="partial"` · 原始错误串不出现）全部在位；**范围少 2 行**：末两条断言实在 `:909` / `:910`，条目记范围止于 `:908`。 |
| `E-0157` | **已验证** | 本席在三个解释器上 **逐条复跑**：系统 `python3` 与 PuPu `.venv` 均 `ModuleNotFoundError: No module named 'unchain'`（逐字一致）· unchain `.venv` **20 passed** · `-k partial` **1 passed, 39 deselected** 逐字一致；两处 venv 的包状态精确。耗时 1.30s vs 所载 1.23s —— 须查类的固有差，不影响结论。 |
| `E-0158` | **已验证** | 四项负向逐条复现：两处 `conftest.py` 均不存在 · `find -maxdepth 3` 三种 pytest 配置 **零命中** · `package.json` 内 `pytest` **零命中**（7 行命中全为 `unchain_runtime` 构建与打包路径）· `unchain_runtime/scripts/` **恰两个构建脚本**。 |
| `E-0159` | **已验证** | `--collect-only` 复跑得 **111 tests collected**；`test_chat_stream_v4.py:554` / `:598` **精确**、`test_models_catalog_route.py:374` **精确**、`:1821` 起点精确；两处轻微：`assertEqual` 块实止于 `:1848`（条目记 `:1845`），且代码块为 **重排后的浓缩引用而非逐字**（已用 `…` 标示，取值全部属实）。 |
| `E-0160` | **已验证** | 每一处引用 **逐行精确**：`:280-289` `replaceMessages` 引文逐字 · `:283` 确为 `chat_storage/*.js` 中 **唯一** `INSERT INTO messages` · `:334-340` `applyImportStore` 与注释逐字 · `:494-522` 六步顺序逐条吻合 · `electron/` 全域 `sanitizeMessages`/`sanitizeMessage(` **实测 0**。**本席特别记明：本条 *未* 重复 `E-0020` 被 S-0055 判为反转的那处 `:313`/`:386` 映射**——它根本不主张调用点归属，且其完整性限制 3 已自行把 `WRITE channel :539-541` 列为未穷举项（本席复核 `:540` 确为 `const write = (store) => { applyOps([{ type: "import_store", store }]); }`，注释自陈 WRITE channel）。 |
| `E-0161` | **已验证** | 两份文档的端点集合 **逐项吻合**（`/chats` 五件 · `/chats/:id/activate` · messages 与 runs 四件 · catalog 三件 · 选择三件 · `### Errors`；debug 五件），`GET /chats/:id` 的返回字段逐字一致，**故障注入端点确为零**；其「文档属传闻类、只证明文档如此声称」的自我限定 **本席确认必须照此约束其证明力**；「唯一路径」句见发现 (三)。 |

### `expert-ux`（S-0027）13 条

| 证据 | 评估结论 | 一句话理由 |
|---|---|---|
| `E-0170` | **已验证** | 两项锚点本席独立复现（先测后读），与主持人 S-0065 第二节一致。 |
| `E-0171` | **已验证** | `:2` / `:318` / `:284-294` / `:600-610` **四处范围全部精确**，**表内 9 组 18 个取值逐字无一偏差**，`timeline` 块内确无 `pointColor`、确无 error/danger 键，`timeline.js:742` props 文档逐字如述；射程见发现 (五)。 |
| `E-0172` | **已验证** | 全部 `file:line` 精确（`:42-48` `resolvePointColor` · `:64-77` `DotDefault` · `:24` · `:34-40` · `:756-762` · `trace_chain.js:1937-1959` 无 `point` 键 · `chat_bubble.js:170-176` 仅 `isUser && !isEditing` 上底色 · `semantic_tokens.js:242`/`:255`）；**四个合成值与四个比值本席从 WCAG 公式重算，逐位精确**（`#C7C7C7` 1.69 · `#D1D1D1` 1.53 · `#464646` 1.99 · `#3D3D3D` 1.72），8 位差 **10/255 · 9/255** 精确，第 5 段四个数（15.91 / 11.67 / 3.35 / 4.53）亦精确。 |
| `E-0173` | **未验证** | 维持 S-0060。本席独立复现其两半：覆写表枚举 **完整精确且 `timeline` 确不在内**；但 `highlightColor` 在覆写表内且经 `tl` 进入 timeline，**故「整条 trace chain 不跟随主题」射程过宽**；本案那一行（`done`/`pending`）上仍成立。完整四项见下。 |
| `E-0174` | **未验证** | 维持 S-0060。本席独立复现其可核部分（`ErrorPoint :543-567` 逐字 · `:1747` 精确 · **10 行 `point:` 枚举逐行吻合** · memory_v2 push **确无 `point`** · `#ef4444` 对比度 **3.76 / 4.98 精确**）；「六个」的读法见发现 (六)。完整四项见下。 |
| `E-0175` | **已验证** | `:196-202` 的 `role="alert"` + `var(--pupu-danger, #c44)` **逐字精确**，`Error code` 行与之相距 148 行（「约 145」属实）；**一处偏一行**：`role="status"` 先例实在 `memory_v2_journal_reload.js:575`，条目记 `:574`。 |
| `E-0176` | **已验证** | 六组 token 取值 **逐字精确**，`:239-266` / `:102-109`（`danger: shipped` · `warning: P1`）/ `:159-172` / `:232` / `container.js:274` `:368` **全部精确**；「无条件执行」经本席读 `container.js:150-176` 确认 —— `themeColorCustomizationEnabled` **只决定读哪份 settings，不决定是否下发调色板**；消费者清单为下界，见发现 (四)。 |
| `E-0177` | **已验证** | 本席 **独立跑了全 9 预设 × 2 模式 × 3 核算面的全量扫描**（未复用其数）：`danger↔background` 下界 **3.05:1（`nord` 暗色 `#bf616a` on `#2e3440`）** · `danger↔sidebar` **2.46:1** · `danger↔surface` **2.11:1** —— **三个下界与其绑定预设逐一精确**；`default` 三档 4.53 / 6.77 / 6.03 精确；次低档实测 3.97 / 4.09 / 4.11 落在其所述 3.97–4.12 内；9 套预设名逐个吻合（`:269` `default` + `:270`–`:478` 八套）。 |
| `E-0178` | **已验证** | `:281-320` 按钮全文、`:307`、`:308-318` 图标 **范围精确**；`TITLE_LINE_H = 18`（`:20`）故 `:256` 的 18px 属实；`outline:"none"` 在位、`timeline.js` 内 **`aria-` 零出现**、`src/*.css` 与 `public/index.html` **无任何 `:focus` / `focus-visible` 规则**；`spanFontSize` 在主题 JSON 中不存在且 `compact` 在本案路径为假（`chat_bubble.js` 不传，`trace_chain.js:643` 默认 `false`），**故 11px 成立**；五个数字（2.44 / 3.21 / 1.65 / 1.92，降幅 32% / 40%）本席重算 **逐位精确**。**一处**：引文标「逐字」但把 `tl.spanFontSize ?? (compact ? "10px" : "11px")` 省成 `?? "11px"` —— 本案路径上取值不变。 |
| `E-0179` | **已验证** | 源取值 **全部精确**（`:35-64` `AuditRow` 范围精确 · `isDark ? "rgba(255,255,255,0.76)" : "rgba(0,0,0,0.72)"` 逐字 · `code` 时 11px 等宽 · `pre-wrap` / `anywhere` / `userSelect:"text"` 逐字），dark 行 **10.48:1 精确**；**light 行 8.74:1 不可复现，实为 8.99:1**，成因已定位、方向保守、`满足 SC 1.4.3` 的结论不变，见发现 (一)。`:344-352` 的范围含 `Error code` 行（其本体在 `:347-352`），前 3 行为相邻的 `Compression`/`Source range`/`Reason`。 |
| `E-0180` | **已验证** | `:162-196` `resolveTraceStatus` 全文 **范围精确**，本席逐条读出全部 8 个 `return`，**确无一条回吐入参字符串**，末行逐字一致；`trace_chain.js:1939-1943` 的标题插值逐字精确。（顺带独立复现 block 3 的 `E-0301`：`:191` 的 `reason` 合取项确在活路径上。） |
| `E-0181` | **已验证** | `grep -rln "Unavailable" src/locales/` **零命中** · `'"Memory V2\|memory_v2' src/locales/en.json` **零命中** · locale 实数 **11** —— 三项全部精确复现。 |
| `E-0182` | **已验证** | 所述机制本席逐环独立走通并确认（`:159 return mode \|\| "off"` → `titleCase` `:150-151` → `modeLabel` → `trace_chain.js:1944-1947` 的 span 槽），`resolveMode` 的 `:154-160` 精确；**`modeLabel` 的定位指错 24 行**，见发现 (二)。 |

---

## 未通过条目的完整四项

### 一 · `E-0154`

- **评估结论**: **相矛盾**
- **证据编号**: `E-0154`
- **来源类型**: `general`
- **真实性**: **多数属实，一项不实且恰好相反。** 逐项实测（PuPu `b2385d5d`，产品树洁净）：

  | `E-0154` 的陈述 | 本席实测 | 判定 |
  |---|---|---|
  | 文件 61 行、2 个 `test()` | `wc -l` = 61；`test(` 两处（`:42` · `:49`） | **属实** |
  | `:9-19` 把 `./lazy_trace_chain` 与 `./trace_chain` 整个 `jest.mock` 掉 | `:9-13` mock `./lazy_trace_chain`，`:15-19` mock `./trace_chain` | **属实** |
  | 替身只渲染 `props.bundle?.memory_v2?.mode \|\| "missing"` | 两个替身逐字如此（`:11` · `:17`） | **属实** |
  | `:33-39` fixture 为 `meta:{bundle:{memory_v2:{mode:"active"}}}`，单键，在 59 项白名单内 | `message` 常量 `:33-39`，`meta` 在 `:38`；本席枚举 `TOP_LEVEL_KEYS` 实数 **59**，`mode` 为第 5 项 | **属实** |
  | `:42-58` 两条断言均为 `expect(screen.getByTestId(...)).toHaveTextContent("active")` | 断言在 `:44-46` 与 `:57-59`，形态逐字一致（第二条末行为 `:59`） | **实质属实** |
  | **「该测试既不经过 `isMemoryV2TraceBundle` 的真实实现」** | `chat_bubble.js:10` **直接 import 真实实现**（未被 mock）；`:107-108` `const hasMemoryV2Audit = isAssistant && isMemoryV2TraceBundle(message.meta?.bundle?.memory_v2);`；`:109-110` `shouldRenderTraceChain = hasVisibleTraceActivity \|\| hasTokenSummary \|\| hasMemoryV2Audit`。该 fixture 下 `traceFrames={[]}` → `hasVisibleTraceActivity` 假；无 `consumed_tokens` → `hasTokenSummary` 假。**故真实的 `isMemoryV2TraceBundle` 是唯一的挂载触发器；若它对 `{mode:"active"}` 返回 `null`，`getByTestId` 即抛错、测试变红。** `character_chat_bubble.js:10` / `:139` 同构 | **不实，且与事实恰好相反**（`E-0323`） |
  | 「不含任何本案形状会新接纳的键」→ 四形状下保持绿、挂载门变宽不在射程内 | fixture 只含白名单内的 `mode`，扩表不改变其准入结果 | **属实** |

  **未被篡改**：本条在 `evidence.md:1606-1616` 的正文与提出发言 `S-0023` 的对应表述一致，误述在两处同样存在，是 **一贯的读法**，不是归档时的改写。

  **本席记明一个可能的宽容读法并说明为何仍不采纳**：若把「它」读作「被断言的那个值的路径」，则该句为真——渲染出的 `"active"` 确由替身直接从原始 bundle 取，未流经 presenter 的输出。但 (a) 该句的字面主语是 **该测试**；(b) 该句在论证中承担的功能，是确立「这条测试在门上不提供任何保护」，而这一点 **materially 为假** —— 它对门的 **收窄** 会变红，只对 **放宽** 失明；(c) 正确的射程是「对变宽失明」，不是「不碰这个门」，二者对 QA-2 所刻画的「今天存在什么保护」是两个不同的事实。

- **可靠性**: **内部来源。** 依[证据规则第三节](../../../codex/lifecycle/evidence-rules.md)，本条自标 **自证类**，本席确认该归类正确（仓库在指定 revision 的文件内容 + 可复跑 `sed`），**但依第六节，自证类的免检在本关失效**，故本席未据其自证身份免除任何一项复跑。**可追溯性完好**：revision 已钉、路径与行号齐备、命令可原样粘贴，本席全部复跑成功。**取证程序无瑕疵**：其完整性限制 2 已逐字声明「两个文件均属 `code-owner-chat-bubble` 边界，越界只读，不主张改法」。**本席指出一项与本次误述直接相关的可靠性特征**：致误的那一步 **不在被引文件之内** —— 它需要同时读 `chat_bubble.js` 的挂载判据才能看出，而 `chat_bubble.js` 在本条的 `取得方式` 里 **一次都没有出现**。**这是一次「只读被 mock 的一侧、未读做决定的那一侧」的取证盲区，与提出方的专业能力无关。**

- **相关性**: **拆为两个可分别回答的命题，分别作答，不合并。**

  **命题（1）「该测试察觉不到 `E-0012` C 段所述的挂载门变宽」—— 完全支持。** 该命题压在一项 **与门的实现无关** 的事实上：fixture 只含 `mode`，而 `mode` 在扩表前后 **准入结果不变**。本席独立走通四个形状，无一能使该 fixture 的准入判定翻转。**成立，且其成立不依赖被认错的那一项。**

  **命题（2）「该测试在 `isMemoryV2TraceBundle` 这道门上不提供任何保护」—— 不支持，且与事实相反。** 该测试的绿灯 **完全由** 真实 `isMemoryV2TraceBundle` 对 `{mode:"active"}` 的返回值决定，是全仓 **唯一一条** 会因该门收窄而变红的渲染侧测试。**故本条据以支持 QA-2 时，可支持的是「对放宽失明」，不可支持「对该门无覆盖」。** 依[证据规则第六节](../../../codex/lifecycle/evidence-rules.md)，受影响项是否仍能由其余证据支撑，**是裁决者的判断，不是本席的**。

- **来源归类**: **内部来源**（`repository` · 自证类；提出方 `expert-qa`，越界只读已依规声明）。

### 二 · `E-0173`（维持 S-0060，本席只作时效性维持与独立复现）

- **评估结论**: **未验证**
- **证据编号**: `E-0173`
- **来源类型**: `general`
- **真实性**: **可核部分全部属实。** `theme_semantic.js:211-303` 确为 `applySemanticPaletteToTheme` 全文（`:211` 声明、`:303` 闭合）；其返回对象显式覆写的顶层键经本席逐行读出为 `highlightColor`(`:233`) · `color`(`:234`) · `backgroundColor`(`:235`) · `foregroundColor`(`:236`) · `icon`(`:237`) · `font`(`:238`) · `input`(`:239`) · `select`(`:245`) · `modal`(`:256`) · `switch`(`:275`)，`deepTier` 分支下 `code`(`:287`) · `textfield`(`:288`) · `markdown`(`:295`) —— **与其枚举逐项一致、无遗漏、无多列**，**`timeline` 确不在其中**；`container.js:165` 的调用点在其所述 `:157-171` 内；`grep -rn "timeline:" src/` **实测零命中**。
- **可靠性**: **内部来源**（`repository` · 自证类；本关免检失效，本席已逐条复跑）。定位完整、命令可复现、越界只读已声明。
- **相关性**: **其「据此」段第 (ii) 项射程过宽，S-0060 已判，本席不重开，只独立复现该判定的机械依据**：`Timeline` 的调色对象为 `tl = { ...(theme?.timeline ?? {}), highlightColor: themeHighlightColor(theme) }`（`timeline.js:756-762`），而 `highlightColor: accent` **正在** 上述覆写表内；`resolvePointColor:44`（`status==="active"` → `tl.pointColor ?? highlight`）与 `resolveLineColor:38`（`active` → `colorWithAlpha(highlight,0.38)`）**确实跟随用户调色板**。**故「整条 trace chain 不跟随」为假。** 而本案那一行的 `status` 由 `trace_chain.js:1948-1949` 取值，**只可能是 `"done"` 或 `"pending"`**，二者分别走 `pointDoneColor` / `pointPendingColor` 这两个固定 alpha 灰，**故「本案那一行上仍成立」经本席独立走通确认为真。**
- **来源归类**: **内部来源**（`repository` · 自证类；提出方 `expert-ux`）。

### 三 · `E-0174`（维持 S-0060，本席只作时效性维持与独立复现）

- **评估结论**: **未验证**
- **证据编号**: `E-0174`
- **来源类型**: `general`
- **真实性**: **可核部分全部属实，一处计数只在一种读法下自洽。** `:543-567` 确为 `ErrorPoint` 全块（`:545` 起 `const ErrorPoint`、`:551` `color: "#ef4444"`、`:567` 闭合），引用代码块为已用 `…` 标示的浓缩引用、取值逐字属实；`:1747` `point: <ErrorPoint />` **精确**；`grep -n "point:"` 的 **10 行输出与其枚举逐行完全一致**（`:1059` `:1139` `:1459` `:1529` `:1726` `:1747` `:1802` `:1824` `:1919` `:2012`）；`trace_chain.js:1937-1959` 的 memory_v2 push **确无 `point` 键**（键为 `key`/`title`/`span`/`status`/`unmountDetailsWhenClosed`/`details`）；`#ef4444` 的两个对比度经本席按 WCAG 公式重算为 **3.76:1 / 4.98:1，逐位精确**；`#ef4444` 确为裸 hex、单值双主题、不经 `theme.semantic.danger`。计数「六个」见结论段发现 (六)。
- **可靠性**: **内部来源**（`repository` · 自证类；本关免检失效，本席已逐条复跑）。定位完整、`grep` 可原样粘贴、越界只读已声明。
- **相关性**: **其第 (ii) 项（「补传 `point` 即可让形状出现」）已由 S-0060 判零支撑，本席依传唤书不重审同一问题。** 本席只记明本条经复现后 **确实支持** 的是三项 **形态存在性** 事实：`point` 槽是本仓既定表达方式 · 仓内已有 `ErrorPoint` 且已在同一文件被使用 · memory_v2 那一项未传 `point`。**它不支持任何关于「挂上去就会显示」的可实施性主张** —— 该主张的落点由 `code-owner-ui-primitives`（S-0052）指认为 `timeline.js:809-810` 的一道门，**而本条的 `取得方式` 里没有任何一条命令触及 `timeline.js`**。
- **来源归类**: **内部来源**（`repository` · 自证类；提出方 `expert-ux`）。

---

- **不确定性**:

  **一 · 是否先测后读：是。** 本席在 **打开本案任何案卷文件之前**，先跑了 `git rev-parse HEAD` 与 `git status --porcelain` 于两仓，据此排除主持人表述对本席测量的锚定效应（S-0076 第五节所记的那项自律，本席照做）。结果与 S-0065 第二节一致，并加测一格：PuPu 的 54 行脏改动 **逐行验明无一在 `.claude/` 之外**，六个产品路径（`src` `electron` `unchain_runtime` `public` `scripts` `package.json`）合计 **0 行**。

  **二 · 是否抽验全局时效性：是，且做了两次。** 第二次在本席执行 pytest **之后** —— 因为本席是本块唯一执行了外部进程的角色，须自证 **本席的复核过程本身未污染被复核对象**。两次均为 0（`E-0320`）。

  **三 · 四格分块代价，逐格独立作答（本席未照抄前三块复核者的结论）。**

  **第一格 —— 判词相互参照压低边缘条目被单独怀疑的概率：本席同意，两面都指名。**
  *代价*：一次 `TOP_LEVEL_KEYS` 字面量解析同时结清了 `E-0151`（零测试引用）、`E-0153`（`arbitrary_provider_payload` 不在表内）、`E-0154`（`mode` 在表内）三条。因此 **`E-0153` 没有得到一个独立 instance 会给它的那一问** —— 「本案四个形状各自会新接纳哪些键，`arbitrary_provider_payload` 是否真的一个都不落」。本席读到「不是本案任一形状会接纳的键名」时 **直接当它 settled**，因为白名单刚在眼前，**而这两件事其实不是同一件事**。同型情形另有：`E-0175` 与 `E-0179` 的 `memory_v2_trace_audit.js` 行号，本席是在同一次读取中一并结清的，`E-0175` **未获一次独立的取证路径**。
  *收益*：`E-0154` 的反转 **只可能在同块并置下被发现** —— 本席是在为 `E-0151` 枚举白名单成员时读到 `mode` 在表内，才回头问「那这条测试今天到底是靠什么绿的」，进而去读 `chat_bubble.js` 的挂载判据。**24 个独立 instance 里，负责 `E-0154` 的那一个没有任何理由去打开 `chat_bubble.js`** —— 它不在 `E-0154` 的 `取得方式` 里。
  **故本席的表述与 block 2 复核者相同：相互参照在本块 *既* 制造了一处本可不发生的发现，*也* 制造了至少两处本应发生而未发生的独立追问；两者不相抵，本席无法自证没有第三类。**

  **第二格 —— 深度按条目不均等分配，且分配是本席自己做的：本席同意，并把分配交出供任何人复核本席是否分错。** 三档：
  - **深（跑了外部进程 / 从公式独立重算 / 独立走通了条目之外的调用链）**：`E-0152` · `E-0154` · `E-0157` · `E-0159` · `E-0160` · `E-0172` · `E-0173` · `E-0177` · `E-0178` · `E-0179`
  - **中（每一处被引行号逐行读出并与引文逐字比对）**：`E-0151` · `E-0153` · `E-0155` · `E-0156` · `E-0158` · `E-0161` · `E-0171` · `E-0174` · `E-0175` · `E-0176` · `E-0180` · `E-0182`
  - **浅（复跑其锚点或单条负向枚举，未再展开）**：`E-0150` · `E-0170` · `E-0181`
  **本席记明一处分配风险**：`E-0161` 被本席分在「中」，而它 **支撑一项 `不成立`** 且其来源是文档（传闻类的边界）。本席读了两份文档全文并核对了端点集合，**但未读 `scripts/test-api/` 的实现，未起过该服务** —— 与该条自陈的限制 1 同一条。**若日后需要把「实现里也没有故障注入」抬为事实主张，本席这一关没有为它增加任何证明力。**

  **第三格 —— 编号顺序与提出方共变：本席同意，且本块是该副作用的一个更干净的样本。** 本块 25 条 **恰好由两名发言人构成，且分界线精确落在 `E-0161`/`E-0170` 之间** —— 12 条全部来自 `expert-qa` 的 `S-0023`，13 条全部来自 `expert-ux` 的 `S-0027`，**中间没有一条交叉**。本席在读到第 4 条时已对 `expert-qa` 形成「负向主张一律自标下界、越界只读逐条声明」的先验，读到第 4 条 `expert-ux` 时已形成「对比度算术可信、行号偏差在个位数」的先验。**这两个先验此后一直在替它们背书，25 个独立 instance 不会持有它。** 本席能报出 `E-0154` 与 `E-0179`，**恰恰是因为本席在这两个先验之外各做了一次不该省的功课**（读了条目之外的 `chat_bubble.js`；从公式而非从条目重算）—— **而本席无法自证在别处没有省。**

  **第四格 —— 簇内一致性像交叉验证但误差相关，对作者级系统性缺陷结构性失明：本席同意，并把本块两名提出方各自的作者级风险指名。**
  - `expert-qa`：其全部负向主张（`E-0151` `E-0158` `E-0160` 的三条 `grep`）**同属字面量检索这一个失败类** —— 以变量、模板串或动态拼接构造的标识符一律漏掉。**本席复现了这些数字，一处也没有消除这一类风险**；本席的复跑与它的原跑 **共享同一个盲点**，簇内一致性在这里 **完全不构成交叉验证**。
  - `expert-ux`：其作者级风险是 **WCAG 复算方法本身**（15 个数字全部由抬头声明的同一套公式产出）。**这一格在本块被部分击穿了 —— 见第五格。**

  **第五格（本席新增）—— 第四格的失明不是均匀的：它取决于该证据的复核方式是「复跑作者的命令」还是「从声明的规则重新推导」。**
  > `expert-ux` 的 15 个对比度数字，本席 **没有复跑它的任何脚本**（它也没有提供脚本），而是按其抬头逐字声明的 WCAG 2.x 公式 **自己写了一份实现从头算**。结果：**14 个逐位精确，1 个不可复现，且本席能定位到那 1 个是怎么算出来的。** 换言之，**在一个 13 条全部出自同一作者的簇里，本席仍然抓到了一处作者级的方法不一致** —— 因为验证路径与作者的路径 **在结构上是独立的**。
  >
  > 与之对照：`expert-qa` 的负向 `grep`，本席的验证方式只能是 **把同一条命令再跑一遍**。它的作者级盲点 **一定会被我原样复制**，簇内并置一条也帮不上。
  >
  > **故第四格的准确表述应当是**：分块复核对作者级缺陷的失明，**在「可从公开规则独立重导」的证据上是可以被击穿的，在「只能复跑作者原命令」的证据上是结构性且无法击穿的**。**这不改变第四格成立，但它把风险 *定位* 到了可以被规则处理的地方** —— 一条证据是否给出了「可独立重导的判据」而不只是「可复跑的命令」，**是一个形式要件问题，可以在入卷时就要求**。本席不提修法建议（那是 `codex` 的事），只交出这一次的实测：**同一名提出方、同一批次、13 条 —— 公式类抓到 1/15，命令类抓到 0/3。**

  **四 · 本席自己的取证限制（与被复核证据同类，不因本席是复核者而豁免）。**
  1. **未跑 jest。** 依传唤书「不触发构建」，本席对 `E-0152` **未执行 `react-scripts test`**，改以静态对账（套件集机械枚举 + 用例数 85+3=88）。**故本席证明的是「该运行时观察与静态事实完全自洽」，不是「本席今天重跑得到了同一结果」。**
  2. **跑了 pytest，并说明本席在哪里划的线。** 本席判定 `python -m pytest` 不属「起应用 / 渲染 DOM / 触发构建」，故为 `E-0157` / `E-0159` 复跑了三个解释器；**跑完立即复测产品树，仍为 0 行**（`E-0320`）。**本席据实披露这条界线，其是否合于传唤书由主持人评价，不由本席自证。**
  3. **未起 sidecar、未跑一次真实回合、未抓 SSE、未在运行中的应用里看过任何一条 Memory V2 trace 行、未在浏览器中目视比对任何颜色。** 全部对比度结论属 **对代码文本取值的算术**，与被复核条目自陈的同一条限制一致 —— **计算值不是仪器测量**。
  4. **本席未审查本块之外的任何证据**，未复核 `S-0023` / `S-0027` 的任何专业结论。本席读它们只为确定每条证据「在撑什么」，**未评价其撑得对不对**。
  5. **本席在开工前读了 `S-0072` / `S-0074` / `S-0076`（传唤书要求）。** 这构成一条 **跨块锚定通道**：本席在动手前已知道 block 3 抓到的是「射程过宽的刻画」，**这确实使本席对射程问题更敏感**，本块交出的六项发现里有三项（(三)(五)(六)）正是射程类。**本席无法判断这是提高了灵敏度还是收窄了搜索面。** 四格未覆盖这一条，本席据实记入，不单列为第六格。
  6. **未派生任何子 instance**（[A-012](../../../codex/adaptations.md)）。全程只读，未改产品代码，未 commit，未起应用，未渲染 DOM，未触发构建。**唯一写入为本文件与一份一次性算术脚本（scratchpad，不在任一仓库内）。**
  7. **时效性**：观察时点 **2026-08-08**。两仓锚点经本席独立复跑（先测后读）与主持人 S-0065 第二节完全一致。**闭庭时点若晚于此且任一仓产品树有变动，本块须重取。**

- **请求/下一步**:

  1. **请 `speaker-of-the-house` 记明：本块 22 条 `已验证` 可继续承重；`E-0173` / `E-0174` 维持既有未通过状态**，其重排已由 S-0060 处置完毕，**本席不请求任何新的重排**。
  2. **`E-0154` 为本块新增的未通过条目，依[证据规则第六节](../../../codex/lifecycle/evidence-rules.md) 须重排并显式呈 `chief-judge`。** 受影响面本席只指认、不裁定：该条同时支撑 **QA-1 / QA-2 / U-Q1**，并被用来 **直接回答 `code-owner-shared-arteries` U-S2 的知情请求**；本席认定失效的是「该测试在挂载门上不提供任何保护」这一读法，**「对挂载门变宽失明」这一读法经本席独立走通仍然成立**。**补强责任在提出方 `expert-qa`（[宪法第五条](../../../codex/constitution.md)）。** 请主持人注意：本条与 `SUMMARY` **C5**（「本仓现有测试一条都不会因本案改动变红」）相邻但 **不同一** —— C5 的结论本席未复核也不主张其为假，**失效的是 C5 论证链里关于「为什么不会变红」的其中一条刻画**。
  3. **请把 `E-0333`（`E-0179` 的 8.74 → 8.99 及其成因）显式呈 `chief-judge`。** 该数字 **不改变任何结论**，本席之所以请求显式呈递，是因为它是 **本案唯一一处对 `expert-ux` 复算方法的独立交叉检验结果**：15 个数字里 14 个逐位精确、1 个可定位成因。**这既是对 UX-C4 / UX-V4 所依赖的那批数字的一次正面背书，也是它的准确误差量级** —— 裁决者压在 `3.05:1`（余量 0.05）这个数上时，应当知道同一作者的同一批算术里存在一处 2.8% 的合成不一致，**而 `3.05:1` 那一档已由本席独立全量重扫、与其精确相符**。
  4. **请主持人考虑要求 `expert-ux` 补记 `E-0182` 的 `modeLabel` 正确行号（`:379`）与 `E-0179` 的 light 行数值。** 本席不代改案卷文件。**两处均不影响其结论，故本席不请求重排。**
  5. **本席对 `S-0072` / `S-0074` / `S-0076` 四格的独立回答见「不确定性」三，并新增第五格**（作者级失明的可击穿性取决于验证路径是「重导」还是「重跑」），**请主持人原样转 `codex`，与前四格并列，不要合并成一条。**

---

## 新增证据

### E-0320 | repository + runtime-artifact | 自证类
- **来源定位**: **本席对全局时效性的两次独立测量：一次在打开本案任何案卷文件之前（排除锚定），一次在执行 pytest 之后（自证复核过程未污染被复核对象）。** PuPu `b2385d5dc7951887b6aeebd4001d17b4cd78af83`（`dev`）· unchain `a4e69f413c449c5768433ba4dddc5b60b8146991`。两次 `git status --porcelain -- src electron unchain_runtime public scripts package.json` 均为 **0 行**；PuPu 全仓 54 行脏改动 **逐行验明全部在 `.claude/` 下**（`git status --porcelain | grep -v '^.. \.claude/'` 为空）；unchain 全仓 **0 行**。
- **取得方式**:
  ```
  cd /Users/red/Desktop/GITRepo/PuPu   && git rev-parse HEAD && git status --porcelain | wc -l
  git status --porcelain -- src electron unchain_runtime public scripts package.json | wc -l   # -> 0
  git status --porcelain | grep -v '^.. \.claude/'                                            # -> 空
  cd /Users/red/Desktop/GITRepo/unchain && git rev-parse HEAD && git status --porcelain | wc -l  # -> 0
  ```
- **提交发言**: S-0077
- **支持/反驳**: **第五次独立满足 `E-0001` 完整性限制所要求的重测**；确认 `E-0150` / `E-0170` 的锚点；确认 S-0065 第二节的机械测量；**并证明本席的 pytest 复跑未改动任何产品文件**。
- **完整性限制**: 与 `E-0001` 同一条 —— 只证明各观察时点测得为 0，**不能证明整场庭审期间无并发改动**。不覆盖 `.claude/` 下的案卷文件。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实跑，先测后读）

### E-0321 | repository | 自证类
- **来源定位**: **`E-0150` / `E-0151` / `E-0153` 的机械复现，含一次白名单成员的独立枚举。** presenter sha256 = `9778e5befffdf85634f8c808eed41099a9d5a83842ee6a95306af00efce4c5b0`（与 `E-0010` / `E-0050` / `E-0150` 逐字符相同）；`src/SERVICEs/runtime_events/*.test.js` = **7** · `src/SERVICEs/chat_storage/*.test.js` = **12** · `src/COMPONENTs/chat-bubble/*.test.js` = **12**，**文件名逐个与 `E-0151` 吻合，合计 31**；`grep -rn "TOP_LEVEL_KEYS" src/` = **2 行，全在 presenter 自身**；`grep -rn "unchain_context_status\|unchain_shadow_status" src/` = **0**；**`TOP_LEVEL_KEYS` 成员本席逐个数得 59 项**，`mode` 为第 5 项、`reason` 为第 9 项、**`arbitrary_provider_payload` 不在其中**。`trace_chain.memory_v2.test.js` = **959 行、15 个 `test()`**（`E-0154` 记「16 个」）。`chat_storage_memory_v2_trace.test.js` 的 `arbitrary_provider_payload` 实在 **`:23`**（`E-0153` 记 `:24`）。
- **取得方式**:
  ```
  shasum -a 256 src/SERVICEs/runtime_events/memory_v2_trace_presenter.js
  ls src/SERVICEs/runtime_events/*.test.js src/SERVICEs/chat_storage/*.test.js src/COMPONENTs/chat-bubble/*.test.js
  grep -rn "TOP_LEVEL_KEYS" src/ ; grep -rn "unchain_context_status\|unchain_shadow_status" src/
  sed -n '9,69p'  src/SERVICEs/runtime_events/memory_v2_trace_presenter.js
  cat src/SERVICEs/chat_storage/chat_storage_memory_v2_trace.test.js
  ```
- **提交发言**: S-0077
- **支持/反驳**: 支持 `E-0150` / `E-0151` / `E-0153` 的复核结论；**记录两处子计数与行号偏差**（959 行文件的 15 vs 16、`:23` vs `:24`），**均不改变任何结论**。
- **完整性限制**: 1. 负向检索为 **字面量 `grep`**，与被复核条目同一失败类 —— 以变量或拼接构造的常量名一律漏掉，故「0」的准确措辞是「在字面量检索范围内为 0」。2. 「15 个 `test()`」为静态调用点计数，**未含 `test.each` 展开**（见 `E-0322`）。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实跑）

### E-0322 | repository | 自证类
- **来源定位**: **`E-0152` 这条须查类运行时观察的静态对账 —— 在不执行 jest 的前提下，其两个报数均可从仓库文本机械重建。**
  - **套件集**：`--testPathPattern="memory_v2|runtime_events"` 机械选出 **15** 个 `*.test.js`，**与 `E-0152` 的 PASS 清单文件对文件完全相同**（含其中 3 个落在 `E-0151` 三目录之外的：`src/electron/tests/main/memory_v2_rollout` · `…startup_readiness` · `src/PAGEs/chat/hooks/use_chat_stream.memory_v2_payload`）
  - **用例数**：15 个文件的静态 `test(`/`it(` 调用点合计 **70**；其中两个 electron 文件各只有一行 `require("…/*.test.cjs")`（计 0），其真身 `electron/tests/main/memory_v2_rollout.test.cjs` = **8** · `…startup_readiness.test.cjs` = **7** → 小计 **85**；再加 `src/SERVICEs/runtime_events/memory_v2_trace_presenter.test.js:104` 的 `test.each([…])` **3 行表**（该调用点未被上述计数捕获，运行时展开为 3 个用例）→ **85 + 3 = 88**，**与 `E-0152` 所载 `Tests: 88 passed` 精确相等**
- **取得方式**:
  ```
  find src -name "*.test.js" | grep -E "memory_v2|runtime_events" | sort           # -> 15
  for f in $(...); do grep -cE "(^|[^a-zA-Z.])(test|it)\s*\(" "$f"; done           # -> 70
  grep -cE "(^|[^a-zA-Z.])(test|it)\s*\(" electron/tests/main/memory_v2_*.test.cjs # -> 8 / 7
  sed -n '104,110p' src/SERVICEs/runtime_events/memory_v2_trace_presenter.test.js  # -> test.each 3 行表
  ```
- **提交发言**: S-0077
- **支持/反驳**: **在不触发构建的前提下为 `E-0152` 提供最强可得的独立佐证** —— 其套件集完全可复现，其用例数可从文本精确重建。**不替代对 pass/fail 与耗时的执行观察**。
- **完整性限制**: 1. **本条不是一次执行观察。** 「15 suites / 88 tests」的 **结构** 被复现，**「全部通过」这一事实本席未复跑**，依[证据规则第四节](../../../codex/lifecycle/evidence-rules.md)，须查类无保管链这一点不因本条而消除。2. 静态计数的正则 **不捕获 `.each` 形态**，本条已单独校正这一个实例；**若另有本席未察觉的动态生成用例，计数会偏**（本条为对账，非独立计数）。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实跑）

### E-0323 | repository | 自证类
- **来源定位**: **`chat_bubble.memory_v2_mount.test.js` 的绿灯完全由真实 `isMemoryV2TraceBundle` 产生 —— 与 `E-0154` 所述相反。**
  - `pupu:src/COMPONENTs/chat-bubble/chat_bubble.js:3` `import TraceChain from "./lazy_trace_chain";` · **`:10` `import { isMemoryV2TraceBundle } from "../../SERVICEs/runtime_events/memory_v2_trace_presenter";`（未被该测试 mock）**
  - `:101` `const hasVisibleTraceActivity = hasToolActivity;` · `:102-106` `hasTokenSummary = isAssistant && message.status === "done" && typeof …consumed_tokens === "number" && …> 0;`
  - **`:107-108` `const hasMemoryV2Audit = isAssistant && isMemoryV2TraceBundle(message.meta?.bundle?.memory_v2);`**
  - `:109-110` `const shouldRenderTraceChain = hasVisibleTraceActivity || hasTokenSummary || hasMemoryV2Audit;` · `:123` `{isAssistant && shouldRenderTraceChain && (<TraceChain … bundle={message.meta?.bundle} />)}`
  - **该测试的 fixture**（`:33-39`）`status:"done"`、**无 `consumed_tokens`** → `hasTokenSummary` 假；`traceFrames={[]}` → `hasVisibleTraceActivity` 假。**故 `shouldRenderTraceChain === hasMemoryV2Audit`，即真实 `isMemoryV2TraceBundle({mode:"active"})` 的返回值。**
  - `presenter:414-415` `export const isMemoryV2TraceBundle = (raw) => sanitizeMemoryV2TraceBundle(raw) !== null;`
  - `character_chat_bubble.js:3` / `:10` / `:139` **同构**（第二条 `test()` 走这一条）
- **取得方式**:
  ```
  grep -n "isMemoryV2TraceBundle\|import TraceChain" src/COMPONENTs/chat-bubble/chat_bubble.js \
                                                     src/COMPONENTs/chat-bubble/character_chat_bubble.js
  sed -n '85,143p' src/COMPONENTs/chat-bubble/chat_bubble.js
  sed -n '1,61p'  src/COMPONENTs/chat-bubble/chat_bubble.memory_v2_mount.test.js
  grep -n "isMemoryV2TraceBundle" -A 2 src/SERVICEs/runtime_events/memory_v2_trace_presenter.js
  ```
- **提交发言**: S-0077
- **支持/反驳**: **反驳** `E-0154` 「净效果」段第一个连言支（「该测试既不经过 `isMemoryV2TraceBundle` 的真实实现」）；**不反驳** 其第二个连言支与其结论（「对挂载门变宽失明」）—— 后者本席独立走通仍成立。**并同时限定 `E-0154` 可支持的射程**：它可支持「对放宽失明」，不可支持「对该门无覆盖」。
- **完整性限制**: 1. **本条为静态分支结构判定，本席未在 jest 下运行该测试**（依传唤书不触发构建）。若 `@testing-library/react` 或 CRA 的 moduleNameMapper 存在某种本席不知道的自动替换使 presenter 也被替身，则结论方向会反转 —— **本席检查了该测试的三个 `jest.mock` 调用，无一指向 presenter，`package.json` 与 `src/setupTests.js` 亦无相关映射**，但未穷举 CRA 内置配置。2. **本条不主张 `E-0154` 的结论为假**，只主张其两个理由中的一个不实。3. `src/COMPONENTs/chat-bubble/**` 属 `code-owner-chat-bubble` 边界，**越界只读，本条不主张任何改法**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 读取，行号与原文可复核）

### E-0324 | repository + runtime-artifact | 须查类
- **来源定位**: **`E-0155`~`E-0159` 的逐行复核与三解释器复跑。**
  - **`E-0155`**：`test_memory_v2_context_reference_policy.py:132-141` 确为 `bind_pupu_context_module(… partial_attempt_sink=lambda boundary, source, error: partials.append((boundary, source, error)) …)`，**逐字精确**；`:873` `assert diagnostics["journal_status"] == "partial"` · `:876` `assert diagnostics["persistence_error_code"] == "contract_invalid"` **精确**；引文首行 `assert len(partials) == 1` 实在 **`:789`**（条目记 `:790`）
  - **`E-0156`**：`:899` 起为 `def test_active_persistence_failure_marks_partial_without_raw_error`，引文 **逐字属实**，三条断言实在 `:908` / `:909` / `:910`（条目记范围止于 `:908`）
  - **`E-0157` 复跑**：系统 `python3`（3.11.7）`import unchain` → `ModuleNotFoundError: No module named 'unchain'`；PuPu `.venv`（3.12.3）**同一错误**；unchain `.venv`（3.12.11）**成功**。`pytest tests/test_memory_v2_context_reference_policy.py -q` → **`20 passed in 1.30s`**；`tests/test_memory_v2_context.py -q -k "partial"` → **`1 passed, 39 deselected in 0.06s`**。`ls -d PuPu/.venv/…/unchain*` → **无匹配**；`unchain/.venv/…/unchain-0.2.0.dist-info` → **存在**
  - **`E-0158`**：两处 `conftest.py` **均不存在**；`find -maxdepth 3` 三种 pytest 配置（排除 `node_modules`）**零命中**；`package.json` 内 `pytest` **零命中**（7 行 `unchain_runtime` 命中全为构建脚本与打包路径）；`unchain_runtime/scripts/` **恰两个构建脚本**
  - **`E-0159`**：`--collect-only -q` → **`111 tests collected in 0.40s`**；`test_chat_stream_v4.py:554` `"type": "stream_summary"` · `:598` `assertEqual(done_payload["bundle"]["memory_v2"]["mode"], "active")` **精确**；`test_models_catalog_route.py:374` `assertNotIn("stream_summary", event_types)` **精确**；`test_unchain_adapter_capabilities.py:1821` `events = list(` 精确，但该 `assertEqual` 块实止于 **`:1848`**（条目记 `:1845`），且其代码块为 **重排浓缩引用**（已用 `…` 标示）
- **取得方式**:
  ```
  sed -n '128,145p;786,800p;868,880p' unchain_runtime/server/tests/test_memory_v2_context_reference_policy.py
  sed -n '895,912p'                   unchain_runtime/server/tests/test_memory_v2_context.py
  python3 -c "import unchain"; /…/PuPu/.venv/bin/python -c "import unchain"; /…/unchain/.venv/bin/python -c "import unchain"
  cd unchain_runtime/server && /…/unchain/.venv/bin/python -m pytest tests/test_memory_v2_context_reference_policy.py -q
                                /…/unchain/.venv/bin/python -m pytest tests/test_memory_v2_context.py -q -k "partial"
                                /…/unchain/.venv/bin/python -m pytest tests/test_unchain_adapter_capabilities.py --collect-only -q
  ls unchain_runtime/server/conftest.py unchain_runtime/server/tests/conftest.py
  find . -maxdepth 3 \( -name pytest.ini -o -name setup.cfg -o -name pyproject.toml \) | grep -v node_modules
  grep -n "pytest\|unchain_runtime" package.json ; ls unchain_runtime/scripts/
  ```
  观察者 `evidence-examiner`，2026-08-08
- **提交发言**: S-0077
- **支持/反驳**: **执行确认** `E-0157` / `E-0159` 的运行时观察；逐行确认 `E-0155` / `E-0156` / `E-0158`；**记录三处轻微行号/范围偏差**（`:789` vs `:790` · `:908` vs `:910` · `:1845` vs `:1848`），**均不改变任何结论**。
- **完整性限制**:
  1. **须查类部分无保管链。** 三份 virtualenv 与解释器均为本机文件，随安装状态改变。**不得据此推断任何 CI 或他人机器上的结果。** 耗时 1.30s vs 条目所载 1.23s，属该类证据的固有差。
  2. **本席与 `E-0157` 同一限制：只试了三个解释器**，未穷举是否存在第四种能 collect 的配置。
  3. **`--collect-only` 不是执行观察** —— 111 是收集计数，不是 111 条全绿。
  4. 全部文件属 `code-owner-runtime` 边界，**越界只读；本席未修改任何文件，跑完立即复测产品树为 0 dirty**（`E-0320`）。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 读取 + 实跑）

### E-0325 | repository | 自证类
- **来源定位**: **`E-0160` 的逐行复跑，并对照 S-0055 认定的 `E-0020` 反转项。**
  - `electron/main/services/chat_storage/service.js:280-289` 为 `replaceMessages` **全函数**，`E-0160` 的引用代码 **逐字精确**；`:283` `"INSERT INTO messages(chat_id, ord, payload) VALUES (?, ?, ?)"` 为该目录下 **唯一** 的 `INSERT INTO messages`（`grep` 得 1 命中）
  - `:334` `const applyImportStore = (op) => {` · `:337-338` 注释逐字含 `rejects the entire store if ANY incoming chat id violates the frozen id contract` · `:339` `const store = assertRecognizableLegacyChatStore(op.store);` · `:340` `const chatsById = store.chatsById || {};` —— **`:334-340` 精确**
  - `:494` `const migrateLegacyFileIfNeeded = () => {` · `:495` `if (!isDbEmpty()) return;` · `:496` `if (!fs.existsSync(legacyFilePath)) return;` · `:499` `JSON.parse` · `:512` `assertRecognizableLegacyChatStore(store)` · `:521` `applyOps([{ type: "import_store", store }])` · `:522` `fs.renameSync(…, + MIGRATED_SUFFIX)` · `:523` 闭合 —— **六步顺序与 `E-0160` 所述逐条吻合**（其范围记 `:494-522`，函数实闭于 `:523`）
  - `:538-542` `// Legacy-compat entry point for the renderer localStorage→IPC migration path (WRITE channel): whole-store import.` / `const write = (store) => { applyOps([{ type: "import_store", store }]); };` —— **`E-0160` 完整性限制 3 所指的第二个派发点确实存在且无 `isDbEmpty()` 约束**
  - `grep -rn "sanitizeMessages\|sanitizeMessage(" electron | wc -l` → **0**
  - **对照**：`E-0160` **通篇未主张 `replaceMessages` 各调用点的 op type 归属**，故 **未重复 S-0055 认定 `E-0020` 反转的那一处（`:313` 实为 `applyPutMessages`、`:386` 才是 `applyImportStore`）**
- **取得方式**:
  ```
  sed -n '278,292p;332,342p;492,524p;536,545p' electron/main/services/chat_storage/service.js
  grep -rn "INSERT INTO messages\|REPLACE INTO messages" electron/main/services/chat_storage/*.js
  grep -rn "sanitizeMessages\|sanitizeMessage(" electron | wc -l
  ```
- **提交发言**: S-0077
- **支持/反驳**: 支持 `E-0160` 的复核结论；**并显式记录一项对裁决者有用的对比**：同一条链路上的两条证据，`E-0020`（`code-owner-shared-arteries`）被判 **相矛盾**，`E-0160`（`expert-qa`）**逐行精确** —— **差别在于后者只主张它实际读到的那一段，未推断调用点归属。**
- **完整性限制**: 1. **本条只证明路径与行号，不证明任何一行历史数据里真的含那四个键** —— 与 `E-0160` 完整性限制 1 同一条，本席同样 **未** 打开任何 `chats.json`、`.migrated-bak` 或数据库。2. `grep` 为字面量检索。3. `electron/main/**` 属 `code-owner-electron` 边界，**越界只读**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 读取，行号与原文可复核）

### E-0326 | repository | 自证类
- **来源定位**: **`E-0161` 的端点集合复核，并核到该条「唯一路径」句的射程依据。**
  - `docs/api-reference/test-api.md`：`### Chat lifecycle`（`:28-33` 六个端点）· `### Messages and async runs`（`:39-43` 五个）· `### Catalog and selection`（`:55-60` 六个）· `### Errors`（`:69`）—— **与 `E-0161` 的枚举逐项吻合**；`:30` `GET /chats/:id` 的返回字段 `{id, title, model, character_id, toolkits, messages}` **逐字精确**
  - `docs/api-reference/test-api-debug.md`：`:5` `GET /debug/state` · `:41` `GET /debug/logs` · `:61` `GET /debug/screenshot` · `:71` `POST /debug/eval` · `:100` `GET /debug/dom` —— **五个，与 `E-0161` 逐项吻合**
  - **负向**：两份文档中 `inject` / `fault` / `simulate` 的全部命中经本席逐条读出，**无一与故障注入相关**（全部是 `fails closed` / `default` / `will fail` 等无关用法）
  - **「唯一路径」句的依据**：`test-api-debug.md:102` 逐字为 `Convenience wrapper for `eval`. Returns the `outerHTML` of a single element matching the selector.`；`GET /debug/state` 的返回体（`:12-38` 的 JSON 样例）为定长字段快照（`active_chat` / `inspected_chat` / `current_model` / `toolkits_active` / `character_id` / `modal_open` / `is_streaming` / `route` / `window_state` / `catalog_loaded`），**不含任何 trace 字段**
- **取得方式**:
  ```
  grep -nE "^#{1,4} |^\| *`?(GET|POST|PATCH|DELETE)" docs/api-reference/test-api.md
  grep -nE "^#{1,4} |^\| "                            docs/api-reference/test-api-debug.md
  grep -niE "inject|fault|fail|error simulat|simulate" docs/api-reference/test-api.md docs/api-reference/test-api-debug.md
  sed -n '1,40p;100,112p' docs/api-reference/test-api-debug.md
  ```
- **提交发言**: S-0077
- **支持/反驳**: 支持 `E-0161` 的复核结论；**并为其「读取渲染状态的唯一路径是 `POST /debug/eval`」一句给出射程依据** —— 该句 **在文档自身措辞下成立**（`/debug/dom` 自称是 eval 的 wrapper，`/debug/state` 不含 trace 字段），**不成立于「实现里也如此」**。
- **完整性限制**: 1. **本条来源同为文档，依[证据规则第三节](../../../codex/lifecycle/evidence-rules.md) 属传闻类的边界情形** —— 本条 **严格证明的是「这两份文档如此写」**，不是「实现如此」。**本席未读 `scripts/test-api/` 的实现，未起过该服务。** 2. 两份文档属 `code-owner-devtools` 边界，**越界只读**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 读取端点清单；**其证明力依第 1 条限制**）

### E-0327 | repository | 自证类
- **来源定位**: **`E-0171` / `E-0172` 的取值与算术全量独立复算（本席自写实现，未复用提出方任何脚本）。**
  - **取值**：`default_mini_theme.json:2` `"light_mode"` · `:284-294` light `timeline` 块 · `:318` `"dark_mode"` · `:600-610` dark `timeline` 块 —— **四处范围精确，表内 9 组 18 个取值逐字无一偏差**；`timeline` 块内 **无 `pointColor`、无 error/danger 键**；**但全文件有两个 `errorAccent`（`:252` `#DC3545` · `:568` `#F87171`），二者均在 `modal` 块内**
  - **解析链**：`timeline.js:24` `DEFAULT_DOT_R = 5` · `:34-40` `resolveLineColor` · `:42-48` `resolvePointColor`（`:45-47` done→`pointDoneColor`，缺省→`pointPendingColor`）· `:64-77` `DotDefault`（`border: 1px solid`、`background:"transparent"`、`10×10`）· `:756-762` `tl` 构造 · `:742` props 文档 `status : "done"|"active"|"pending"` **逐字**；`trace_chain.js:1937-1959` push 的六个键 **确无 `point`**；`chat_bubble.js:170-176` **仅 `isUser && !isEditing` 上底色**；`semantic_tokens.js:242` `#ffffff` / `:255` `#121212` —— **全部精确**
  - **算术（本席按 WCAG 2.x 公式自写实现重算）**：light done `#C7C7C7` **1.69:1** · light pending `#D1D1D1` **1.53:1** · dark done `#464646` **1.99:1** · dark pending `#3D3D3D` **1.72:1**；8 位差 **light 10/255 · dark 9/255**；标题 **15.91:1 / 11.67:1**；span **3.35:1 / 4.53:1** —— **十项逐位与 `E-0172` 相符**
- **取得方式**:
  ```
  sed -n '2p;283,296p;318p;599,612p' src/BUILTIN_COMPONENTs/theme/default_mini_theme.json
  grep -n "pointColor\|error\|danger"  src/BUILTIN_COMPONENTs/theme/default_mini_theme.json
  sed -n '22,80p;738,748p;754,765p'    src/BUILTIN_COMPONENTs/timeline/timeline.js
  sed -n '1935,1960p'                  src/COMPONENTs/chat-bubble/trace_chain.js
  sed -n '85,185p'                     src/COMPONENTs/chat-bubble/chat_bubble.js
  sed -n '239,270p'                    src/BUILTIN_COMPONENTs/theme/semantic_tokens.js
  # 对比度：L = 0.2126R+0.7152G+0.0722B（线性化后），ratio=(L_亮+0.05)/(L_暗+0.05)，半透明先合成
  ```
- **提交发言**: S-0077
- **支持/反驳**: **独立复现并确认** `E-0171` / `E-0172` 的全部取值与全部数字；**限定** `E-0171` 「该 JSON 无任何 error / danger 键」一句的射程（**及于 `timeline` 块为真，及于整个文件为假**）；**独立佐证 S-0046 此前实测的 8 位差 10/255 · 9/255**。
- **完整性限制**: 1. **为计算值非仪器测量**，**本席未在浏览器中目视比对任何颜色** —— 与被复核条目同一限制。2. 数字锚定在默认调色板上，**用户自定义 `background` 后须重算**。3. **本席不就「对比度是否达标」发表任何意见** —— 该判断属 `expert-ux`，本条只复核取值与算术。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实读 + 独立重算）

### E-0328 | repository | 自证类
- **来源定位**: **`E-0173` 覆盖表的全量枚举，及其射程过宽的机械反例。** `applySemanticPaletteToTheme` 全文实为 **`:211-303`**（`:211` 声明、`:303` 闭合），其返回对象显式覆写的顶层键 **完整枚举**：`:233` `highlightColor: accent` · `:234` `color: text` · `:235` `backgroundColor: background` · `:236` `foregroundColor: surface` · `:237` `icon` · `:238` `font` · `:239` `input` · `:245` `select` · `:256` `modal` · `:275` `switch`，`deepTier` 分支下 `:287` `code` · `:288` `textfield` · `:295` `markdown` —— **与 `E-0173` 的枚举逐项一致，无遗漏无多列，`timeline` 确不在其中**；`container.js:165` 为调用点；`grep -rn "timeline:" src/` **零命中**。**反例**：`timeline.js:756-762` 的 `tl` **携带 `highlightColor: themeHighlightColor(theme)`**，而 `highlightColor` **正在覆写表内**，故 `resolvePointColor:44`（`active` → `tl.pointColor ?? highlight`）与 `resolveLineColor:38`（`active` → `colorWithAlpha(highlight,0.38)`）**跟随用户调色板**。**本案那一行的 `status` 由 `trace_chain.js:1948-1949` 取值，只可能是 `"done"` 或 `"pending"`**，故走固定 alpha 灰。
- **取得方式**:
  ```
  sed -n '211,303p' src/CONTAINERs/config/theme_semantic.js | grep -nE "^\s{4}[a-zA-Z]+:"
  sed -n '296,306p' src/CONTAINERs/config/theme_semantic.js
  grep -n "applySemanticPaletteToTheme" src/CONTAINERs/config/theme_semantic.js src/CONTAINERs/config/container.js
  grep -rn "timeline:" src/     # -> 零命中
  sed -n '34,48p;756,762p' src/BUILTIN_COMPONENTs/timeline/timeline.js
  sed -n '1946,1950p' src/COMPONENTs/chat-bubble/trace_chain.js
  ```
- **提交发言**: S-0077
- **支持/反驳**: **独立复现 S-0060 对 `E-0173` 的两半判定** —— 其枚举精确；其「整条 trace chain 不跟随主题」射程过宽；**本案那一行上仍成立**。本席据此维持 **未验证**，**不重开该问题**。
- **完整性限制**: 1. **本条只走静态解析链，未在运行中的应用里观察任何一个像素。** 2. **未核实** 是否另有第三处把 `timeline` 注入 theme 的通路（`grep "timeline:"` 为字面量检索，JSON 侧用的是 `"timeline":`，本席两种形态都查了，但以变量拼接的注入会漏）。3. 全部文件属 `code-owner-shared-arteries` / `code-owner-ui-primitives` 边界，**越界只读**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实跑）

### E-0329 | repository | 自证类
- **来源定位**: **`E-0174` 的 `point` 槽全量枚举与其「六个」计数的唯一自洽读法。** `grep -n "point:" src/COMPONENTs/chat-bubble/trace_chain.js` 实得 **10 行，与 `E-0174` 的枚举逐行完全一致**：`:1059` `<AccentPoint …/>` · `:1139` `<HammerPoint …/>` · `:1459` `<SubagentPoint …/>` · `:1529` `<SubagentPoint …/>` · `:1726` `toolPointEl` · **`:1747` `<ErrorPoint />`** · `:1802` `"loading"` · `:1824` `"loading"` · `:1919` `<HammerPoint …/>` · `:2012` `"end"`。**分解**：字符串预设 **3**（`"loading"`×2 · `"end"`）· 元素值 **7** · 不同的自定义组件 **5**（`AccentPoint` / `HammerPoint` / `SubagentPoint` / `toolPointEl` / `ErrorPoint`）。**「六」= 7 − `ErrorPoint` 自身**，即该条第 1 点在排除其第 2 点单论的 `ErrorPoint` 之后的计数 —— **该读法自洽，其余读法（7 / 5 / 10）均不等于 6**。`:543-567` 为 `ErrorPoint` 全块，`:551` `color: "#ef4444"`；`#ef4444` 对比度经本席重算 **3.76:1（on `#ffffff`）/ 4.98:1（on `#121212`）**，与其所载 **逐位精确**。
- **取得方式**:
  ```
  grep -n "point:" src/COMPONENTs/chat-bubble/trace_chain.js
  sed -n '543,568p;1745,1749p;1935,1960p' src/COMPONENTs/chat-bubble/trace_chain.js
  ```
- **提交发言**: S-0077
- **支持/反驳**: **独立复现 `E-0174` 的全部可核部分**；**限定** 其「六个」的读法为唯一自洽者。**不重开** S-0060 已判的第 (ii) 项。
- **完整性限制**: 1. **字面量 `grep "point:"`**，以变量或展开构造的 prop 会被漏掉，故 10 为下界。2. **本条不主张任何形态挂得上去** —— 该主张的落点在 `timeline.js:809-810`（`code-owner-ui-primitives` S-0052 指认），**而 `E-0174` 与本条的取得方式均未触及 `timeline.js`**。3. `trace_chain.js` 属 `code-owner-chat-bubble` 边界，**越界只读**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实跑 + 独立重算）

### E-0330 | repository | 自证类
- **来源定位**: **`E-0175` / `E-0176` 的行号复核，及 `E-0176` 消费者清单的下界性质。**
  - **`E-0175`**：`memory_v2_trace_audit.js:196-202` 的 `{reader.error ? (<div role="alert" style={{ fontSize: 11, color: "var(--pupu-danger, #c44)" }}>{reader.error}</div>) : (` **逐字精确**；`Error code` 行本体在 `:347-352`（条目记 `:344-352`，前 3 行为相邻的 `Compression`/`Source range`/`Reason`），两者相距 **148 行**（「约 145」属实）；**一处偏一行**：`role="status"` 先例实在 `memory_v2_journal_reload.js:575`（条目记 `:574`）
  - **`E-0176`**：`semantic_tokens.js:239-266` 为 `SEMANTIC_DEFAULTS` **全块，六组 token 取值逐字精确**；`:107` `warning … phase:"P1"` · `:108` `danger … phase:"shipped"` **精确**；`theme_semantic.js:159-172` 发出 `--pupu-{varName}` 与 `-rgb`、`:232` `semantic` 挂进 theme、`container.js:274` / `:368` 两处调用点 **全部精确**
  - **无条件执行**：`container.js:152` `themeColorCustomizationEnabled = false` · `:157-159` `const themeSettings = themeColorCustomizationEnabled ? readThemeSettings() : defaultThemeColorSettings();` · `:160-165` `resolveSemanticPalette(...)` 与 `applySemanticPaletteToTheme(base, semantic, themeMode)` **无任何条件包裹** —— **该 flag 只决定读哪份 settings，不决定是否下发调色板，`E-0176` 的这一主张成立**
  - **消费者清单为下界**：本席复跑得两条该条未列的 `var(--pupu-danger)` 消费者 —— `src/COMPONENTs/chat-bubble/artifact-summary/files_changed_card.js:97` · `src/COMPONENTs/toolkit/pages/plugin_detail_page.js:477`；其所列 `toast_host.js:100` 经核 **成立**（`src/BUILTIN_COMPONENTs/toast/toast_host.js:100` `error: semantic.danger || "#d64545",`）
- **取得方式**:
  ```
  sed -n '194,204p;340,354p;35,70p' src/COMPONENTs/chat-bubble/memory_v2_trace_audit.js
  sed -n '572,576p'                 src/COMPONENTs/chat-bubble/memory_v2_journal_reload.js
  sed -n '100,112p;239,270p'        src/BUILTIN_COMPONENTs/theme/semantic_tokens.js
  sed -n '159,172p;230,234p'        src/CONTAINERs/config/theme_semantic.js
  sed -n '150,180p;272,276p;366,370p' src/CONTAINERs/config/container.js
  grep -rn "pupu-danger\|semantic?.danger" src/ | grep -v "\.test\.js"
  sed -n '96,104p' src/BUILTIN_COMPONENTs/toast/toast_host.js
  ```
- **提交发言**: S-0077
- **支持/反驳**: 支持 `E-0175` / `E-0176` 的复核结论；**限定** `E-0176` 消费者清单为 **下界**（至少漏 2）—— **该漏列只增强其存在性主张，不削弱它**。
- **完整性限制**: 1. 检索为字面量 `grep`；以变量或模板串构造的 `--pupu-danger` 引用会被漏掉，**故本席的「至少漏 2」同样是下界**。2. **本条不主张 `E-0176` 的漏列有任何后果** —— 它是一条存在性证据。3. 相关文件分属 `code-owner-chat-bubble` / `code-owner-ui-primitives` / `code-owner-shared-arteries` 边界，**越界只读**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实跑）

### E-0331 | repository | 自证类
- **来源定位**: **`E-0177` 的全 9 预设 × 2 模式 × 3 核算面独立全量扫描（本席自写解析与算术，未复用提出方任何数字）。** 预设经本席从 `semantic_tokens.js` 机械解析得 **9 套**（`:269` `default` 指向 `SEMANTIC_DEFAULTS` · `:270` `ocean` · `:298` `warm` · `:326` `high_contrast` · `:366` `graphite` · `:394` `violet` · `:422` `rose` · `:450` `nord` · `:478` `midnight`），**与 `E-0177` 的列举逐个吻合**。扫描结果：

  | 核算面 | 本席实测下界 | 绑定预设 | `E-0177` 所载 |
  |---|---|---|---|
  | `danger ↔ background` | **3.05:1** | `nord` 暗色 `#bf616a` on `#2e3440` | **3.05:1，同一预设同一取值** |
  | `danger ↔ sidebar` | **2.46:1** | `nord` 暗色 `#bf616a` on `#3b4252` | **2.46:1，同一** |
  | `danger ↔ surface` | **2.11:1** | `nord` 暗色 `#bf616a` on `#434c5e` | **2.11:1，同一** |

  `default` 三档实测 **4.53:1 / 6.77:1 / 6.03:1**，与其所载 **逐位相同**；`sidebar` 次低几档实测 **3.97（`warm` 亮）· 4.09（`violet` 亮）· 4.11**，落在其所述「3.97–4.12」内；**`nord` 暗色在三个核算面上均为最小值，即其「唯一的绑定约束」成立**。`nord` 暗色的四个取值经本席读源确认：`danger:"#bf616a"` · `background:"#2e3440"` · `sidebar:"#3b4252"` · `surface:"#434c5e"`。
- **取得方式**:
  ```
  sed -n '450,478p' src/BUILTIN_COMPONENTs/theme/semantic_tokens.js
  grep -n "^  [a-z_]*: {" src/BUILTIN_COMPONENTs/theme/semantic_tokens.js
  # 解析 SEMANTIC_PRESETS 的 9×2 调色板，对 danger 与 background/sidebar/surface 两两求 WCAG 比，取每面最小值
  ```
- **提交发言**: S-0077
- **支持/反驳**: **独立复现并确认 `E-0177` 的全部三个下界、其绑定预设、其 `default` 三档与其次低档区间** —— **本条是本案对 `expert-ux` 复算方法最强的一次正面交叉检验**：一次全量重扫，零偏差。
- **完整性限制**: 1. **为计算值非仪器测量。** 2. **只覆盖出厂预设**；用户自定义 `danger` 或 `background` 后须重算。3. **本条不就 UX-C4 / UX-V4 是否应当采纳发表任何意见** —— 只复核数字。4. **本席的预设解析为正则解析**，若某预设以本席未预期的写法定义，会被漏掉；本席以「解析到 9 套且名称与源文件 `grep` 到的 9 个键逐个吻合」自校，**但未逐字节比对每套的全部字段**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 独立全量重扫）

### E-0332 | repository | 自证类
- **来源定位**: **`E-0178` 五项判据取值的逐条复核，含三项该条未给出而结论依赖的前提。**
  - `timeline.js:281-320` 为 `detail` 按钮全块（`:281` `{hasDetails && (` · `:320` `)}`）；`:288` `padding:"0"` · `:289` `background:"transparent"` · `:290` `border:"none"` · `:293` `color: tl.seeDetailsColor ?? "rgba(0,0,0,0.35)"` · `:295` `outline:"none"` · `:300-302` `onMouseEnter → opacity "0.6"` · `:307` `{isExpanded ? "hide" : "detail"}` · `:308-318` 14×14 `<Icon>` —— **全部精确**
  - **`:20` `const TITLE_LINE_H = 18;`**，故 `:256` `lineHeight: \`${TITLE_LINE_H}px\`` = **18px 属实**
  - **`timeline.js` 全文 `aria-` 零出现** → 无 `aria-expanded` / `aria-controls` **属实**
  - **`src/App.css` · `src/index.css` · `public/index.html` 中 `:focus` / `focus-visible` 零命中** → 「内联样式，无伪类可用」**属实**（该条未给出这一步，本席补核）
  - **`spanFontSize` 在 `default_mini_theme.json` 中不存在**，且 `compact` 在本案路径为 **假**（`chat_bubble.js` 的 `<TraceChain>` 不传 `compact`；`trace_chain.js:643` 默认 `compact = false`）→ **11px 成立**（该条未给出这两步，本席补核）
  - **算术**：`rgba(0,0,0,0.35)` on `#ffffff` = **2.44:1** · `rgba(255,255,255,0.35)` on `#121212` = **3.21:1** · hover 有效 α=0.21 → **1.65:1 / 1.92:1**；降幅 **1−1.65/2.44 = 32.4%** · **1−1.92/3.21 = 40.2%** —— **五个数字逐位与其所载相符**
  - **一处**：其引文标「逐字」，但把 `:292` 的 `fontSize: tl.spanFontSize ?? (compact ? "10px" : "11px")` 省作 `?? "11px"`。**本案路径上取值不变**
- **取得方式**:
  ```
  sed -n '20p;254,258p;281,320p' src/BUILTIN_COMPONENTs/timeline/timeline.js
  grep -n "aria-"   src/BUILTIN_COMPONENTs/timeline/timeline.js      # -> 零命中
  grep -rn "focus-visible\|:focus" src/*.css public/index.html       # -> 零命中
  grep -n  "spanFontSize" src/BUILTIN_COMPONENTs/theme/default_mini_theme.json  # -> 零命中
  grep -n  "compact" src/COMPONENTs/chat-bubble/trace_chain.js ; sed -n '123,143p' src/COMPONENTs/chat-bubble/chat_bubble.js
  ```
- **提交发言**: S-0077
- **支持/反驳**: 支持 `E-0178` 的复核结论，**并补齐其结论所依赖但未出证的三个前提**（`TITLE_LINE_H=18` · 无全局 `:focus` 规则 · `compact` 在本案路径为假故 11px 成立）。**本条不就 SC 2.4.7 / 4.1.2 / 2.5.8 / 1.4.3 是否满足发表任何意见** —— 该判断属 `expert-ux`。
- **完整性限制**: 1. **未在浏览器中验证任何焦点行为、未测量任何实际目标尺寸** —— 「约 45×18px」是该条自陈的估算，**本席未独立重算，也不主张其准确**。2. `grep "focus"` 只覆盖 `src/*.css` 与 `public/index.html`，**未穷举第三方样式注入**。3. `timeline.js` 属 `code-owner-ui-primitives` 边界，**越界只读**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实跑 + 独立重算）

### E-0333 | repository | 自证类
- **来源定位**: **`E-0179` 的 light 行对比度不可复现，实为 8.99:1，且 8.74:1 的产生方式可唯一定位。**
  - **源取值全部精确**：`memory_v2_trace_audit.js:35-64` 为 `AuditRow` 全块；`:52` `color: isDark ? "rgba(255,255,255,0.76)" : "rgba(0,0,0,0.72)"` **逐字**；`:53-54` `fontFamily: code ? mono : "inherit"` / `fontSize: code ? 11 : 12` · `:55-57` `whiteSpace:"pre-wrap"` / `overflowWrap:"anywhere"` / `userSelect:"text"` **逐字**；`detailsBackground` 取值见 `E-0171`
  - **本席按该条抬头声明的方法重算**（前景合成到 `detailsBackground` 叠 `--pupu-background` 的合成底上，再与该合成底求比）：
    - **dark**：底 = `0.03·255 + 0.97·18 = 25.11`；前景 = `0.76·255 + 0.24·25.11 = 199.83` → **10.48:1**，**与其所载精确相符**
    - **light**：底 = `0.975·255 = 248.63`；前景 = `0.28·248.63 = 69.62` → **8.99:1**，**其所载为 8.74:1**
  - **穷举后 8.74:1 只有一种产生方式**：前景 `rgba(0,0,0,0.72)` 合成到 **纯 `#ffffff`**（= `0.28·255 = 71.4`），却与 `detailsBackground` 的合成底（`248.63`）求比 → **8.74:1**。其余候选均不等于 8.74（合成底与前景同底 = 8.99；两者均取整 = 8.96；前景对纯白且底取纯白 = 9.23；前景对合成底而底取纯白 = 9.50）
  - **净事实**：**同一张表的两行使用了两种不同的合成方式**；dark 行与该条抬头声明的方法一致，light 行不一致。偏差 **2.8%**，方向 **保守（低估）**，其 `满足 SC 1.4.3` 的结论 **不变**
- **取得方式**:
  ```
  sed -n '35,70p' src/COMPONENTs/chat-bubble/memory_v2_trace_audit.js
  # 以 evidence.md:2160 抬头逐字声明的公式自写实现，对六种合成变体逐一求值
  ```
- **提交发言**: S-0077
- **支持/反驳**: **限定** `E-0179` light 行的数值（`8.74 → 8.99`）；**不反驳** 其任何源取值、其 dark 行数值、或其结论。**并为 `expert-ux` 本块 15 个对比度数字提供一次交叉检验的量化结果：14 个逐位精确，1 个可定位成因。**
- **完整性限制**: 1. **为计算值非仪器测量。** 2. **本条不主张该差异有任何实体后果** —— 8.74 与 8.99 均远高于 4.5:1。3. **本席穷举的是六种合成变体，不是全部可能的算法** —— 「只有一种产生方式」的准确措辞是「在本席枚举的六种变体内唯一」。4. **本条不就展开态是否合格发表意见** —— 该判断属 `expert-ux`。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 独立重算）

### E-0334 | repository | 自证类
- **来源定位**: **`E-0180` / `E-0181` / `E-0182` 的复核，含一处 24 行的定位偏差。**
  - **`E-0180`**：`memory_v2_trace_presenter.js:162-196` 为 `resolveTraceStatus` **全文**，本席逐条读出其全部 8 个 `return`，**每一条都是 `"Complete"` / `"Partial"` / `"Legacy"` / `"Unavailable"` 四个字面量之一，无任何一条回吐入参字符串**；末行 `:195` `return mode === "active" || mode === "shadow" ? "Complete" : "Unavailable";` **逐字**；`trace_chain.js:1939-1943` 的 `title: (<span data-testid="memory-v2-trace-title">Memory V2 · {memoryV2Audit.status}</span>)` **逐字**。（顺带独立复现 block 3 的 `E-0301`：`:188-192` 的第三个析取支确为 `normalizedText(raw.reason).toLowerCase().includes("unavailable")`，`reason` 为 `TOP_LEVEL_KEYS` 第 9 项。）
  - **`E-0181`**：`grep -rln "Unavailable" src/locales/` **零命中** · `grep -rn '"Memory V2\|memory_v2' src/locales/en.json` **零命中** · `ls src/locales/*.json | wc -l` = **11**（`de es en fr it ja ko pt-BR ru zh-CN zh-TW`）—— **三项精确**
  - **`E-0182`**：`:154-160` `resolveMode` **精确**（`:159` `return mode || "off";`）；`titleCase` 实为 **`:149-152`**（条目 `定位` 记 `:148-151`，**首尾各偏一行**；其行内注释所标的 `:150-151` **精确**）；**`modeLabel` 实在 `:379`（`modeLabel: titleCase(mode) || "Off",`），条目 `定位` 记 `:355-357` —— 该处实为 `predictedTokens`，偏差 24 行**；`trace_chain.js:1944-1947` 的 span 取 `modeLabel` **逐字精确**。**其所述机制经本席逐环走通，成立。**
- **取得方式**:
  ```
  sed -n '146,196p;353,359p' src/SERVICEs/runtime_events/memory_v2_trace_presenter.js
  grep -n  "modeLabel"        src/SERVICEs/runtime_events/memory_v2_trace_presenter.js   # -> :379
  sed -n '1935,1960p'         src/COMPONENTs/chat-bubble/trace_chain.js
  grep -rln "Unavailable" src/locales/ ; grep -rn '"Memory V2\|memory_v2' src/locales/en.json ; ls src/locales/
  ```
- **提交发言**: S-0077
- **支持/反驳**: 支持 `E-0180` / `E-0181` / `E-0182` 的复核结论；**记录 `E-0182` 的 `modeLabel` 定位偏差 24 行与 `titleCase` 范围偏一行**，**二者均不改变其所述机制，本席已逐环独立走通**。
- **完整性限制**: 1. `E-0181` 的两条为 **字面量负向 `grep`** —— 以变量键或运行时拼接的 i18n key 会被漏掉，故为下界形式的负向主张。2. **本席未运行 `resolveTraceStatus`**，其返回域的封闭性为 **静态读出全部 `return` 语句** 所得，若存在本席未察觉的间接返回路径会漏。3. 相关文件分属 `code-owner-shared-arteries` / `code-owner-chat-bubble` 边界，**越界只读**。
- **验证历史**: S-0077 | 已验证（由 `evidence-examiner` 实跑）
