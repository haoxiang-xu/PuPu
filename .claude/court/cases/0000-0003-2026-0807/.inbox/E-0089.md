### E-0089 | repository
- **来源定位**: `/Users/red/Desktop/GITRepo/PuPu/unchain_runtime/server/` 全目录（排除 `tests/`）
- **取得方式**: 全仓计数检索（dev @ `8d7fbd1d`）：
  ```
  cd unchain_runtime/server && grep -rn 'str(exc)\|str(e)\b' --include="*.py" . | grep -v tests | wc -l
  → 109
  ```
- **提交发言**: S-0013
- **支持/反驳**: **扩展** `code-owner-runtime` 的 C3（错误响应体不得回传 `str(exc)`）—— 证明 `/memory/projection` 是一个类的一个样本，而非孤例
- **完整性限制**: **只是计数，不是审计。** 109 处中有多少真的进入 HTTP 响应体、有多少只进日志或只在内部消费 —— **未逐条核实**。因此本条 **不支持任何关于该类整体 severity 的结论**。

**结论：`str(exc)` 在 sidecar 里是一个 109 处的编码模式。E-0084 只测了其中 2 处。**

这一点直接约束本案能给出的 severity：

- **单点定级可以做**：`/memory/projection` 与 `/memory/long-term/projection` 两条，泄漏内容为异常 message（可能含本机绝对路径），前置条件为渲染进程已可执行 JS —— 可定级（见 S-0013 第五问）。
- **类整体定级不能做**：其余 107 处中，只要有 **一处** 位于异常 message 可能嵌入 **用户内容、模型输出或凭据材料** 的路径上（例如 provider SDK 异常回显请求体片段、以用户文本为键的 `KeyError`），其 severity 与 projection 这两条 **完全不同量级**。

**因此本人明确拒绝从一个样本推断一个类的定级。** 建议单独开一个只读侧案：*「sidecar 错误归一化审计 —— 109 处 `str(exc)` 中哪些进入响应体、其中哪些可能承载用户内容或凭据」*。该侧案与本案（Memory 的用户可见面）**非阻塞**。
