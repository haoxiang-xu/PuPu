### E-0088 | repository
- **来源定位**: `/Users/red/Desktop/GITRepo/PuPu/unchain_runtime/server/route_projection.py:406-414, 456-460`
- **取得方式**: 当前 checkout 只读检查（dev @ `8d7fbd1d`）
- **提交发言**: S-0013
- **支持/反驳**: **新增**（无对应前序发言）—— 依 `Expert` 的主动指出义务提交
- **完整性限制**: 静态读取。未实测未授权请求。仅一条路由，未审计 sidecar 全部路由的鉴权顺序。

**结论：`/memory/projection` 的参数校验发生在鉴权之前。Informational 级，登记备查。**

```python
@api_blueprint.get("/memory/projection")
def memory_projection() -> Response:
    root = _root()
    session_id = request.args.get("session_id", "").strip()
    if not session_id:
        return jsonify({"error": "session_id is required"}), 400   # ← 先于鉴权返回
    if not root._is_authorized():
        return root._json_error("unauthorized", "Unauthorized", 401)
```

对照组：同文件 `/memory/long-term/projection`（`:456-460`）**第一件事就是 `_is_authorized()`**，顺序正确。所以这不是全局约定，是这一条路由的局部偏差。

**影响评估**：未持 token 的本机调用方可据 400 与 401 之别区分「路由存在且参数缺失」与「未授权」。它不泄漏任何数据、不绕过任何检查（带 `session_id` 的未授权请求仍然 401）。在 PuPu 的威胁模型下（本机 sidecar、每会话随机 token、逐路由强制认证 —— 见本人 memory `flask-sidecar-posture`），能发这个请求的攻击者已经在本机上，且能从进程列表得到同等信息。

**定级：Informational。** 非本案范围，非阻塞，建议顺手把两行对调。
