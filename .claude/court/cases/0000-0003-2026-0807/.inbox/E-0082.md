### E-0082 | repository
- **来源定位**: `/Users/red/Desktop/GITRepo/PuPu/src/SERVICEs/chat_storage/chat_storage_store.js:1222-1233`；`/Users/red/Desktop/GITRepo/PuPu/src/COMPONENTs/side-menu/hooks/use_chat_tree_store.js:3, 9`；`/Users/red/Desktop/GITRepo/PuPu/src/PAGEs/chat/chat.js:18, 230`；`/Users/red/Desktop/GITRepo/PuPu/src/SERVICEs/bridges/memory_vault_bridge.js:51, 53-61`；`/Users/red/Desktop/GITRepo/PuPu/electron/main/services/memory_vault/service.js:2087-2105`
- **取得方式**: 当前 checkout 只读检查（dev @ `8d7fbd1d`）
- **提交发言**: S-0013
- **支持/反驳**: **部分反驳** S-0008 / E-0034 第（四）点 —— 「新增 scope 枚举直接推翻『渲染进程不可全局枚举 vault』这条 P0 不变量」这一表述高估了该不变量当前提供的保证
- **完整性限制**: 静态读取。未实际执行扇出枚举（那会产生写以外的真实 IPC 流量，超出只读范围）。本机 vault 无 descriptor 数据，即使执行也无样本。

**结论：该不变量今天是「一次调用不可枚举」，不是「不可枚举」。渲染进程已经手握完整的 scope-id 全集。**

**（一）`chat` 域 scopeId ≡ chatId**（`memory_vault_bridge.js:53-61`）：

```js
if (scopeChoice !== "chat") return null;
const normalizedChatId = ...;
return { scopeKind: "chat", scopeId: normalizedChatId };
```

**（二）渲染进程本地持有全部 chatId，且是同步可读的普通对象**（`chat_storage_store.js:1222-1233`）：

```js
export const bootstrapChatsStore = () => {
  const store = getChatsStore();
  ...
  return { store, activeChat: ..., tree: store.tree };
};
```

`store.chatsById` 是一个以 chatId 为键的全量映射（side-menu 的会话树正是靠它渲染 —— `use_chat_tree_store.js:9`、`chat.js:230`）。因此 `Object.keys(store.chatsById)` 在 renderer 内 **零 IPC、零权限** 即得到全部 `chat` 域 scopeId。

**（三）`user` 域 scopeId 是编译期常量**（`memory_vault_bridge.js:51`）：`MEMORY_VAULT_USER_SCOPE_ID = "pupu.user"`。

**（四）由（一）(二)(三)：** 渲染进程今天已经能构造出 vault 中 **全部 in-use scope 的完整键集合**，对每个键各发一次 `listDescriptors` 即可重建全量清单。main 侧的 `validateScopeKind` + `validateScopeId` + `WHERE s.scope_kind = ? AND s.scope_id = ?`（`service.js:2093-2105`）**每一次都会放行** —— 因为每一次都在命名一个合法且真实存在的 scope。

**（五）所以该 P0 不变量实际提供的保证是**：

| 表述 | 是否成立 |
|---|---|
| 「渲染进程不能在**一次调用**里拿到全库」 | ✅ 成立 |
| 「渲染进程不能拿到**它不知道 scopeId 的** scope」 | ✅ 成立 |
| 「渲染进程不能枚举 vault」 | ❌ **不成立** —— 它知道所有活着的 scopeId |

**（六）因此新增 scope 枚举能力真正新增的信息，只有一项**：**渲染进程本来不知道的 scopeId**，即 **孤儿 scope**（chat 行已消失但 vault 行还在）。除此之外，它不给渲染进程任何今天拿不到的东西。这把「是否新增枚举能力」这个取舍的量级，从「推翻一条 P0 不变量」下调为「是否让渲染进程看见孤儿 scope-id」。

**（七）反向推论（对 Q4-B 有直接影响）**：S-0008 认为「全部已捕获凭据」清单**做不出来**是不准确的 —— 它今天就做得出来，代价是 O(会话数) 次 IPC 往返，属**性能**问题而非**契约或安全**问题。安全侧对扇出方案 **无反对**。
