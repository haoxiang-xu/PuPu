// Shared size for every toggle inside the agent builder: the theme's default
// switch (64×32) scaled to 80%, rounded to even pixels so the plain switch's
// thumb geometry (offsets derived from height) stays crisp.
export const BUILDER_SWITCH_SIZE = Object.freeze({ width: 52, height: 26 });
