import json,pathlib,urllib.request,hashlib,subprocess
s=json.load(open('/tmp/pupu-third-party-state.json'));w=pathlib.Path(s['work']);out=pathlib.Path(s['out']);j=json.load(urllib.request.urlopen('https://pypi.org/pypi/cisco-ai-skill-scanner/2.1.0/json'));a=next(a for a in j['urls'] if a['filename'].endswith('macosx_13_0_arm64.whl'));b=urllib.request.urlopen(a['url']).read();assert hashlib.sha256(b).hexdigest()==a['digests']['sha256'];p=w/a['filename'];p.write_bytes(b);(out/'skill-scanner-identity.json').write_text(json.dumps({'version':'2.1.0','download':a['url'],'sha256':a['digests']['sha256'],'mode':'local static/behavioral only; no cloud analyzers'},indent=2));subprocess.run(['uv','venv',str(w/'scanner-venv'),'--python','/Users/red/Desktop/GITRepo/unchain/.venv/bin/python'],check=True);subprocess.run(['uv','pip','install','--python',str(w/'scanner-venv/bin/python'),'--only-binary',':all:',str(p)],check=True)
