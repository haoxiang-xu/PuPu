# Ticket 289 unsigned diagnostic candidate — 2026-09-22 local

This is a development candidate built from `/Users/red/Desktop/GITRepo/pupu-289` on base `bd94efe8facb38ed4afa7488a46e68d5e6cbb15a` plus the working-tree #289 catalog diff. It is not a signed release, a full package qualification, a live app run, or a live-model result. No app was launched.

The package reused the preserved #291/#327 wheel and artifact record at `/Users/red/Desktop/GITRepo/skills-delivery-evidence-2026-09-21/unchain-0.2.0-py3-none-any.whl` and `unchain-artifact.json`. Wheel SHA-256 is `6544306f55267482c9dfcd7fa346ed218a83ee8e4ec68a3ed761600f7119f194`; the artifact runtime manifest digest is `2d0587fa3670329bc42c1a936b37b85a8c9248e45896cc79ec31acd74628d12c`. `verify-unchain-artifact.mjs` checked the wheel bytes and importable runtime manifest. The packaged sidecar was extracted from the preserved #291 diagnostic zip, not rebuilt from a mutable sibling checkout; both source and candidate binary SHA-256 are `ffc8aada968f1d012a94f2ff88f82690d9452da548a2f88bd8ac034cb7ca9619`. The enabled feature snapshot was copied byte-for-byte from the same evidence directory; SHA-256 `5ec27ac531f7f382a294b7ccd6e9fb65fa589cf01a2ea04e96a001a84d363a47`. The pinned MCP runtime cache was copied from the separate #329 clone; electron-builder's afterPack hook reported staged and verified darwin-arm64 node, uv and Python runtimes.

`PUPU_BUILD_FEATURE_SNAPSHOT_PATH=.local/build_feature_flags.snapshot.json PUPU_BUILD_VERSION=0.1.11 npm run build:web` compiled successfully. Direct `electron-builder --mac --arm64 --dir --publish never --config.mac.identity=null` produced `/Users/red/Desktop/GITRepo/pupu-289/dist/mac-arm64/PuPu.app`. SHA-256 of `Contents/Resources/app.asar`: `70a6364524e27a735b2509f8853a87835d6eed20d76720021599a47757973906` (439,465,674 bytes). Inside it, `build/static/js/45.498896b6.chunk.js` contains the `skillpack.doc-coauthoring` catalog identity with the new unspecified-license label, upstream-download disclosure and unchanged SKILL.md manifest hash; the matching source chunk SHA-256 is `3b973c2b65b7afdcf179588280a90bb6511c470dd7fd27fd1a3855cf59a69cf5`.

The **single full-app diagnostic candidate** is `/Users/red/Desktop/GITRepo/pupu-289-candidate-evidence-2026-09-22/PuPu-289-macos-arm64.zip`, created from that `.app` with `ditto -c -k --sequesterRsrc --keepParent`. ZIP SHA-256: `6a691789348c604951cffbf64a1024672720594d4a2934f79a7b9d42c4a6dbb6` (463,884,008 bytes). Streaming the ZIP's `app.asar` and packaged `unchain-server` entries produced the same hashes as the built app above (`70a63645…73906` and `ffc8aada…a9619`). Use the ZIP hash to identify the complete candidate; the asar hash identifies only its application resources. Compression did not change the app bundle.

The packaged sidecar protocol smoke used the binary inside this candidate app and the preserved wheel/artifact/snapshot. It passed 5 checks: started, unauthenticated requests rejected, authenticated health, authenticated context status and manifest/snapshot projections. Machine result: [candidate-sidecar-smoke.json](candidate-sidecar-smoke.json). This proves the reused packaged runtime starts and matches the preserved protocol identity; it does not exercise Store UI, installation or model behavior inside the app.

Before the direct builder run, `npm run notices:check` reported 1412 npm packages OK but failed because this diagnostic clone lacks `.venv-unchain-build` and the staged `unchain_runtime/mcp_runtime` directory. The direct `--dir` builder path did not run the notice gate. This candidate therefore does **not** pass the full release build chain; a release candidate must run that gate with its complete build environment. The builder's dependency collection also reported existing `npm invalid` diagnostics for react/react-dom/yaml but exited successfully. The focused renderer suite passed 194 tests / 7 suites and the opt-in pinned lifecycle/activation script passed after the metadata correction. Current-path live model, UI cards, cold app restart and packaged #289 installation remain NOT_RUN.
