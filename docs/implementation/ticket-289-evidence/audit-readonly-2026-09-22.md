# Ticket #289 read-only audit applicability check — 2026-09-22

This is a preparation check under `issue-feature-audit`, **not** a posted feature audit or PASS. The product diff is still only the two intended JSON additions (6 listing lines, 41 curation lines) on PuPu base `bd94efe8facb38ed4afa7488a46e68d5e6cbb15a`. No candidate package digest exists for #289, and current-path live model/UI and packaged-pair acceptance are not run. Overall: **INCOMPLETE**.

Remote readback: [#289](https://github.com/haoxiang-xu/PuPu/issues/289) is OPEN, labelled `new feature`, Size S, PUPU Project Status In Progress. Its direct parent is OPEN [Release #216](https://github.com/haoxiang-xu/PuPu/issues/216), Size Release, Status In Progress. The remote #289 body still describes pre-#291 composer expansion; the local [implementation plan](../ticket-289.md) records the current inventory-backed invocation path. This check did not change the issue, Project or parent.

1. **i18n: PASS for current source.** Full skill audit script found zero missing/orphan/placeholder-mismatch entries across 10 locales and zero `missingInEn`. It also reported 65 possible dead keys and 48 dynamic-key sites, the scanner's stated blind spots. No translations were changed; the feature diff adds only catalog text fields (`titleZh`, `blurbZh`, `descriptionZh`) and no translation keys.
2. **UI: N/A for new UI implementation.** The feature diff adds data consumed by the existing Store card/detail components. No component, button, popover, theme branch or layer was added. The two gradient colors are catalog data in the same shape as existing skill packs; actual rendering still belongs to the unrun current-path app check.
3. **Model × agent builder: N/A for model selection.** No model/provider/effort/recipe or agent-builder implementation changed. The skill runs under the selected model; scripted ModelIO verifies activation wiring only.
4. **Static rules: PASS for the feature diff.** `git diff --name-only` lists only `plugin_store_curation.json` and `plugin_listing_records.json`; `git diff --check` passes. The additions contain no `ipcRenderer`, `localStorage`, foreign router/context, overlay or Electron-test change. JSON parses; four focused Store/trust suites pass (79 tests). Existing tests iterate the real curation and listing records, so the new entry resolves as third-party/unverified/officially curated.
5. **End-to-end: NOT_RUN on the current app path.** The opt-in isolated lifecycle script passes real pinned download/import/store/inventory and scripted ModelIO activation, second turn, cold agent rebuild, and menu removal after uninstall; see [skill-results.json](skill-results.json). Its fixed Unchain wheel SHA-256 is `6544306f55267482c9dfcd7fa346ed218a83ee8e4ec68a3ed761600f7119f194`, runtime manifest digest `2d0587fa3670329bc42c1a936b37b85a8c9248e45896cc79ec31acd74628d12c`. These are development wiring results. A current-path real app/model probe, write/edit confirmation cards, cold app/sidecar restart, and exact #289 packaged PuPu + wheel pair remain unrun. The 2026-09-21 model result used the retired composer-expansion path and cannot satisfy this check.

## Catalog and license review

The added curation has one `skillpack.doc-coauthoring` row with a 40-hex pinned source SHA, one subset/manifest path, the previously verified 64-hex file digest, a review record and one command preview. The listing file has one matching `skillpack`/repository row marked `officially_curated`. The Store's listing gate accepts it; the installer and inventory checks preserve the same ID. At this read-only audit, `source.license` displayed `Apache-2.0`; the later metadata correction below changed that display without changing the pin or manifest.

At [the pinned upstream revision](https://github.com/anthropics/skills/tree/34040c9c568585f6929bedeaad110ad08f079624), `skills/doc-coauthoring/` contains only `SKILL.md`: there is no folder `LICENSE.txt` or repository-root license. The [README](https://github.com/anthropics/skills/blob/34040c9c568585f6929bedeaad110ad08f079624/README.md) says **many** skills are Apache-2.0 and separately identifies `docx/pdf/pptx/xlsx` as source-available exceptions; it does not say every other skill is Apache-2.0. The [marketplace manifest](https://github.com/anthropics/skills/blob/34040c9c568585f6929bedeaad110ad08f079624/.claude-plugin/marketplace.json) groups doc-coauthoring with `example-skills`, but grouping is not a license grant. The latest upstream `main` checked on 2026-09-22 resolves to the same pinned SHA and still has no doc-coauthoring `LICENSE.txt`. PuPu's catalog stores a URL, hash, and metadata; the user downloads the upstream body from GitHub, so PuPu is not bundling that body in this change. The initial `Apache-2.0` label was unverified and the Store detail badge presented it as an unqualified fact. In the subsequently authorized metadata correction, `source.license` became `License not specified for this skill`, `review.licenseBasis` now states the limited README evidence, and both blurbs disclose upstream download and the unspecified license. The pin, subset and manifest were retained.

No audit comment, status transition, commit, push, PR or Project mutation was made.
