# Ticket 289 development evidence

PuPu base for the original run: `c5209de25b29c45061cc22c201d40aa5fec1e4fa` (remote dev). Resumption base: `bd94efe8facb38ed4afa7488a46e68d5e6cbb15a`. Isolated clone `/Users/red/Desktop/GITRepo/pupu-289`, branch `codex/ticket-289-doc-coauthoring-skill`. This is development evidence for a catalog-only change, not feature-audit acceptance or release qualification.

## Resumption checkpoint — 2026-09-22

- The [read-only audit applicability check](audit-readonly-2026-09-22.md) records the five checks, current remote issue/Release status, and the remaining upstream license ambiguity. It is **INCOMPLETE**, with no audit comment or Project mutation.
- The catalog metadata was corrected to display `License not specified for this skill`; both locales now disclose that PuPu downloads the upstream body at install time and does not bundle it. The fixed upstream SHA, subset and file digest are unchanged. The opt-in verifier now asserts this metadata alongside the pinned identity. See the [candidate package evidence](candidate-package-2026-09-22.md) for the subsequent unsigned diagnostic build and packaged sidecar smoke.
- The refreshed opt-in `verify_skill.cjs` passes the current importer/store schemas with exact key-set checks. Its real pinned GitHub download still yields `SKILL.md` sha256 `2e47d78846faeea4a56e9809c52700087a15a2155a3f293a3efbaded81398ef4` (15,815 bytes); the imported body hash remains `cdc0afd5dccbe0731d74ef86ff60c3e79b9b477a37ebc7be375d4cec8aa18046` (15,341 bytes). Import warnings, skips, degradation and rejection are empty. Duplicate install is 409, a fresh Python process reads the normalized row, uninstall/reinstall succeeds, and a tampered manifest fails integrity without partial output.
- The isolated inventory read uses the preserved, hash-checked #291/#327 Unchain wheel from `/Users/red/Desktop/GITRepo/skills-delivery-evidence-2026-09-21/`, never the mutable sibling source. Wheel sha256 `6544306f55267482c9dfcd7fa346ed218a83ee8e4ec68a3ed761600f7119f194`; source revision `fd7f7395ba39c5ce694191e5c160734f477e0957`; runtime manifest digest `2d0587fa3670329bc42c1a936b37b85a8c9248e45896cc79ec31acd74628d12c`. With workspace and user skill directories disabled, the inventory has one entry: `id=skillpack:skillpack.doc-coauthoring:doc-coauthoring`, `source=skillpack`, `source_id=skillpack.doc-coauthoring`, both invocation flags true, no aliases, no diagnostics. Its revision and the exact artifact paths are recorded in [skill-results.json](skill-results.json). This is development evidence; the later candidate's packaged sidecar protocol smoke passed, but #289 Store install/UI qualification in that package is still unrun.
- **Current activation wiring PASS (scripted ModelIO only):** [verify_activation.py](verify_activation.py) reads the real imported row from the isolated store and builds PuPu's real developer agent against that wheel. `/doc-coauthoring` activates the exact 15,341-byte body once, with source identity `skillpack:skillpack.doc-coauthoring:doc-coauthoring`; the provider request keeps the original user text verbatim. A second ordinary turn retains that body and activation identity. A fresh Python process rebuilds the agent from the same isolated store and transcript, then preserves the same identity on its next turn. The real renderer `syncSkillInventory` projection registers `/doc-coauthoring` with an empty `expandsTo`, and uninstall removes both inventory entry and menu command. No model provider, credential, personal profile, workspace write, or UI interaction is involved. This proves wiring, not LLM document quality or app acceptance.
- Focused regressions on the resumption base: renderer 194 passed / 7 suites; Electron downloader 36 passed / 1 suite; Python skill pack, inventory route and agent mounting 27 passed plus 11 subtests. The Python tests load the same preserved wheel via `PYTHONPATH`; their source path is inside that wheel. No product code was edited.
- The 2026-09-21 live model run below predates #291/#327. It established the former composer-expansion behavior only. The scripted proof above confirms the current backend/renderer wiring; **current-path live model/UI evidence is NOT_RUN**. A fresh app run must confirm Store discover/detail/install, activation through the real composer, write/edit confirmations, subsequent turn, cold app/sidecar restart, and uninstall in the UI. The parent agent owns that live run and final delivery assessment.

## Historical result — 2026-09-20/21

- One instruction-only skill pack added: `skillpack.doc-coauthoring` — `anthropics/skills` @ `34040c9c568585f6929bedeaad110ad08f079624`, `skills/doc-coauthoring/SKILL.md`, sha256 `2e47d78846faeea4a56e9809c52700087a15a2155a3f293a3efbaded81398ef4` (15,815 bytes). Listing provenance record added in `plugin_listing_records.json`. No code, schema, IPC, runtime or UI change.
- **Regression tests: 203 passed** — frontend 150 (7 suites: plugin_trust, skill_pack_store_install, plugins_categories_page, skill_pack_detail_page, skill_pack_import, plugins_discover_page, mcp_toolkit_store), Electron `skill_repo_download` 36, Python `test_skill_packs.py` 17 (+11 subtests).
- **AC-289-002 / SEQ-289-001 PASS** (`verify_skill.cjs`, real Electron downloader + fresh Python store processes on an isolated temp store): real pinned archive download, downloaded bytes hash equals the manifest, imported body (15,341 bytes, sha256 `cdc0afd5…`) equals the upstream file minus its frontmatter, `rejected/skipped/degraded = []`, duplicate install → 409, fresh-process read identical, uninstall then reinstall PASS, tampered manifest hash → `integrity` with no partial output. See [skill-results.json](skill-results.json).
- **AC-289-001 PASS (live dev app)**: the row appears in Store → Skills; the detail page shows `Doc Co-Authoring`, `Third-party · Officially curated`, `Unverified`, `anthropics/skills @ 34040c9`, `SHA-256 check on download`, `Reviewed 2026-09-20 · Apache-2.0`, one command `/doc-coauthoring`. GET on the detail page ran the real install chain; the pack was persisted to `skill_packs.json`, the Installed count went 13 → 14, and the v2 catalog listed the pack with one composer skill (body 15,341 bytes).
- **AC-289-004 (live dev app, `openai:gpt-4.1`, core toolkit, default workspace)** — see [live-model-run.json](live-model-run.json):
  - Restart persistence: the dev app (Electron + sidecar) was cold-restarted between install and the chat run; after restart the pack was still installed, the `/doc-coauthoring` command was registered and listed in the composer command menu (screenshot retained locally).
  - Turn 1 (sent through the test API): the model created the scaffold via `write` after one `ask_user_question` for an absolute path and one `shell mkdir` (both confirmation cards approved); file `doc-coauthoring-289/decision-doc.md` was created in the workspace. **Limitation of the harness, not the product:** `POST /chats/:id/messages` does not run composer command expansion — the persisted user message was the raw 107/552-byte text without the template and without a composer sidecar (re-verified in a second chat after the registry was synced). Turn 1 therefore proves the tool path, not the skill.
  - Turn 2 (typed into the real composer + Enter): the persisted user message is 15,782 bytes with the full upstream template and a composer sidecar; the model followed the skill's Stage 2 (brainstormed 5 options, kept 1–3) and applied the revision with a single `edit` call (confirmation card approved, `status: success`); it then produced Stage 3 manual reader-test instructions. Those instructions say **"Open a fresh Claude conversation: https://claude.ai"**, exactly as the upstream text does — the Store description tells the PuPu user to use a new PuPu chat instead; the skill text is byte-preserved and was not rewritten.
  - Uninstall: `deleteSkillPack` through the renderer bridge removed the pack from the store file and the v2 catalog; the composer menu no longer lists `/doc-coauthoring`.
  - Cleanup: both probe chats deleted, the synthetic `doc-coauthoring-289/` folder removed from the workspace, the dev app stopped. An accidental Ponytail install (first GET button in DOM order) was reverted immediately with `deleteSkillPack`; the user's store was left with the same five packs it had before the run.

## Observed limitations

- The skill's connector guidance (Slack/Teams/Drive) and `create_file`/`str_replace` names are not PuPu tools; in the live run the model used `write`/`edit`/`read`/`glob`/`shell` correctly on the first workflow turn, but first tried relative paths (`path must be an absolute path`) before recovering. This is model behaviour on an unchanged upstream text, recorded as a limitation.
- The reader-test step names claude.ai. PuPu's supported alternative (new PuPu chat) is stated in the Store blurb and command preview; users following the model's wording verbatim would be sent to claude.ai.
- No sub-agent path was exercised (normal chat has no registered subagent templates).
- Packaged PuPu + fixed Unchain wheel acceptance, second interaction in one attempt, retry/resume and graph/subagent paths: `NOT_RUN` / `N/A` per the plan's state matrix; belongs to the feature audit on `close`.
- License basis: the historical `Apache-2.0` display inferred too much from the repository's “many skills” README statement and `example-skills` membership. The corrected catalog states that this specific skill's license is not specified upstream. The source pin and downloaded body are unchanged.

## Reproduce

```sh
CI=true npm test -- --watchAll=false --runInBand --runTestsByPath src/SERVICEs/plugin_trust.test.js src/COMPONENTs/toolkit/utils/skill_pack_store_install.test.js src/COMPONENTs/toolkit/pages/plugins_categories_page.test.js src/COMPONENTs/toolkit/pages/skill_pack_detail_page.test.js src/SERVICEs/skill_pack_import.test.js src/SERVICEs/plugin_skill_sync.test.js src/SERVICEs/skill_inventory_store.test.js
npm run test:electron -- --runTestsByPath electron/tests/main/skill_repo_download.test.cjs
PYTHONPATH="$PINNED_UNCHAIN_WHEEL" "$PUPU_TEST_PYTHON" -m pytest -q unchain_runtime/server/tests/test_skill_packs.py unchain_runtime/server/tests/test_route_skills_inventory.py unchain_runtime/server/tests/test_adapter_skills_module.py
PUPU_TEST_PYTHON="$PUPU_TEST_PYTHON" PUPU_TEST_UNCHAIN_WHEEL="$PINNED_UNCHAIN_WHEEL" PUPU_TEST_UNCHAIN_ARTIFACT_EVIDENCE="$PINNED_UNCHAIN_ARTIFACT_EVIDENCE" node docs/implementation/ticket-289-evidence/verify_skill.cjs
```

The verify script performs a live pinned GitHub download, validates the supplied wheel against its artifact evidence, and reads an isolated store/inventory. For the resumption run, `PUPU_TEST_PYTHON=/Users/red/Desktop/GITRepo/PuPu/.venv/bin/python`, and the two pinned artifact variables name the wheel and `unchain-artifact.json` in `/Users/red/Desktop/GITRepo/skills-delivery-evidence-2026-09-21/`. The historical live model run used `npm start`-equivalent dev processes from this clone (`node scripts/start-dev.cjs`), the test API for state/screenshots/eval, the real composer for the skill turn, and confirmation cards clicked through `/debug/eval`. No commit, push, PR or feature audit is included in this start phase.

## Completed current-path behavior — 2026-09-23 UTC

The current importer ran through the real Electron download/scan/install IPC into an isolated profile. `openai:gpt-4.1` received the original slash-command user text and the exact imported body once on every one of 12 recorded provider requests. It wrote a synthetic memo via PuPu `write`, requested feedback, read and revised through `edit` with visible confirmation controls, and explained reader testing in a fresh PuPu chat. The first edit attempt was correctly blocked for not reading the file; the model then read and retried. A fresh reader correctly caught a $400/$500 contradiction left by the model; feeding that finding back resulted in a verified file with $400 throughout and Ada Chen responsible for expenses. This demonstrates the review workflow, not guaranteed model accuracy. No real user document was read or changed.

Evidence: `current-live-chat.json`, `current-live-wire-summary.json` (digests/counts only, no copied upstream body), `current-reader-check.json`, `live-draft.md`, `live-final-memo.md`. The isolated app was cold-restarted with the exact diagnostic candidate sidecar binary (shared byte-for-byte with #329); the installed command survived with the accepted runtime manifest, then uninstall returned an empty inventory (`current-cold-inventory.json`). Real behavior used the dev-only Test API and the immutable wheel; candidate ZIP/catalog/sidecar qualification is separately recorded in `candidate-package-2026-09-22.md`. No production rollout, signing, full third-party security certification or cross-platform qualification is claimed.
