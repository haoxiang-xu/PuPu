# Ticket 283 implementation plan

Ticket: https://github.com/haoxiang-xu/PuPu/issues/283
Release: #203 (v0.1.11)
Workspace: /Users/red/Desktop/GITRepo/pupu-283
Branch: codex/ticket-283-marketplace-integrations
Base: remote dev at 65811ee3d7bb62cc86f27c681764d7de0bfeef48 (dev advanced while cloning; the initial start comment recorded the earlier observed tip).

## Goal and decisions

Add four officially maintained MCP recipes and two compatible curated skill packs. Context7 remains #175. Reuse current Categories, skill-pack detail, setup and review-status affordances; no component, layout, primitive, IPC, runtime protocol or persistence format change. UI design exploration is not applicable to this catalog-only slice.

MCP recipes enter `trustLevel=needs_review`, `status=needs_review`, `installable=false`, and `policySummary.reviewed=false`, per docs/contributing/mcp-store-submission.md and the actual validator. Source identity and successful probes do not imply maintainer security approval. No default tool selection or credentials. Keep dependencyCutoff=2026-07-28T00:00:00Z unchanged.

- dev.microsoft-learn: HTTP/streamable_http, https://learn.microsoft.com/api/mcp, no secrets; microsoft_docs_search, microsoft_code_sample_search, microsoft_docs_fetch. Hosted service, source docs https://github.com/MicrosoftDocs/mcp; license `hosted` (documentation repository license does not license the remote service). Official initialize succeeded 2026-09-12.
- productivity.brave-search: stdio npx -y --before=2026-07-28T00:00:00Z @brave/brave-search-mcp-server@2.1.0 --transport stdio --enabled-tools brave_web_search brave_news_search brave_image_search brave_video_search. Required secret BRAVE_API_KEY. Only the four named read/search tools exposed; no server listening port.
- productivity.tavily: stdio npx -y --before=2026-07-28T00:00:00Z tavily-mcp@0.2.21. Required TAVILY_API_KEY for a predictable complete tool set, despite upstream keyless mode. Tools tavily_search, tavily_extract, tavily_crawl, tavily_map, tavily_research. Research/crawl can incur provider credits; require confirmation conservatively for all tools in this new recipe. Do not use the nonexistent @tavily/mcp package.
- productivity.firecrawl: stdio npx -y --before=2026-07-28T00:00:00Z firecrawl-mcp@3.22.4. Required FIRECRAWL_API_KEY. Conservative confirmation on every listed tool, including scrape because it can accept interactive actions. Tool preview is not an execution allowlist; document the server's broader capabilities, external processing and credit usage. Start with verified firecrawl_scrape, firecrawl_search, firecrawl_map previews; confirmation for all discovered tools is a required promotion check; catalog preview metadata alone does not prove runtime enforcement. Bundled runtime compatibility must be checked against Node >=22; do not ask users to install Node.

For all MCP rows: official source and documentation URLs, non-sensitive setupPreview, account/cost and data-access explanations, policySummary counts equal the preview list. These three npm artifacts were downloaded and their SHA-512 integrity checked before inspection; local dependency installation with the existing cutoff succeeded.

Skills use the existing pinned GitHub tarball -> SHA-256 manifest -> scan -> instruction-only import -> install -> catalog/command path. Review only selected instruction content, not entire repositories. Select one self-contained SKILL.md per source:
- skillpack.vercel-web-review: vercel-labs/agent-skills @ 063bee94c3f4df8453406c830b0a7df0f2860278, skills/web-design-guidelines/SKILL.md. MIT stated in that revision's README. Covers interface quality, accessibility and performance. Requires file read and web fetch; the skill explicitly fetches current official Web Interface Guidelines on each use, so the pinned skill hash does not pin those fetched guidelines. Do not claim to bundle the full React best-practices guide (its 108261-byte AGENTS.md exceeds the importer body cap and its rules are separate files).
- skillpack.trailofbits-audit-prep: trailofbits/skills @ 321ccfe628eca0d314b0ee4eaffcdd8a05639aaf, plugins/building-secure-contracts/skills/audit-prep-assistant/SKILL.md. CC-BY-SA-4.0 from the upstream LICENSE. Unmodified instruction content; credit publisher/source and license. Requires workspace/core tools; language-specific analyzers are optional external prerequisites and are not bundled or installed by this pack. No assertion that running this skill certifies security. Other scripts, agents and reference-dependent skills are excluded.

## Boundaries and sequences

BC-283-001: JSON registry producer -> frontend normalizeEntry and Python normalize_registry_payload via shared file. OPEN metadata extensions, existing canonical entry/toolkit identity and stdio/http shapes; optional metadata is ignored by consumers that do not support it. Invalid recipe/schema fails validation; unreviewed recipes fail installation admission on both sides. Identity is entry id + toolkitId + exact package/version/cutoff or HTTPS endpoint. AC-283-001: real new JSON passes JS schema/invariants and Python normalization, IDs/categories unique, secret values absent, policy counts exact. AC-283-002: entryInstallState and actual Python install admission reject these unreviewed rows, and existing entries retain behavior.

BC-283-002: official GitHub immutable tarball -> Electron createSkillRepoDownloader (CLOSED manifest-hit markdown bytes) -> scanner -> buildSkillPackFromScan -> backend skill-pack persistence. Exact repo+commit+path+SHA256, only selected .md, existing size/path/script rejection, no executable installation. AC-283-003: real pinned archives download and hash-check, scan imports exactly the advertised commands with no rejected/degraded skills; an altered hash fails with integrity and no partial output. AC-283-004: actual backend accepts the resulting descriptor, duplicate install fails without mutation, a fresh store read preserves identity/body, uninstall/reinstall preserves correct catalog behavior. SEQ-283-001: absent -> download/scan -> installed -> duplicate rejected -> fresh-process read -> uninstall -> reinstall. Existing schema unchanged; no chat journal migration, retry/resume/provider projection change. First/second chat interactions and normal/graph/subagent runtime qualification are NOT_RUN until packaged integration qualification, not inferred from catalog tests.

BC-283-003: actual package process/remote Microsoft server -> MCP initialize/tools-list/tool-call consumer. VERSIONED MCP negotiation, exact names/schema and failures preserved. AC-283-005: record initialize/list and public read probes against actual source artifacts; exercise missing/invalid credential errors without exposing credentials. No paid search success is inferred from tools/list. External endpoints/version/auth differences are recorded. Full deployed PuPu + one fixed Unchain wheel matrix is NOT_RUN during this start slice; active rollout remains INCOMPLETE. Do not enable MCP rows or claim a packaged-release PASS.

## Graph evidence and verification

Clone indexed at this base using GitNexus 1.6.11. Query located handleInstallPack and backend catalog paths. listStoreSkillPacks: LOW, one direct packItems caller. _normalize_mcp: LOW, five upstream nodes through _normalize_entry, normalize_registry_payload, _load_registry, external registry and validation. createSkillRepoDownloader: LOW, five upstream nodes through runtime service/main. buildSkillPackFromScan: HIGH, five upstream nodes involving both marketplace install paths and local folder import; no edits to this function. MCP_STORE_ENTRIES: UNKNOWN, corroborated by direct imports and storeEntries initialization/cache reset/list access plus Python registry_path/read; zero edges is not low risk. Index has default large-file and flow limits, so graph evidence is supplemented by current source and targeted boundary tests; no complete-repository safety claim.

Run validate:mcp and registry validator tests; frontend mcp_toolkit_store/mcp_install, skill_pack_import, skill_pack_store_install and plugin category/detail tests via react-scripts; Electron downloader tests via repository Electron test command. Run targeted Python registry/skill store tests with Python, never Jest. Add only meaningful boundary checks if current suites lack coverage. Record actual download/import/store and provider probes, exact identities, limitations and results in ticket-283-evidence.

## Delegation assessment and checkpoints

Partially suitable for a weaker implementation model. A gpt-5.6-luna worker may only append the four fully specified MCP JSON entries to src/SERVICEs/mcp_toolkit_registry.json, run required graph impact and validate:mcp, then stop. This is a bounded data change with closed identifiers, pinned versions, chosen transport/auth and observable schema/admission checks. It may not choose new authentication, change cutoff/schema/runtime/UI, promote trust or touch other files. Strong parent retains skill selection/provenance, live probes, contract tests and final integration. Checkpoint 1: review worker diff and recipe semantics before integrating evidence; checkpoint 2: independent validation/contract regression review. No commit/push/PR/audit until user requests close.

## Implementation results (2026-09-12)

The catalog-only implementation is ready for review. Four MCP rows and two skill packs were appended; existing entries and the dependency cutoff are unchanged. Evidence and reproducible opt-in scripts are in [ticket-283-evidence/README.md](ticket-283-evidence/README.md).

Checkpoint 1 corrected the delegated Microsoft documentation URL and the inaccurate pending-auth wording for its keyless service. It also removed unproven claims that catalog confirmation metadata enforces all discovered tools. Checkpoint 2 passed the real frontend/Python admission boundary, schema and regression suites. There are 182 passing regression tests (114 frontend, 36 Electron, 22 Python, 10 validator), plus successful actual skill lifecycle and negative hash checks. All four MCPs initialize and list tools; Microsoft real search and Tavily's upstream keyless search succeed. Brave invalid authentication and Firecrawl keyless rejection are recorded, not counted as authenticated success.

Activation remains incomplete. npm advisory matching returned 1 high + 2 moderate dependency findings each for Brave and Tavily, and 2 high + 2 moderate for Firecrawl. These are untriaged dependency matches, not proven exploitability in PuPu; remediation or an applicability review is required before promotion. No cutoff change or advisory waiver was applied. Catalog rows disclose the unresolved dependency review and remain blocked by both actual installation consumers.

A source check of companion Unchain b98e532f244170e33c9c63f47f5c9ed9ade69567 shows MCP tool conversion initializes confirmation false and derives it from destructiveHint, while PuPu discovers tools through _tool_to_dict. Thus a JSON preview flag is not sufficient proof of execution confirmation, especially for Firecrawl's 26 discovered tools. Full confirmation enforcement must be qualified with the exact packaged pair before promotion; this catalog-only ticket does not modify or certify that companion runtime. Authenticated provider workflows, model invocation, packaged pair matrix and feature audit remain NOT_RUN. No Python product changes were made and no running sidecar was restarted.
