# Ponytail Store integration

Completed: AC-PT-001/002 and SEQ-PT-001 passed, including the exact packaged pair, original icon display/cold persistence and real GPT-4.1 `/ponytail` response. See [evidence](ponytail-store-evidence/README.md). 116 targeted tests and five packaged smoke checks passed; full i18n scan is clean. Final graph change analysis reports CRITICAL shared-flow coverage, addressed with scoped tests and packaged observations rather than treating zero/unresolved edges as safe. No commit/push or release rollout was performed.

User request: add DietrichGebert/ponytail to PuPu Store with its upstream icon. Branch: codex/add-ponytail-store, from dev after #287. No commit/push requested for this addition.

Pin upstream 4.9.0 at `356918eba965ee1eac64bd3a7f0dd02108350de5`. Include the unmodified core, review, audit and debt SKILL.md files. Exclude lifecycle hooks, scripts and MCP: PuPu installs this as manually invoked instructions. Exclude gain (superseded benchmark claims) and help (other-host hook/config/update instructions). Describe this subset in both languages. Content inspection found no secret-exfiltration instructions or hidden executable dependencies in these four files; this is a scoped curation check, not a Verified security attestation. Core instructions explicitly preserve requested work, safety and accessibility. Review/audit cover complexity, not security correctness.

Use unmodified `assets/logo-dark.png` (1085 × 1241, 132499 bytes), which has an outline suitable for light/dark surfaces. Bundle its base64 bytes in the entry's existing-format file icon; retain source URL, SHA-256 and full MIT license in curation metadata so attribution is shipped. No runtime remote image request. Store list/detail use ToolkitIcon; installation passes the optional icon to persistence.

BC-PT-001: immutable upstream archive -> actual downloader/hash manifest -> scanner/importer -> Python skill store. OPEN pack metadata; existing skill schema and closed exact repo/commit/file hash admission unchanged. AC-PT-001: four actual commands import without rejected/degraded rows; changed hash fails closed; repeated install conflicts, cold read retains commands, uninstall/reinstall works. No shell hooks are installed or implied.

BC-PT-002: frontend curated `icon` -> install descriptor `toolkitIcon` -> persisted `toolkit_icon` -> catalog and ToolkitIcon. Optional field, existing store version 1; missing/null uses the previous default. CLOSED icon keys: type, mimeType, content; only type=file and image/png, strict base64, PNG header, positive dimensions no larger than 2048, at most 256 KiB decoded. Invalid icon fails before writing; no external URL/SVG/scripts accepted by this addition. Exact pack identity remains the curation id. AC-PT-002: real producer bytes survive fresh-process persistence and icon rendering; invalid type/extra keys/base64/header/size/dimensions fail without a partial install; legacy packs keep the default. SEQ-PT-001: absent -> install -> duplicate rejected -> fresh-process read -> uninstall -> reinstall, same id and image bytes. Existing schema/version and Unchain protocol are unchanged. Repeat/restart are applicable; durable chat interaction/replay is outside icon installation and unchanged.

Graph: PuPu root index refreshed. installStoreSkillPack HIGH (Store row and detail install callers); warned before edit. listStoreSkillPacks LOW. SkillPackDetailPage, PluginsCategoriesPage and install_skill_pack UNKNOWN; source confirms React route/test consumers and Python route registration, so zero edges is not an all-clear. Existing shared icon renderer is reused without modification. Test focused UI/orchestration plus real immutable download/scanner/store and strict negative cases. Exact packaged pair qualification remains NOT_RUN until recorded; no active rollout/release certification is implied.
