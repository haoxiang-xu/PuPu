# Ponytail integration evidence

Ponytail 4.9.0 is available through PuPu's existing Skill Pack Store path, pinned to `DietrichGebert/ponytail@356918eba965ee1eac64bd3a7f0dd02108350de5`. The four unmodified selected commands are `/ponytail`, `/ponytail-review`, `/ponytail-audit` and `/ponytail-debt`. Hooks, scripts, MCP, the outdated gain scoreboard and host-specific help are excluded. The listing description states the instruction-only subset. Origin remains third-party and verification remains Unverified.

The original upstream `assets/logo-dark.png` is embedded locally, with its original bytes, source URL, SHA-256 and full MIT license in the shipped curation metadata. `ponytail-icon.png` and `LICENSE.ponytail.txt` are matching copies for review. The application uses its existing ToolkitIcon renderer in the Store, detail and installed views; icon bytes survive installation and cold restart. Existing iconless packs keep the command fallback. The new optional persistence field admits only bounded embedded PNG descriptors, with closed keys and base64/header/dimension checks.

Validation: 99 frontend tests and 17 backend tests passed. The live `verify_skills.cjs` uses the actual immutable archive downloader, scanner, importer and fresh Python store processes. It verifies all four command identities, no rejected/degraded skills, preserved icon bytes, duplicate rejection, uninstall/reinstall, one altered file hash failing closed, and an invalid icon leaving no partial install. The script's own `liveModelInvocation: NOT_RUN` accurately describes that script; the separate packaged probe below supplies model evidence.

An unsigned macOS arm64 app was freshly built from the working-tree source. All five package smoke checks passed using the single retained Unchain wheel. Actual UI observations: original PNG decoded at 1085 × 1241 in the Store and installed list; four commands and identical icon SHA-256 survived a cold app/sidecar restart. `/ponytail` expanded into 101 lines of upstream instructions and completed a synthetic addition-function request through real `openai:gpt-4.1`, with a persisted assistant `done` response containing `PONYTAIL_OK`. No user workspace files or tools were used by this synthetic request.

Exact package digest: `sha256:fdbd68ed188297c14d6f4450603684b2ffeb458c8c78113b42c46386bacca39d`. Wheel: `sha256:f2e6ddeb85363f1ae54583c7c7ea9d9c6effb1f5c2cba5d7a39c0607d0bec9b7`. Actual imported manifest: `sha256:ab00567fe76a80e8661415eaea0ab57bba1d1c76ad19158153c51f1e64f2c6fc`. `candidate-tree.json` records every packaged file/symlink and the hashing method; `source.patch`, `results.json`, `package-smoke.json`, `cold-catalog.json` and the imported-runtime status bind the evidence. All changed tracked source files were byte-compared to the packaged clone before reporting.

Graph analysis marked the shared installer HIGH before edits, with Store/detail callers; unresolved React/Python callback edges were corroborated by source. Final diff analysis reports CRITICAL because the shared page/installer touches 24 indexed flows; it also attributes some new insertion ranges to older symbols. This is a broad graph signal, not a claim that those functions were all changed. Focused regressions and the actual packaged install/cold-read/model path cover the changed behavior. Full i18n scan found no missing/orphan/placeholder-mismatched keys or missing English source keys.

Screenshots: `store.png`, `detail.png`, `installed.png`, `model-success.png`. This is integration evidence, not comprehensive upstream security verification, advice-quality certification, actual code-editing safety qualification, signing or a release rollout. The isolated app and complete test profile, including its temporary encrypted provider credential copy, were removed; the user's existing app and data were untouched. Changes remain uncommitted on `codex/add-ponytail-store`.
