# Theme color customization: remove the feature gate

Owner request (2026-09-11): remove the theme-color feature flag and expose the existing customization unconditionally. Preserve saved presets, custom colors, details and all unrelated flags. No backend runtime change, release dispatch, deployment or commit is part of this change.

- BC-001: persisted appearance settings -> boot palette / ConfigContainer / AppearanceSettings. Existing appearance schema and settings helpers remain unchanged. Always resolve saved theme settings; existing validators retain their defaults for absent/invalid settings. Feature-flag reads/writes remain CLOSED to registered definitions; the retired key is ignored in old storage/build defaults and dropped on the next normal flag write. Do not erase appearance data or add a storage migration. AC-001: existing tests exercise default visibility, saved colors, CSS variables, legacy theme fields, boot palette, mode changes and remaining flag isolation without opt-in.
- BC-002: checked-in release profile -> write-build-feature-snapshot -> build-web -> renderer. All profile now uses the existing VERSIONED v1 schema with the exact feature key `enable_memory_v2`; its Memory V2 environment values stay identical. Remove the old theme-bearing v2 profile and its admission, update all three workflows together. Unsupported schemas, missing/extra feature keys and noncanonical environments fail before snapshot generation. AC-002: execute the real snapshot producer and build consumer, compare exact keys/values and retain negative admission tests. Frozen historical release artifacts are not rewritten; old v2 input to the new producer is rejected.
- SEQ-001 (BC-001, AC-001): saved custom palette -> startup -> mode switch -> persistence/reload retains configured colors without a flag. No settings -> built-in default palette. An unrelated flag change cannot reset the active palette because theme consumers no longer subscribe to flags.
- SEQ-002 (BC-002, AC-002): new All profile -> snapshot -> build reader; exact feature snapshot contains only Memory V2 plus its reserved environment envelope. Shadow remains on the same v1 contract. Workflow source must reference the matching new profile. Old producer/profile combinations remain in immutable historical artifacts; mixing new producer and v2 profile fails closed.

Impact: GitNexus LOW for theme application/boot helpers and AppearanceSettings; UNKNOWN for ConfigContainer and property/config constants. Text inspection confirms App -> ConfigContainer, settings modal -> AppearanceSettings, and the three release workflows -> snapshot producer. Scope is this PuPu checkout. Python/provider/durable-interaction protocols and Unchain artifact pairing are N/A to this renderer/build-profile change; no rollout qualification is claimed.

Verification: before the code change, updated acceptance tests reproduced 11 failures / 17 passes. After removal, 28 appearance/container/feature-flag tests and 118 theme editor/storage/preview/palette tests pass (146 total). The build snapshot suite passes 6 tests; its Windows-only identity test is skipped on macOS. The real new All profile -> snapshot producer -> build-web `--print-flags` path returns exactly `{"enable_memory_v2":true}` and retains the existing All environment. No retired gate or obsolete All-profile path remains in src/scripts/contracts/workflows. Whitespace checks pass. Refreshed GitNexus index/change report is supporting evidence only: the index reports capped/omitted flows and the aggregate report includes earlier uncommitted work. No production build, release workflow or deployment was run. Logs: `/tmp/theme-flag-red.log`, `/tmp/theme-flag-green.log`, `/tmp/theme-flag-editor.log`, `/tmp/theme-flag-build.log`, `/tmp/theme-flag-graph.log`.
