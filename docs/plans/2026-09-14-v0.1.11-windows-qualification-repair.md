# v0.1.11 Windows qualification repair

Date: 2026-09-14

## Incident scope

Qualification run `34918980959` verified the sealed candidate, macOS arm64/x64,
and Linux x64, but failed both Windows paths. The restart-update fixture passed
`UNCHAIN_SOURCE_PATH` while the package wrapper consumes
`UNCHAIN_ARTIFACT_SOURCE_PATH`; the resulting fallback directory did not exist
and surfaced as `spawnSync git ENOENT`. The fresh-install path separately lost
the NSIS process exit identity and reported only `unknown failure`.

Qualification run `34923227917` proved that the workflow was still executing
from the immutable `v0.1.11` tag rather than the later `dev` repair. Its complete
log also exposed two downstream fixture-build contract gaps that the old
PowerShell behavior had hidden: no explicit N-1 build version and loss of the
top-level Electron `main` entry when projecting `package.json` into a standalone
electron-builder configuration.

## BC-QUAL-001 — workflow to immutable Unchain artifact builder

- Producer: `_shared-release-windows-restart-update.yml`; consumer:
  `run-with-unchain-artifact.mjs`; boundary: GitHub Actions environment into the
  Windows Node child process.
- Producer shape and canonical representation: absolute checked-out source path
  in `UNCHAIN_ARTIFACT_SOURCE_PATH` plus the exact 40-character revision in
  `UNCHAIN_ARTIFACT_SOURCE_REF`.
- Consumer shape: the same two exact environment keys. Admission is `CLOSED`;
  aliases and mutable refs are rejected rather than projected.
- Failure semantics: a missing directory, wrong revision, or non-zero native
  command stops the fixture build immediately. There is no degraded fixture.
- Identity: the values bind the N-1 published manifest revision to the checked
  out `fixture-unchain` source and the one wheel built for that fixture.
- AC-QUAL-001: the workflow contract contains both canonical keys and the exact
  revision producer.
- AC-QUAL-002: the old `UNCHAIN_SOURCE_PATH` alias is absent from the fixture
  build environment.
- AC-QUAL-003: PowerShell stops on the first failed native build command.
- AC-QUAL-007: every N-1 build command receives the exact `from_version` through
  `PUPU_BUILD_VERSION`.

## BC-QUAL-002 — NSIS process to installed qualification

- Producer: the signed NSIS installer process; consumer:
  `installed-package-qualification.mjs`; boundary: Windows child-process result.
- Producer shape: `status`, `signal`, `error.code`, stdout, and stderr from
  `spawnSync`; canonical representation is a bounded per-attempt diagnostic.
- Consumer shape: success only for status `0` with no spawn error. Admission is
  `CLOSED`; missing output never becomes success and all failures retain process
  identity fields.
- Projection: stdout/stderr become bounded diagnostic text. Unknown process
  fields are ignored; exhausted retries fail closed.
- Identity: installer path and install root remain fixed for every attempt; no
  candidate bytes or manifest identity may change between retries.
- AC-QUAL-004: one transient failure followed by success records one retry and
  returns successfully.
- AC-QUAL-005: exhausted attempts report attempt count, status, signal, and
  error code instead of `unknown failure`.
- AC-QUAL-006: partial output is removed before retry and a cleanup failure is
  terminal.

## BC-QUAL-003 — package metadata to standalone fixture build config

- Producer: immutable N-1 `package.json`; consumer: electron-builder invoked
  with the generated standalone fixture config.
- Producer shape: a non-empty, trimmed top-level `main` entry plus optional
  object-valued `build.extraMetadata`.
- Consumer shape: `extraMetadata.main` in the generated config, preserving the
  original application entry while the updater provider alone changes to the
  loopback qualification feed.
- Failure semantics: missing or malformed entry metadata is rejected before any
  package build; there is no electron-builder preset fallback.
- AC-QUAL-008: the generated fixture config binds `extraMetadata.main` exactly
  to the immutable source package entry.
- AC-QUAL-009: missing or malformed source entry metadata fails closed.

## SEQ-QUAL-001 — bounded Windows installer retry

- Identity key: candidate manifest digest + Windows installer path + install
  root. Initial state is `not_started`.
- Sequence: attempt install → observe exact child result → on failure remove the
  partial install root → wait two seconds → retry, at most three attempts.
- Observable results: success advances to strict installed-resource inspection;
  exhaustion throws the collected diagnostics. Resume/restart/rollback are N/A
  because every hosted job starts from a new temporary root and no report is
  written before full success.
- Persistence boundary: only the final qualification JSON artifact persists;
  failed attempts remain job logs. Related contracts: BC-QUAL-002 and
  AC-QUAL-004..006.

## Release evidence

### Follow-up: run 35003877987, asynchronous installer result consumed too early

- Windows job 104499170463 passed fixture updater validation before and after
  signing/packaging, but the executor threw `Cannot read properties of undefined
  (reading 'hashes')`. All four installed-candidate checks passed; restart update
  never reached the real application launch.
- Cause: the installer retry change made `installWindowsNsis` asynchronous.
  Ordinary installed qualification awaited it, while both calls via
  `installTargetPackage` in the restart executor still consumed the Promise as
  an installed-package result. `expectedIdentity` failed and `finally` began
  cleanup before installation had settled.
- Repair: await both existing calls, without changing the installer, retries,
  updater contract, signed bytes, or success criteria.

#### BC-QUAL-005 — asynchronous Windows installer to restart executor

- Producer: real `installWindowsNsis`; consumer: real
  `runRestartUpdateQualification`, via `installTargetPackage`. Boundary: async
  completion of the NSIS process plus installed-resource inspection.
- Shape: `Promise<installed-package>` resolves to the existing handle containing
  `identity`, `executablePath`, `launchCwd`, `sidecarNeedle`, `candidateNeedle`,
  `close`, and `cleanup`. The Promise itself is not an admitted identity.
- Admission: CLOSED completion ordering; only a resolved handle proceeds into
  the unchanged strict resource/feed validators. No schema or unknown-field
  policy change. Rejection propagates its original error; no empty identity or
  fallback hashes are introduced.
- Identity: candidate installer remains from the sealed candidate directory;
  N-1 installer remains bound to its fixture evidence. Expected hashes cannot be
  read until candidate install resolves. Installed fixture YAML cannot be read
  until fixture install resolves. Runtime manifest/wheel identity checks stay
  unchanged; actual deployed candidate/wheel execution remains NOT_RUN locally.
- AC-QUAL-015: delayed candidate and fixture installation preserve exact event
  order and cannot start feed/runtime or cleanup while installation is pending.
- AC-QUAL-016: candidate rejection stops fixture installation and preserves the
  original error, not a secondary `hashes` TypeError.
- AC-QUAL-017: fixture rejection stops YAML/feed/runtime consumption and preserves
  the original error. Cleanup happens only after rejection settles.
- AC-QUAL-018: even immediately resolved Promises are awaited at both call sites.
- Evidence approach: execute the actual complete orchestrator, Windows adapter,
  target selector, and identity consumer in a VM. Only OS/preflight/process
  boundaries are isolated; the NSIS boundary uses controlled completion/rejection.
  Feed YAML is checked by the real strict validator. The probe deliberately stops
  at application launch and is not described as a full Windows upgrade test.

#### SEQ-QUAL-003 — candidate/fixture installation completion and failure

- Key: exact candidate installer + exact fixture installer + isolated temporary
  root. Sequence: candidate pending → resolved/inspected → fixture pending →
  resolved/inspected → validate YAML → feed → runtime. Each failure instead
  propagates the original exception and then performs existing cleanup.
- No cross-attempt durable resume is added. NSIS retries remain inside the
  producer; success/failure only crosses this boundary once. Reset starts with a
  fresh root. The report persists only on full success; rollback/chat/interaction
  states are N/A for this pre-launch orchestration fix. Real upgrade/restart
  remains required on hosted Windows. Covers BC-QUAL-005 and AC-QUAL-015..018.
- Red-before-green: all four sequence regressions fail on the original caller,
  including the exact CI TypeError and premature-cleanup assertion.
- Green evidence: all four new tests pass after adding only the two awaits;
  release-qa totals are 256 passed, 1 skipped, 0 failed. `git diff --check` passed.
  No local application/model launch or release dispatch was performed.
- Graph limitation: refreshed PuPu index at this checkout still returns UNKNOWN
  for the installer and executor names (full-text index failure). Staged change
  analysis reports CRITICAL, with flows attributed to same-named test fields and
  the local `executor` constant, not a reliable runtime impact verdict. This is
  not claimed as a clean graph check. Direct reference review confirms the CLI
  awaits the executor, ordinary package qualification already awaits the Windows
  adapter, and the only functional changes are its two missing await consumers.

### Follow-up: run 34934079566, missing unsigned fixture updater config

- Observed failure: Windows job 104268520969 completed the N-1 unpacked build,
  then the strict validator failed with ENOENT for `resources/app-update.yml`.
- Root cause: electron-builder 26.8.1's actual `PublishManager` afterPack handler
  skips Windows `dir` targets. Supplying a generic publish config does not cause
  that target to emit updater YAML. This is not an install/auth/network failure.
- Repair scope: materialize the sole allowed fixture updater configuration before
  signing; keep the strict validator unchanged. Revalidate after both prepackaged
  installer passes. No production feed, candidate bytes, version, or gate is relaxed.
- Dependency path: `PlatformPackager.doPack` returns immediately for prepackaged
  inputs. The two existing Azure-signing installer passes therefore preserve the
  prepared file; they do not need a new build-config input or an extra NSIS build.

#### BC-QUAL-004 — unsigned fixture updater YAML to signed/installed consumer

- Producer: candidate-owned `prepare-qualification-fixture-app-update.mjs`, only
  on the runner-local rebuilt N-1 unpacked payload. Consumer: existing strict
  updater validator before signing, after packaging, and on installed resources
  in `run-restart-update-qualification.mjs`.
- Canonical wire shape: YAML object with exactly `provider`, `url`, and
  `updaterCacheDirName`. Values are `generic`, exact runner loopback URL, and
  `pupu-updater`. Admission is CLOSED; no extra keys or remote feed permitted.
- Projection: only the already-authorized fixture updater feed difference; no
  application/sidecar edits. Publisher identity is checked by the existing signed
  fixture evidence and installed qualification, not invented in the YAML.
- Identity: immutable N-1 tag/source/version remains bound by existing fixture
  evidence; the N candidate and its once-built wheel remain immutable. The file
  is finalized before Azure signing, installer creation, and receipt sealing.
- Failure semantics: absent file is created exclusively; existing matching bytes
  are preserved; unknown fields, foreign providers/cache names, URL drift, or
  missing resources directory fail closed. No overwrite-based repair of bad data.
- AC-QUAL-010: execute installed electron-builder's real afterPack producer for
  Windows dir; prove ENOENT before preparation, then strict admission succeeds.
- AC-QUAL-011: execute its NSIS YAML producer and independently compare its parsed
  output and computed cache identity to the prepared config through the validator.
- AC-QUAL-012: execute real prepackaged doPack twice; updater bytes stay identical.
  A control invocation of the actual writer with production config must change
  those bytes and be rejected, proving the test detects feed drift.
- AC-QUAL-013: unknown fields, wrong cache/provider/URL, invalid URL, and missing
  payload are rejected without overwriting existing data or creating directories.
- AC-QUAL-014: workflow ordering is build → prepare/validate → isolate → sign and
  package → validate-only → seal → install/upgrade; native failures remain fatal.

#### SEQ-QUAL-002 — preparation and repeated prepackaged installer passes

- Identity key: runner fixture payload path + exact loopback feed + N-1 identity.
- Initial state: unpacked payload exists, updater YAML absent. Prepare and validate
  it, move the payload, prime elevation helper, sign payload, package/sign installer,
  validate payload again, seal installer evidence, then install and check its YAML.
- Retry: valid existing config is an idempotent read with byte preservation;
  mismatched output fails. Two prepackaged passes preserve the file. No recovery
  edits after signing; a failed job is restarted with a new runner-local fixture.
- Persistence: payload and installer during the job; existing evidence artifacts
  at completion. No durable user-state mutation; chat/interaction/replay paths are
  N/A for this build-only repair. Actual Windows installed upgrade/restart is
  NOT_RUN locally and must pass on the exact candidate/wheel pair in hosted CI.
- Evidence: AC-QUAL-010..014 are local dependency/CLI/workflow tests, not a full
  Windows signing or NSIS execution. Active rollout remains INCOMPLETE pending CI.
- Local verification: all release-qa tests completed with 252 passed, 1 skipped,
  0 failed. The installed app-builder-lib version used by dependency regressions
  is 26.8.1, matching the immutable `v0.1.10` package lock and failed runner log.
  `git diff --check` also passed.

GitNexus initially reported LOW for the validator (direct CLI and restart-upgrade
consumer). Refresh completed the graph but failed its full-text index; subsequent
symbol lookup returned UNKNOWN. This is not an all-clear. Direct call-site and
workflow inspection confirmed the two consumers; the existing validator and its
runtime consumer are unchanged, and the producer is a new fixture-only helper.

Pre-commit refresh indexed the new files. `detect-changes --scope all` then
reported CRITICAL with 1,011 flows, attributed primarily to same-named `modulePath`
constants in unrelated modules. This is not recorded as a clean graph verdict.
Manual verification: the new `modulePath` is module-local and not exported; the
new preparation helper is referenced only by its test and the fixture workflow
(plus the workflow contract test), with no src/electron/backend callers. Both
the unchanged strict consumer and all 253 release tests were checked. This
supports the scoped fixture-only change despite the graph's name-collision
over-approximation; hosted Windows execution remains required.

Unit and workflow-contract tests provide red-before-green evidence. Active
rollout remains `INCOMPLETE` until a new immutable candidate and qualification
run validate the exact PuPu candidate plus its once-built Unchain artifact.
Because qualification workflows execute from the tag commit, a repair merged
only to `dev` cannot certify the existing tag identity; the unpublished
`v0.1.11` tag must be recreated by the project owner on the repaired commit and
the Candidate and Qualification phases must both be rerun.

### 2026-09-15 — N-1 release snapshot omission (run 35019537988)

The N candidate and all four fresh-installed targets passed. The Windows N-1
fixture logged `snapshot not found; using local defaults` during build, then
failed the unchanged installed consumer with `installed app has no valid enabled
release build snapshot`. This is a fixture input omission, not a timeout or an
installer retry failure.

#### BC-QUAL-006 — historical release profile → build snapshot → packaged ASAR

- Producer: the immutable N-1 checkout's own deterministic workflow, literal
  profile selection, `write-build-feature-snapshot.cjs`, and rollout module.
  Consumer: that checkout's `build-web.cjs`, then byte verification against the
  generated snapshot and the existing strict installed-package consumer.
- Transport: runner-local JSON file via `PUPU_BUILD_FEATURE_SNAPSHOT_PATH`, then
  `build/build_feature_flags.json` in `resources/app.asar`. The workflow requires
  `PUPU_REQUIRE_BUILD_FEATURE_SNAPSHOT=1`; missing input cannot become dev defaults.
- Admission: VERSIONED profile through the historical producer's CLOSED schema;
  exactly one literal checkout-relative profile path. No candidate-profile
  substitution, shell evaluation, unknown-field dropping or fallback selection.
  Emitted flags/environment must exactly equal the selected profile; release
  metadata has an exact key set and valid schema/fingerprint shapes. A SHA-256
  sidecar pins the generated bytes. Packaged bytes must equal them exactly.
- Identity: existing prerequisites bind fixture checkout to `FROM_COMMIT` and
  published manifest to tag/version/commit/Unchain revision. Published 0.1.10 is
  commit `020d898de56d1cdebafb136f6176c4d10bbfdcf8`, using
  `contracts/memory-v2/release-profile.all.v2.json`, with both Memory V2 and theme
  customization enabled and rollout `all`. Current candidate uses a different
  path/schema/flag set. Published manifest/QA/qualification receipts do not
  expose the old snapshot fingerprint; do not claim published-byte equality.
  This is deterministic reconstruction from the immutable historical producer.
- Failure: reject missing/dynamic/duplicate/traversing profile selection, invalid
  profile schema/keys, missing required snapshot, checksum mismatch, and any
  packaged snapshot drift before signing. Revalidate after installer packaging;
  never repair a signed payload. Snapshot and checksum are retained with evidence.
- AC-QUAL-019: reproduce default-off output with real build-web snapshot logic,
  pack a real ASAR and observe the unchanged installed consumer reject it.
- AC-QUAL-020: execute real producer/build-web/ASAR/strict consumer with required
  snapshot. Repeat against exact 0.1.10 source and confirm its theme flag survives,
  rollout stays all despite shadow environment inputs, and bytes are identical.
- AC-QUAL-021: malformed profile selection/schema/keys, missing snapshot, unknown
  packaged flag and checksum drift fail closed at their respective boundaries.
- AC-QUAL-022: workflow contract proves preparation precedes build, missing-input
  check precedes sidecar build, and strict byte checks surround signing/packaging.

#### SEQ-QUAL-004 — reconstruct, build, sign and revalidate

- Key: immutable N-1 source commit + producer/profile + snapshot SHA-256.
- Sequence: verify source → reconstruct snapshot → required-input preflight →
  old web/sidecar build → ASAR byte verification → existing loopback-feed
  preparation/signing/two prepackaged installer passes → ASAR byte verification
  → seal → install/restart into exact retained N candidate.
- Repeat preparation produces identical bytes; checksum/packaged drift fails.
  Job retry uses a new runner. No signed-file repair or snapshot fallback.
  Persistence is runner-local snapshot/payload plus retained Actions evidence.
- BC-QUAL-006 maps to AC-QUAL-019..022. Chat/interaction/replay variants are N/A
  because this patch changes only fixture packaging. Real Windows signing,
  NSIS installation and restart upgrade remain NOT_RUN locally; rollout remains
  INCOMPLETE until hosted qualification verifies the exact candidate/wheel pair.
- Local integration uses real producer, build-web snapshot logic, ASAR, and
  installed consumer; only React bundling is stubbed, and executable/sidecar files
  are test placeholders. This is not full Windows app execution. Historical
  integration explicitly skips if a shallow checkout lacks the exact old commit;
  the portable current-source boundary test always runs.
- Impact analysis: repository/worktree `/Users/red/Desktop/GITRepo/PuPu`, refreshed
  at `4ac9cbd6`. Graph lookups for producer and installed consumer returned UNKNOWN
  after FTS degradation. Direct references confirm these shared product functions
  are unchanged; the new helper is fixture-workflow/test-only. Not a clean graph
  safety verdict.

Verification for this patch: `node --test scripts/release-qa/*.test.mjs` completed
with 262 passed, 1 Windows-only skip, and 0 failed (263 total), including actual
0.1.10 source reconstruction and CLI prepare/verify tests. `git diff --check`
passed. Full React compilation and Windows execution are not claimed.

After indexing the new files, `detect-changes --scope all --repo .` included all
four changed files and reported CRITICAL / 1,029 processes, attributed in its
listed flows to `profilePath` property-name links in unrelated modules. The user
was warned. Direct reference inspection confirms the three new exports are
used only by the fixture helper/its tests; production snapshot producer and
installed consumer are unchanged. This does not count as a clean graph check;
hosted Windows qualification remains required.

Pre-submit repetition caught a race in the new test harness: the pinned ASAR
library returns its destination stream from `out.end()` before the stream is
necessarily finished. Immediate same-process extraction could see incomplete
bytes. The harness now awaits `finished(archiveStream)` before inspection or
repacking; no retries, sleeps, production changes or weaker assertions were used.
After this harness correction, all six focused tests passed in ten consecutive
runs, and the full release suite passed three consecutive times (262 passed,
1 platform skip, 0 failures each). Hosted Windows execution is still pending.
