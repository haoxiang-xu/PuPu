# v0.1.11 Windows qualification repair

Date: 2026-09-14

## Incident scope

Qualification run `34918980959` verified the sealed candidate, macOS arm64/x64,
and Linux x64, but failed both Windows paths. The restart-update fixture passed
`UNCHAIN_SOURCE_PATH` while the package wrapper consumes
`UNCHAIN_ARTIFACT_SOURCE_PATH`; the resulting fallback directory did not exist
and surfaced as `spawnSync git ENOENT`. The fresh-install path separately lost
the NSIS process exit identity and reported only `unknown failure`.

Qualification run `34923227917` proved that the workflow was still executing
from the immutable `v0.1.11` tag rather than the later `dev` repair. Its complete
log also exposed two downstream fixture-build contract gaps that the old
PowerShell behavior had hidden: no explicit N-1 build version and loss of the
top-level Electron `main` entry when projecting `package.json` into a standalone
electron-builder configuration.

## BC-QUAL-001 — workflow to immutable Unchain artifact builder

- Producer: `_shared-release-windows-restart-update.yml`; consumer:
  `run-with-unchain-artifact.mjs`; boundary: GitHub Actions environment into the
  Windows Node child process.
- Producer shape and canonical representation: absolute checked-out source path
  in `UNCHAIN_ARTIFACT_SOURCE_PATH` plus the exact 40-character revision in
  `UNCHAIN_ARTIFACT_SOURCE_REF`.
- Consumer shape: the same two exact environment keys. Admission is `CLOSED`;
  aliases and mutable refs are rejected rather than projected.
- Failure semantics: a missing directory, wrong revision, or non-zero native
  command stops the fixture build immediately. There is no degraded fixture.
- Identity: the values bind the N-1 published manifest revision to the checked
  out `fixture-unchain` source and the one wheel built for that fixture.
- AC-QUAL-001: the workflow contract contains both canonical keys and the exact
  revision producer.
- AC-QUAL-002: the old `UNCHAIN_SOURCE_PATH` alias is absent from the fixture
  build environment.
- AC-QUAL-003: PowerShell stops on the first failed native build command.
- AC-QUAL-007: every N-1 build command receives the exact `from_version` through
  `PUPU_BUILD_VERSION`.

## BC-QUAL-002 — NSIS process to installed qualification

- Producer: the signed NSIS installer process; consumer:
  `installed-package-qualification.mjs`; boundary: Windows child-process result.
- Producer shape: `status`, `signal`, `error.code`, stdout, and stderr from
  `spawnSync`; canonical representation is a bounded per-attempt diagnostic.
- Consumer shape: success only for status `0` with no spawn error. Admission is
  `CLOSED`; missing output never becomes success and all failures retain process
  identity fields.
- Projection: stdout/stderr become bounded diagnostic text. Unknown process
  fields are ignored; exhausted retries fail closed.
- Identity: installer path and install root remain fixed for every attempt; no
  candidate bytes or manifest identity may change between retries.
- AC-QUAL-004: one transient failure followed by success records one retry and
  returns successfully.
- AC-QUAL-005: exhausted attempts report attempt count, status, signal, and
  error code instead of `unknown failure`.
- AC-QUAL-006: partial output is removed before retry and a cleanup failure is
  terminal.

## BC-QUAL-003 — package metadata to standalone fixture build config

- Producer: immutable N-1 `package.json`; consumer: electron-builder invoked
  with the generated standalone fixture config.
- Producer shape: a non-empty, trimmed top-level `main` entry plus optional
  object-valued `build.extraMetadata`.
- Consumer shape: `extraMetadata.main` in the generated config, preserving the
  original application entry while the updater provider alone changes to the
  loopback qualification feed.
- Failure semantics: missing or malformed entry metadata is rejected before any
  package build; there is no electron-builder preset fallback.
- AC-QUAL-008: the generated fixture config binds `extraMetadata.main` exactly
  to the immutable source package entry.
- AC-QUAL-009: missing or malformed source entry metadata fails closed.

## SEQ-QUAL-001 — bounded Windows installer retry

- Identity key: candidate manifest digest + Windows installer path + install
  root. Initial state is `not_started`.
- Sequence: attempt install → observe exact child result → on failure remove the
  partial install root → wait two seconds → retry, at most three attempts.
- Observable results: success advances to strict installed-resource inspection;
  exhaustion throws the collected diagnostics. Resume/restart/rollback are N/A
  because every hosted job starts from a new temporary root and no report is
  written before full success.
- Persistence boundary: only the final qualification JSON artifact persists;
  failed attempts remain job logs. Related contracts: BC-QUAL-002 and
  AC-QUAL-004..006.

## Release evidence

### Follow-up: run 35003877987, asynchronous installer result consumed too early

- Windows job 104499170463 passed fixture updater validation before and after
  signing/packaging, but the executor threw `Cannot read properties of undefined
  (reading 'hashes')`. All four installed-candidate checks passed; restart update
  never reached the real application launch.
- Cause: the installer retry change made `installWindowsNsis` asynchronous.
  Ordinary installed qualification awaited it, while both calls via
  `installTargetPackage` in the restart executor still consumed the Promise as
  an installed-package result. `expectedIdentity` failed and `finally` began
  cleanup before installation had settled.
- Repair: await both existing calls, without changing the installer, retries,
  updater contract, signed bytes, or success criteria.

#### BC-QUAL-005 — asynchronous Windows installer to restart executor

- Producer: real `installWindowsNsis`; consumer: real
  `runRestartUpdateQualification`, via `installTargetPackage`. Boundary: async
  completion of the NSIS process plus installed-resource inspection.
- Shape: `Promise<installed-package>` resolves to the existing handle containing
  `identity`, `executablePath`, `launchCwd`, `sidecarNeedle`, `candidateNeedle`,
  `close`, and `cleanup`. The Promise itself is not an admitted identity.
- Admission: CLOSED completion ordering; only a resolved handle proceeds into
  the unchanged strict resource/feed validators. No schema or unknown-field
  policy change. Rejection propagates its original error; no empty identity or
  fallback hashes are introduced.
- Identity: candidate installer remains from the sealed candidate directory;
  N-1 installer remains bound to its fixture evidence. Expected hashes cannot be
  read until candidate install resolves. Installed fixture YAML cannot be read
  until fixture install resolves. Runtime manifest/wheel identity checks stay
  unchanged; actual deployed candidate/wheel execution remains NOT_RUN locally.
- AC-QUAL-015: delayed candidate and fixture installation preserve exact event
  order and cannot start feed/runtime or cleanup while installation is pending.
- AC-QUAL-016: candidate rejection stops fixture installation and preserves the
  original error, not a secondary `hashes` TypeError.
- AC-QUAL-017: fixture rejection stops YAML/feed/runtime consumption and preserves
  the original error. Cleanup happens only after rejection settles.
- AC-QUAL-018: even immediately resolved Promises are awaited at both call sites.
- Evidence approach: execute the actual complete orchestrator, Windows adapter,
  target selector, and identity consumer in a VM. Only OS/preflight/process
  boundaries are isolated; the NSIS boundary uses controlled completion/rejection.
  Feed YAML is checked by the real strict validator. The probe deliberately stops
  at application launch and is not described as a full Windows upgrade test.

#### SEQ-QUAL-003 — candidate/fixture installation completion and failure

- Key: exact candidate installer + exact fixture installer + isolated temporary
  root. Sequence: candidate pending → resolved/inspected → fixture pending →
  resolved/inspected → validate YAML → feed → runtime. Each failure instead
  propagates the original exception and then performs existing cleanup.
- No cross-attempt durable resume is added. NSIS retries remain inside the
  producer; success/failure only crosses this boundary once. Reset starts with a
  fresh root. The report persists only on full success; rollback/chat/interaction
  states are N/A for this pre-launch orchestration fix. Real upgrade/restart
  remains required on hosted Windows. Covers BC-QUAL-005 and AC-QUAL-015..018.
- Red-before-green: all four sequence regressions fail on the original caller,
  including the exact CI TypeError and premature-cleanup assertion.
- Green evidence: all four new tests pass after adding only the two awaits;
  release-qa totals are 256 passed, 1 skipped, 0 failed. `git diff --check` passed.
  No local application/model launch or release dispatch was performed.
- Graph limitation: refreshed PuPu index at this checkout still returns UNKNOWN
  for the installer and executor names (full-text index failure). Staged change
  analysis reports CRITICAL, with flows attributed to same-named test fields and
  the local `executor` constant, not a reliable runtime impact verdict. This is
  not claimed as a clean graph check. Direct reference review confirms the CLI
  awaits the executor, ordinary package qualification already awaits the Windows
  adapter, and the only functional changes are its two missing await consumers.

### Follow-up: run 34934079566, missing unsigned fixture updater config

- Observed failure: Windows job 104268520969 completed the N-1 unpacked build,
  then the strict validator failed with ENOENT for `resources/app-update.yml`.
- Root cause: electron-builder 26.8.1's actual `PublishManager` afterPack handler
  skips Windows `dir` targets. Supplying a generic publish config does not cause
  that target to emit updater YAML. This is not an install/auth/network failure.
- Repair scope: materialize the sole allowed fixture updater configuration before
  signing; keep the strict validator unchanged. Revalidate after both prepackaged
  installer passes. No production feed, candidate bytes, version, or gate is relaxed.
- Dependency path: `PlatformPackager.doPack` returns immediately for prepackaged
  inputs. The two existing Azure-signing installer passes therefore preserve the
  prepared file; they do not need a new build-config input or an extra NSIS build.

#### BC-QUAL-004 — unsigned fixture updater YAML to signed/installed consumer

- Producer: candidate-owned `prepare-qualification-fixture-app-update.mjs`, only
  on the runner-local rebuilt N-1 unpacked payload. Consumer: existing strict
  updater validator before signing, after packaging, and on installed resources
  in `run-restart-update-qualification.mjs`.
- Canonical wire shape: YAML object with exactly `provider`, `url`, and
  `updaterCacheDirName`. Values are `generic`, exact runner loopback URL, and
  `pupu-updater`. Admission is CLOSED; no extra keys or remote feed permitted.
- Projection: only the already-authorized fixture updater feed difference; no
  application/sidecar edits. Publisher identity is checked by the existing signed
  fixture evidence and installed qualification, not invented in the YAML.
- Identity: immutable N-1 tag/source/version remains bound by existing fixture
  evidence; the N candidate and its once-built wheel remain immutable. The file
  is finalized before Azure signing, installer creation, and receipt sealing.
- Failure semantics: absent file is created exclusively; existing matching bytes
  are preserved; unknown fields, foreign providers/cache names, URL drift, or
  missing resources directory fail closed. No overwrite-based repair of bad data.
- AC-QUAL-010: execute installed electron-builder's real afterPack producer for
  Windows dir; prove ENOENT before preparation, then strict admission succeeds.
- AC-QUAL-011: execute its NSIS YAML producer and independently compare its parsed
  output and computed cache identity to the prepared config through the validator.
- AC-QUAL-012: execute real prepackaged doPack twice; updater bytes stay identical.
  A control invocation of the actual writer with production config must change
  those bytes and be rejected, proving the test detects feed drift.
- AC-QUAL-013: unknown fields, wrong cache/provider/URL, invalid URL, and missing
  payload are rejected without overwriting existing data or creating directories.
- AC-QUAL-014: workflow ordering is build → prepare/validate → isolate → sign and
  package → validate-only → seal → install/upgrade; native failures remain fatal.

#### SEQ-QUAL-002 — preparation and repeated prepackaged installer passes

- Identity key: runner fixture payload path + exact loopback feed + N-1 identity.
- Initial state: unpacked payload exists, updater YAML absent. Prepare and validate
  it, move the payload, prime elevation helper, sign payload, package/sign installer,
  validate payload again, seal installer evidence, then install and check its YAML.
- Retry: valid existing config is an idempotent read with byte preservation;
  mismatched output fails. Two prepackaged passes preserve the file. No recovery
  edits after signing; a failed job is restarted with a new runner-local fixture.
- Persistence: payload and installer during the job; existing evidence artifacts
  at completion. No durable user-state mutation; chat/interaction/replay paths are
  N/A for this build-only repair. Actual Windows installed upgrade/restart is
  NOT_RUN locally and must pass on the exact candidate/wheel pair in hosted CI.
- Evidence: AC-QUAL-010..014 are local dependency/CLI/workflow tests, not a full
  Windows signing or NSIS execution. Active rollout remains INCOMPLETE pending CI.
- Local verification: all release-qa tests completed with 252 passed, 1 skipped,
  0 failed. The installed app-builder-lib version used by dependency regressions
  is 26.8.1, matching the immutable `v0.1.10` package lock and failed runner log.
  `git diff --check` also passed.

GitNexus initially reported LOW for the validator (direct CLI and restart-upgrade
consumer). Refresh completed the graph but failed its full-text index; subsequent
symbol lookup returned UNKNOWN. This is not an all-clear. Direct call-site and
workflow inspection confirmed the two consumers; the existing validator and its
runtime consumer are unchanged, and the producer is a new fixture-only helper.

Pre-commit refresh indexed the new files. `detect-changes --scope all` then
reported CRITICAL with 1,011 flows, attributed primarily to same-named `modulePath`
constants in unrelated modules. This is not recorded as a clean graph verdict.
Manual verification: the new `modulePath` is module-local and not exported; the
new preparation helper is referenced only by its test and the fixture workflow
(plus the workflow contract test), with no src/electron/backend callers. Both
the unchanged strict consumer and all 253 release tests were checked. This
supports the scoped fixture-only change despite the graph's name-collision
over-approximation; hosted Windows execution remains required.

Unit and workflow-contract tests provide red-before-green evidence. Active
rollout remains `INCOMPLETE` until a new immutable candidate and qualification
run validate the exact PuPu candidate plus its once-built Unchain artifact.
Because qualification workflows execute from the tag commit, a repair merged
only to `dev` cannot certify the existing tag identity; the unpublished
`v0.1.11` tag must be recreated by the project owner on the repaired commit and
the Candidate and Qualification phases must both be rerun.

### 2026-09-15 — N-1 release snapshot omission (run 35019537988)

The N candidate and all four fresh-installed targets passed. The Windows N-1
fixture logged `snapshot not found; using local defaults` during build, then
failed the unchanged installed consumer with `installed app has no valid enabled
release build snapshot`. This is a fixture input omission, not a timeout or an
installer retry failure.

#### BC-QUAL-006 — historical release profile → build snapshot → packaged ASAR

- Producer: the immutable N-1 checkout's own deterministic workflow, literal
  profile selection, `write-build-feature-snapshot.cjs`, and rollout module.
  Consumer: that checkout's `build-web.cjs`, then byte verification against the
  generated snapshot and the existing strict installed-package consumer.
- Transport: runner-local JSON file via `PUPU_BUILD_FEATURE_SNAPSHOT_PATH`, then
  `build/build_feature_flags.json` in `resources/app.asar`. The workflow requires
  `PUPU_REQUIRE_BUILD_FEATURE_SNAPSHOT=1`; missing input cannot become dev defaults.
- Admission: VERSIONED profile through the historical producer's CLOSED schema;
  exactly one literal checkout-relative profile path. No candidate-profile
  substitution, shell evaluation, unknown-field dropping or fallback selection.
  Emitted flags/environment must exactly equal the selected profile; release
  metadata has an exact key set and valid schema/fingerprint shapes. A SHA-256
  sidecar pins the generated bytes. Packaged bytes must equal them exactly.
- Identity: existing prerequisites bind fixture checkout to `FROM_COMMIT` and
  published manifest to tag/version/commit/Unchain revision. Published 0.1.10 is
  commit `020d898de56d1cdebafb136f6176c4d10bbfdcf8`, using
  `contracts/memory-v2/release-profile.all.v2.json`, with both Memory V2 and theme
  customization enabled and rollout `all`. Current candidate uses a different
  path/schema/flag set. Published manifest/QA/qualification receipts do not
  expose the old snapshot fingerprint; do not claim published-byte equality.
  This is deterministic reconstruction from the immutable historical producer.
- Failure: reject missing/dynamic/duplicate/traversing profile selection, invalid
  profile schema/keys, missing required snapshot, checksum mismatch, and any
  packaged snapshot drift before signing. Revalidate after installer packaging;
  never repair a signed payload. Snapshot and checksum are retained with evidence.
- AC-QUAL-019: reproduce default-off output with real build-web snapshot logic,
  pack a real ASAR and observe the unchanged installed consumer reject it.
- AC-QUAL-020: execute real producer/build-web/ASAR/strict consumer with required
  snapshot. Repeat against exact 0.1.10 source and confirm its theme flag survives,
  rollout stays all despite shadow environment inputs, and bytes are identical.
- AC-QUAL-021: malformed profile selection/schema/keys, missing snapshot, unknown
  packaged flag and checksum drift fail closed at their respective boundaries.
- AC-QUAL-022: workflow contract proves preparation precedes build, missing-input
  check precedes sidecar build, and strict byte checks surround signing/packaging.

#### SEQ-QUAL-004 — reconstruct, build, sign and revalidate

- Key: immutable N-1 source commit + producer/profile + snapshot SHA-256.
- Sequence: verify source → reconstruct snapshot → required-input preflight →
  old web/sidecar build → ASAR byte verification → existing loopback-feed
  preparation/signing/two prepackaged installer passes → ASAR byte verification
  → seal → install/restart into exact retained N candidate.
- Repeat preparation produces identical bytes; checksum/packaged drift fails.
  Job retry uses a new runner. No signed-file repair or snapshot fallback.
  Persistence is runner-local snapshot/payload plus retained Actions evidence.
- BC-QUAL-006 maps to AC-QUAL-019..022. Chat/interaction/replay variants are N/A
  because this patch changes only fixture packaging. Real Windows signing,
  NSIS installation and restart upgrade remain NOT_RUN locally; rollout remains
  INCOMPLETE until hosted qualification verifies the exact candidate/wheel pair.
- Local integration uses real producer, build-web snapshot logic, ASAR, and
  installed consumer; only React bundling is stubbed, and executable/sidecar files
  are test placeholders. This is not full Windows app execution. Historical
  integration explicitly skips if a shallow checkout lacks the exact old commit;
  the portable current-source boundary test always runs.
- Impact analysis: repository/worktree `/Users/red/Desktop/GITRepo/PuPu`, refreshed
  at `4ac9cbd6`. Graph lookups for producer and installed consumer returned UNKNOWN
  after FTS degradation. Direct references confirm these shared product functions
  are unchanged; the new helper is fixture-workflow/test-only. Not a clean graph
  safety verdict.

Verification for this patch: `node --test scripts/release-qa/*.test.mjs` completed
with 262 passed, 1 Windows-only skip, and 0 failed (263 total), including actual
0.1.10 source reconstruction and CLI prepare/verify tests. `git diff --check`
passed. Full React compilation and Windows execution are not claimed.

After indexing the new files, `detect-changes --scope all --repo .` included all
four changed files and reported CRITICAL / 1,029 processes, attributed in its
listed flows to `profilePath` property-name links in unrelated modules. The user
was warned. Direct reference inspection confirms the three new exports are
used only by the fixture helper/its tests; production snapshot producer and
installed consumer are unchanged. This does not count as a clean graph check;
hosted Windows qualification remains required.

Pre-submit repetition caught a race in the new test harness: the pinned ASAR
library returns its destination stream from `out.end()` before the stream is
necessarily finished. Immediate same-process extraction could see incomplete
bytes. The harness now awaits `finished(archiveStream)` before inspection or
repacking; no retries, sleeps, production changes or weaker assertions were used.
After this harness correction, all six focused tests passed in ten consecutive
runs, and the full release suite passed three consecutive times (262 passed,
1 platform skip, 0 failures each). Hosted Windows execution is still pending.

### 2026-09-15 — restart runtime ownership and first-error loss (35037639772)

Qualification `35037639772`, Windows job `104610590075`, passed fixture snapshot
checks before and after signing, then failed deleting the live N-1 installation
with `EBUSY`. All four fresh-install targets passed. Retained loopback-feed
evidence contains no requests. The initial runtime exception was replaced by
the unguarded cleanup exception; its exact cause cannot be recovered from these
artifacts. Do not describe this repair as proof that the full upgrade passes.

Confirmed code defects: runtime ownership was assigned only after renderer and
sidecar readiness, CDP ownership could be lost during a partial connection, and
cleanup exceptions could replace the original failure or skip subsequent
cleanup. The restarted N PID was also not registered before its identity checks.

#### BC-QUAL-007 — spawned runtime/CDP resources to qualification teardown

- Producer: real child-process spawn, CDP connection, and relaunched-process
  discovery. Consumer: the qualification runner's independent cleanup steps.
  Boundary: OS processes and renderer socket lifetimes across rejected awaits.
- Canonical handle: child, bounded output, observed PID set, userData, debug port,
  browser (nullable until acquired), and spawn error (nullable). The same mutable
  session is registered immediately after spawn; browser ownership is recorded
  immediately after connection, before any renderer probe. Runtime readiness
  does not decide whether a resource needs cleanup.
- Admission: CLOSED PID selection. Only positive safe-integer PIDs in this
  run's known tree or exact private installation directory are selected; current
  and parent PIDs are excluded before descendant expansion. Bare product-name
  matches and prefix-neighbor directories are not admitted. OS row extensions
  are ignored; only PID, parent PID and command are projected.
- Failure semantics: print and rethrow the first runtime error unchanged.
  Record each cleanup error separately; attempt every remaining cleanup step.
  Enumeration failure still allows known PIDs to be terminated. Check actual
  taskkill status/spawn errors and wait for exit before bounded directory-removal
  retries. CDP/feed close waits are bounded; native operations have finite
  per-call timeouts. Cleanup-only failure is terminal, never a passing report.
- Identity: isolated roots belong to the already validated fixture/candidate
  installers. Existing hashes, snapshot, signer, runtime and updater admission
  are unchanged. Relaunched N enters the owned PID set before sidecar/identity
  validation. No signing, product flag, installation or upgrade semantics change.
- AC-QUAL-023: actual startup retains its child/CDP session when spawn, renderer
  or sidecar readiness fails, and actual teardown terminates those resources.
- AC-QUAL-024: renderer failure plus EBUSY/enumeration/taskkill/evidence-write or
  socket-close failures preserves the original cause and attempts all cleanup.
- AC-QUAL-025: late N identity failure cleans the new process tree; current,
  parent, unrelated PuPu and path-prefix siblings are not killed.
- AC-QUAL-026: the successful lifecycle still reaches the existing passing
  report; an otherwise successful lifecycle with cleanup failure rejects.

#### BC-QUAL-008 — failed runtime to retained diagnostic artifact

- Producer: the real runner catch/finally; consumer: human Actions diagnosis,
  not the release qualification validator. Transport: runner-local JSON outside
  the removable temp root, retained by the existing always-upload evidence step.
- Shape: `pupu.restart-update-diagnostics.v1`, failed status, target, candidate
  tag/commit/manifest digest, fixture tag/commit, stage, primary error, secondary
  cleanup errors, bounded output tail, owned process rows and feed-request count.
  Text redacts common credential forms. No environment dump or global process
  table is uploaded; at most 256 scoped process rows are retained.
- Admission: OPEN diagnostic extensions only. This JSON cannot authorize
  admission; the CLOSED success report schema and its exact filename remain
  unchanged. Missing or malformed diagnostics never imply a passing run.
- Failure semantics: log first failure before cleanup, write diagnostic before
  removing resources, then update it with cleanup results. Evidence write errors
  are secondary; remaining cleanup is still attempted. Upload failure cannot
  turn the failed runtime outcome into success.
- AC-QUAL-027: diagnostic stage, first/secondary causes, candidate/fixture identity,
  scoped output and empty feed evidence remain available despite runtime failure;
  retained text is bounded and credential-redacted.
- AC-QUAL-028: parsed workflow contract retains the separate diagnostic filename
  with `always()` upload and still enforces the actual runtime step outcome.

#### SEQ-QUAL-005 — failed partial startup or failed relaunched N

- Key: exact candidate manifest + signed N-1 fixture identity + fresh temp root.
  Spawn → immediately register child → connect/register CDP → renderer ready →
  sidecar ready → existing updater checks → old exit → immediately register N
  relaunch → existing N identity/settings/feed checks → teardown.
- Any rejection → print original cause → capture scoped process evidence/write
  diagnostic → close CDP → terminate scoped processes/wait → write feed evidence
  and close server → cleanup installations/root → update diagnostic → reject
  original error (or reject cleanup error if lifecycle otherwise succeeded).
- Repeated native kill races tolerate an already-dead process, but an actual
  still-live failed kill is recorded. Retrying a hosted job starts fresh; there
  is no durable cross-job resume or reuse of a partly upgraded installation.
  Chat/interaction/provider/replay variants are N/A: only the qualification
  harness is changed, not application behavior. Success criteria are unchanged.
- Persistence: failed JSON/log artifacts outside the temp root; successful
  receipt only after all required checks and teardown succeed. Covers
  BC-QUAL-007/008 and AC-QUAL-023..028. Full Windows execution of the exact signed
  candidate plus once-built Unchain wheel remains NOT_RUN locally / INCOMPLETE.
- Local evidence executes the actual complete runner in a VM with strict OS/CDP/
  updater boundary fakes and a virtual clock. It launches no PuPu, NSIS, browser
  or local model. It validates harness lifetimes, not real Windows installation.

Impact analysis used PuPu at `/Users/red/Desktop/GITRepo/PuPu`, based on commit
`2e9c0de7883dc020d3bfbf82f758f2bbcfa3cbf4`. Refresh completed but FTS failed and
symbol lookup mixed JavaScript names with unrelated Python identities. Results
included UNKNOWN and CRITICAL; the user was warned. They are not clean safety
evidence. Direct caller inspection bounds the edited helpers to this runner and
its tests; the Windows workflow is its deployed caller. Product code and closed
qualification validators are unchanged.

#### BC-QUAL-009 — Windows native profile path through NSIS relaunch

- Further source audit found a separate deterministic mismatch: the harness
  predicted `<temp>/appdata/PuPu` by overriding `APPDATA`, but N-1 saves its
  sentinel via Electron `app.getPath("userData")`. Electron 40.8.5 (pinned by
  the exact 0.1.10 lockfile) obtains that from native appData plus application
  name. Its Chromium 144.0.7559.236 resolves Windows appData using
  `SHGetFolderPath(CSIDL_APPDATA)`, not the injected environment variable.
  Sources: [Electron provider](https://raw.githubusercontent.com/electron/electron/v40.8.5/shell/common/electron_paths.cc),
  [Windows alias](https://raw.githubusercontent.com/electron/electron/v40.8.5/shell/common/electron_paths.h),
  [Chromium Windows implementation](https://raw.githubusercontent.com/chromium/chromium/144.0.7559.236/base/base_paths_win.cc).
  The historical app has no `app.setPath` override. This is a confirmed harness
  defect, not proof of the initial exception lost from run 35037639772.
- Producer: native Windows ApplicationData known-folder query through PowerShell
  `[Environment]::GetFolderPath('ApplicationData')`. Consumer: launch description
  and sentinel-file reader; actual N-1/N writes are the existing Electron API.
  The package name in both immutable source versions is `PuPu`.
- Canonical shape: one non-empty, absolute local Windows directory string plus
  the fixed `PuPu` child. CLOSED admission rejects absent/malformed paths, local
  or self-hosted execution, and any pre-existing PuPu profile before either
  installer runs; the profile check repeats immediately before app launch.
  No arbitrary fallback to environment values or temporary profile is allowed.
- The environment must identify a disposable GitHub-hosted runner. No
  APPDATA/LOCALAPPDATA override or `--user-data-dir` is introduced; NSIS's normal
  N relaunch uses the same native profile as N-1. The harness does not delete or
  replace a native profile. That fresh profile's lifetime ends with disposal of
  the hosted VM; only this run's temporary installation tree is deleted locally.
- Identity/compatibility: signed N-1 bytes remain untouched, and exact candidate
  artifact/sidecar/snapshot checks remain unchanged. The path is a filesystem
  lookup, not protocol admission or runtime capability evidence.
- AC-QUAL-029: different inherited APPDATA and native known-folder paths produce
  sentinel reads at the native path, matching both pre/post-restart reads; no
  environment override or relaunch-lost userData argument is emitted.
- AC-QUAL-030: missing native path, non-hosted execution and pre-existing profile
  fail before either installer or app spawn and never delete the native profile. macOS's existing
  isolated launch remains unchanged. VM tests fake native boundaries only.
- Applies to SEQ-QUAL-005: native profile preflight precedes resource acquisition;
  the same native identity persists through the real installer relaunch. Hosted
  end-to-end evidence remains NOT_RUN, not simulated as a completed upgrade.

Red-before-green for BC-QUAL-007/008 used the actual old runner from `2e9c0de7`
and the same VM boundary harness: old code did not register the partial runtime,
returned the injected EBUSY instead of the renderer error, left PIDs 101/102 and
the CDP browser open, and wrote no diagnostic. Patched code registers ownership,
returns the exact original error, terminates both PIDs/closes CDP, and preserves
diagnostics. No real OS process, installer, browser or model was started.

The final graph change check was repeated with `--scope all --repo . --limit
1000`. It still reported CRITICAL / 1,030 flows through the test double's `error`
method, while omitting the actual edited runner symbols and untracked new test
from its changed-symbol listing. This is an unresolved index defect, not a clean
change-impact result. Direct diff/caller review and executable regressions are
the local evidence; no commit, pipeline dispatch or release admission is implied.

Final local verification: all 286 release-qa tests completed with 285 passed,
one existing Windows-only platform skip and zero failures. The 32 focused
lifecycle/sequence/launch/workflow regressions passed three consecutive runs.
The full-run VM success case crosses the real strict restart report validator
and updater-config validator; a mismatched payload-hash case completes the
simulated lifecycle but is rejected at `validate-report` and still cleans up.
`git diff --check` and runner syntax checks pass. Independent final review found
no remaining actionable issues after moving safety preflight ahead of NSIS.
Actual signed Windows N-1 → N execution remains pending hosted qualification.

Submission check: after explicitly staging all seven repair files, repeated
`detect-changes --scope all --repo . --limit 2000` included seven files and 51
symbols, but still reported CRITICAL / 1,030 flows with corrupt unrelated symbol
names mapped to the lifecycle test. The user was warned; this remains an index
limitation, not a clean graph verdict. The full suite was rerun immediately
before submission: 285 passed, one platform skip, zero failures.

### 2026-09-15 — real Windows N-1 sidecar orphan (35046185941)

Candidate `35042352864` / commit `74d5f49275c83dd9b32d29de928669686322d87a`
passed packaging and all four fresh-install targets. Qualification reached
downloaded → install requested → old Electron exit, but the old onefile
sidecar child remained alive. Diagnostic artifact `10427319719` records old
Electron PID 6656 and wrapper 1156 gone, child 8528 plus its console 2984 alive,
and candidate installer 7420 started. The primary failure is
`old N-1 process tree cleanup timed out`; browser-close timeout is secondary.
This proves an old sidecar leak, not the installer's exact internal/UI state.

Historical 0.1.10 commit `020d898de56d1cdebafb136f6176c4d10bbfdcf8` stops only
its direct ChildProcess. Killing a PyInstaller onefile wrapper is not proof
that its child exited. The candidate still has the same lifecycle behavior.
Do not modify historical fixture code, let the test runner kill processes to
produce a passing result, waive the old-tree check, or call a longer wait a fix.

#### BC-QUAL-010 — successor installer to existing installation processes

- Producer: signed candidate NSIS one-click installer; consumer: its embedded
  Windows PowerShell/native process helper, before running the old uninstaller
  or replacing any files. `$INSTDIR` is selected by NSIS `initMultiUser`, including
  the existing registry path and `/D` override. Other install modes are rejected
  at compilation rather than silently using an intermediate directory.
- Transport: an argument to `-File`, never interpolated PowerShell source. The
  installer embeds the helper and native source in its private plugin directory;
  both prepackaged signing passes consume the same candidate source files.
- Admission: CLOSED local absolute install directory, rejecting drive roots,
  UNC/relative/invalid paths and protected profile/system directory roots. Only
  the canonical exact `resources/unchain_runtime/dist/windows/unchain-server.exe`
  image is an ownership root. Another installation, path-prefix neighbor, or
  command line mentioning that path is not an ownership match.
- Canonical identity: held Windows process handle + creation time + exact image
  path (native final-path resolution handles short-name aliases). Descendants
  require a recorded parent identity and compatible creation/exit times.
  Current helper/installer ancestry is excluded. Never infer sibling ownership
  from a dead wrapper PID or globally kill a process by executable name.
- If the exact installed PuPu UI is still running, wait a short bounded interval
  and fail without terminating its sidecar. For an already exiting update, pin
  owned sidecar/descendant handles, terminate only those instances, and verify
  exit. Repeated scans catch children created during teardown. Enumeration,
  identity, access, timeout or termination failure aborts before file replacement.
- Identity is tied to the exact candidate installer and unchanged N-1 fixture.
  There is no feature flag, runtime protocol, updater feed or user-data change.
  Helper JSON is diagnostic only: schema `pupu.installer-sidecar-cleanup.v1`,
  status and termination count; it never substitutes for qualification evidence.
- AC-QUAL-031: native harmless Windows fixtures reproduce wrapper-exited child
  survival, and the actual embedded helper stops the exact orphan and its tree.
- AC-QUAL-032: same-name unrelated installations, prefix neighbors, active UI,
  invalid/protected paths, repeat execution, and spaced/quoted/Unicode/short paths
  prove scope and fail-closed admission. Failed probes cannot authorize copying.
- AC-QUAL-033: actual NSIS compilation plus source/workflow contracts prove the
  helper runs before uninstall/overwrite in candidate packages, including both
  signing passes; the immutable old fixture is not patched with this repair.

#### BC-QUAL-011 — managed sidecar lifetime through Windows app exit/restart

- Producer: Electron's owned ChildProcess; consumer: synchronous bounded native
  Windows tree termination while the root still exists. CLOSED admission accepts
  only the current managed instance's safe PID, never a name/global process scan.
- Windows must not kill the wrapper first and then discover its children. A
  successful termination request is still followed by the managed exit fence;
  failure remains visible and blocks starting another generation. Repeat stop
  is idempotent; delayed callbacks from an old generation cannot clear a new one.
- Non-Windows TERM/escalation behavior and existing stop/restart callers remain
  compatible. This changes no IPC/runtime capability or persisted application
  schema. Application quit cannot rely on an unawaited timer to finish cleanup.
- AC-QUAL-034: actual service with strict native boundary fakes proves tree-kill
  precedes wrapper exit, bounded/error/status handling, protected PID rejection,
  repeated stop, successful second start and failed stop preventing a second start.
- AC-QUAL-035: non-Windows lifecycle and boot-recovery regressions remain green;
  Electron `.js`/`.cjs` test twins remain synchronized.

#### SEQ-QUAL-006 — upgrade from unmodified legacy sidecar lifecycle

- Identity: exact N-1 fixture + candidate installer/manifest + once-built
  candidate Unchain wheel. Start old app → persist sentinel → download exact N →
  request install → old UI/wrapper exits → N installer's preflight observes and
  stops old orphan → old uninstall/file replacement → relaunch N → verify exact
  hashes, settings, feed and no old processes. The runner remains an observer.
- Retry the helper after already-stopped processes is safe; a failed preflight
  does not overwrite files. No cross-job reuse of partial installations. Normal
  app restart exercises stop → verified exit → second start; failed stop blocks
  the next start. User data is never deleted. Persisted settings are compared by
  the unchanged qualification consumer across the upgrade.
- Maps BC-QUAL-010/011 to AC-QUAL-031..035 and the existing full restart report.
  Chat/message/interaction/provider/replay matrices are N/A to the installer
  input contract; general runtime regression tests cover shared stop callers.
  Exact signed Windows N-1 → N plus subsequent N shutdown remains NOT_RUN until
  hosted qualification. Local tests must not be reported as release clearance.

Impact binding: `/Users/red/Desktop/GITRepo/PuPu`, repair branch based on
`74d5f49275c83dd9b32d29de928669686322d87a`. GitNexus index at `eb460bb` has
unresolved symbol mappings/FTS failure: stopMiso/createUnchainService and package
lookups returned UNKNOWN, not a safe verdict. Direct callers include will-quit,
boot-recovery and explicit restart. Installer consumers are local NSIS builds
and both Windows Artifact Signing prepackaged passes. User was warned before
edits; native Windows tests and final hosted artifact qualification are required.

Implementation and local verification for BC-QUAL-010/011:

- Candidate-only `nsis.include` embeds the PowerShell launcher and C# helper.
  Native handles pin process generation through image checks, descendant
  expansion, termination and post-kill rescans. Query-only inspection precedes
  termination rights, so another installation is not asked for kill access.
  Directory admission covers redirected and native system/program-files roots.
- Application stop uses bounded synchronous Windows PID-tree termination before
  killing the wrapper; actual exit remains the restart fence. Invalid/stale PIDs,
  taskkill errors/timeouts and missing exit events block another server start.
  Late events from an earlier instance cannot overwrite the current instance.
- The actual NSIS compiler in the pinned electron-builder cache compiled the
  production include/embedded files and rejected the unsupported assisted mode.
  This is compilation evidence, not execution of the Windows installer.
- Full release tooling: 290 tests, 289 passed, one existing Windows platform
  skip, zero failures (run twice). Four new installer contracts, including real
  NSIS compilation, passed. The service/boot-readiness set ran through
  react-scripts with Node environment and Electron roots: 126 passed. Frontend
  setup is excluded for these Node tests; default CRA src/jsdom setup is not a
  compatible Electron-test launcher. The unchanged service loader imports the
  `.cjs` tests, so no test twin can diverge.
- Red-before-green: the same added Windows lifecycle regression against exact
  baseline `74d5f492` fails because the old stop sends SIGTERM to the parent before
  a tree shutdown; repaired code passes. No product, browser or local model was
  launched on the developer Mac.
- Blocking native tests are wired into Windows packaging, in the `pupu`
  checkout, before build/signing. They execute the actual cleanup helper under
  Windows PowerShell 5.1 x64 AND x86 against harmless real process trees/orphans,
  active-UI denial, other installations, path collisions, protected paths and
  Unicode/quoted paths. Real 8.3 alias cases run when the filesystem exposes an
  alias; otherwise report NOT_RUN. Access-denial and deterministic PID-reuse
  injection are explicitly NOT_RUN, not inferred from positive cases. No native
  test has run on this Mac; no Windows execution or upgrade pass is claimed.
- Shared stop also reaches active normal/graph/subagent/tool descendants. Their
  termination is intentional during quit/restart, not an IPC or durable-state
  format change. Existing persistence/resume contracts remain unchanged; real
  interrupted-job resume after this Windows lifecycle change is NOT_RUN here,
  not marked N/A. It must not be inferred from mocked process tests.
- Independent review corrections: added checkout working-directory, query-only
  identity admission before terminate rights, and native Sysnative/system-root
  denial under x86 redirection. Final source review found no remaining concrete
  blocker. The previous secondary CDP-close timeout still needs observation in
  the real upgrade; no unsupported claim about its cause is made.
- GitNexus refresh completed at the repair baseline but FTS/symbol mapping still
  fails. Two change scans list only eight document sections and omit actual
  edited service symbols/new untracked installer files, despite reporting low
  risk. This is incomplete graph coverage, NOT a clean safety verdict. Exact
  source/diff review and executable local tests are the current evidence.
  A subsequent `impact Run` lookup bound to the new native source reported
  CRITICAL (657 symbols/111 flows), but listed unrelated Python and frontend
  functions as direct callers. The user was warned; direct references confirm
  the fully qualified `PuPu.Installer.SidecarCleanup` API is called only by its
  embedded launcher and native contract test. This contradictory graph result
  remains unresolved rather than being relabeled as low risk.

Release admission remains INCOMPLETE: the next candidate must rebuild/sign this
installer and pass both native host architectures, then qualification must use
the unchanged exact 0.1.10 fixture and the retained exact candidate/Unchain wheel.
No tag, candidate run, qualification, release or feature flag was changed here.

Submission check after staging all ten scoped files: `detect-changes --scope all
--repo . --limit 2000` includes ten files and 19 symbols but reports CRITICAL /
1,037 flows with unrelated cross-language callers. This remains an unresolved
index limitation, not clean graph evidence; the user was warned. The complete
Release tooling tests were rerun (289 passed, one existing platform skip) and
the Electron service/boot-readiness regressions passed again (126/126).

### 2026-09-16 — deterministic updater admission after run 35061288572

Evidence: candidate 35058158712 at `89f25f7ada26debd3a7437eb78b7b99271b7b603`
passed packaging, signatures, and native cleanup tests. Qualification 35061288572
passed all four fresh installations but stopped at `check-and-download`, before
install/restart/old-process cleanup. Its diagnostics omit the two IPC results.
The original 0.1.10 updater reproduces the misleading combined assertion when
its eight-second startup check wins: both manual requests correctly return
`started:false`. This is a demonstrated harness race, not proof of the precise
unrecorded IPC results in that run. A missing old blockmap correctly triggered
full download but would later fail the blanket HTTP-error assertion. Secondary
ENOTEMPTY occurred while cleaning up the interrupted feed.

#### BC-QUAL-012 — qualification profile and real updater IPC

- Producer: harness-owned fresh profile; consumer: unchanged packaged updater.
  Filesystem representation is the ordinary `auto_update_pref.json` containing
  exactly `{"enabled":false}`, written exclusively BEFORE process creation.
  Windows continues using native Known Folder ApplicationData, with hosted-runner
  admission and refusal of pre-existing profiles before either installer and
  again before launch. Never overwrite/delete a pre-existing user profile.
- The real public `setAutoUpdate(false)` remains exercised after connection;
  the settings sentinel is still hashed before/after the upgrade. No old binary,
  fixture source, feed binding, feature flag, or updater behavior is modified.
- Manual check admission is CLOSED: initial state idle, first `started:true`,
  duplicate `started:false`, observed checking/downloading/downloaded. Unexpected
  state or either bad result fails with a distinct cause. Diagnostic projection
  is additive OPEN metadata, containing bounded/redacted selected state and
  result fields, never permissive input to the strict success receipt.
- AC-QUAL-036: execute the real production updater with a controlled startup
  timer both before and after renderer readiness; only the manual check starts
  network work, duplicate remains blocked, and the complete runner passes its
  existing strict report consumer. Failed/existing-profile writes block spawn.
- AC-QUAL-037: initial non-idle state, first rejection, duplicate acceptance and
  IPC exceptions remain failures; diagnostics distinguish them without secrets.

#### BC-QUAL-013 — sealed loopback feed and expected differential fallback

- Producer: existing allowlisted feed HTTP server; consumer: existing updater
  plus harness request validation. No extra files become downloadable.
- CLOSED exception: Windows only, one GET 404 for the exact prior version's
  Windows blockmap name derived from the release artifact contract and validated
  fixture version; a later GET 200 of the sealed candidate installer must exist.
  Metadata and payload must actually be GET successes. Wrong versions, paths,
  methods, statuses, current blockmap failures, arbitrary 404s, absent payloads
  or range-only fallback all fail. Existing downloaded-stage, artifact hashes,
  signature and relaunched identity checks remain mandatory.
- AC-QUAL-038: real loopback server → recorded requests → strict consumer accepts
  the expected fallback; negative matrix rejects every other error and wrong
  identity. Direct full download and successful differential requests still pass.

#### BC-QUAL-014 — feed teardown and open file handles

- Producer: HTTP response/ReadStream; consumer: feed close then temp cleanup.
  Admission is CLOSED resource ownership: track only this feed's own streams,
  destroy on client disconnect, stop accepting and close connections on shutdown,
  and await owned stream close before resolving. Repeat close is idempotent;
  errors remain secondary to the original upgrade error, not suppressed.
- AC-QUAL-039: actual loopback HTTP with aborted and paused downloads proves
  owned file streams are closed; otherwise-successful teardown failures still
  fail qualification. Does not claim a reproduced Windows antivirus/file-lock
  cause for the original ENOTEMPTY.

#### SEQ-QUAL-007 — controlled manual update without startup competition

Identity: exact unmodified 0.1.10 fixture plus the selected sealed candidate and
the once-built candidate Unchain wheel. Fresh-profile refusal → exclusive false
preference → launch → slow/fast readiness → subscribe → public preference write
→ idle admission → first manual check → duplicate rejection → optional exact
old-blockmap 404/full download → downloaded → install/cleanup/relaunch → exact
identity and retained settings → owned feed teardown. Maps BC-QUAL-012..014 to
AC-QUAL-036..039. No cross-job resume of a partial installation; retries begin
with fresh hosted state. Duplicate requests are tested, not silently retried.
Chat/interaction/graph/subagent/provider matrices are N/A to these test-harness
only changes; product and runtime code are unchanged. Exact signed artifact-pair
upgrade is NOT_RUN for this patch until a new hosted qualification passes.

Impact: `/Users/red/Desktop/GITRepo/PuPu`, index refreshed at `89f25f7a`.
Disambiguated graph calls report LOW: startFixtureRuntime and assertFeedRequests
reach runRestartUpdateQualification; feed server reaches that runner, its CLI
and feed tests. Text references confirm workflow invocation. Index flow labels
still contain unrelated enrichment, so graph output is supporting, not sole,
evidence. No tag, Actions run, GitHub issue, release, or product setting is changed.

#### Local implementation acceptance (2026-09-16)

- RED before the runner fix: lifecycle tests had 20 passes and seven new
  failures, including the slow-start timer race, missing pre-launch preference,
  misleading IPC failure classification, and rejected historical-blockmap
  fallback. After the fix, the complete runner exercises the actual updater
  implementation with the startup timer both before and after readiness.
- The product updater loaded by that test is byte-identical to the exact
  0.1.10 source at `020d898de56d1cdebafb136f6176c4d10bbfdcf8`; both SHA-256
  values are `9639aa628d121e653b452c61aa09a85a38d489506e667526a6d11f25e61627e7`.
  OS, Electron and network boundaries are controlled in that test; this does
  not substitute for running the signed installer pair on Windows.
- RED before the feed teardown fix: real HTTP client-abort and paused-client
  cases both timed out waiting for open file handles; the other three feed
  tests passed. Both teardown cases now pass, including repeated close calls.
- AC-QUAL-036..039 local coverage passes: pre-existing/raced profiles and failed
  preference writes prevent launch; both IPC results must have the exact shape;
  wrong states and IPC errors retain bounded/redacted diagnostics; real sealed
  HTTP fallback passes while wrong paths/versions/methods/statuses, additional
  errors, missing GETs and range-only fallback remain rejected.
- Final `npm run test:release-qa:unit`: 302 tests, 300 passed, zero failed,
  two existing environment skips (Windows staged-artifact embedding and the
  opt-in native NSIS compiler fixture). No application or local model launched.
- `git diff --check` passed. `detect-changes --scope all --limit 2000` recognized
  all six scoped files and 34 indexed symbols, but reported CRITICAL / 16 flows,
  including unrelated parser/Python paths. This contradicts the disambiguated
  LOW upstream impact results and remains unresolved graph-quality evidence;
  the user was warned. The CLI display abbreviates symbols/flows, so it is not
  claimed as a complete clean graph check or permission to skip runtime tests.
- Hosted exact-artifact upgrade, old-process exit and final qualification seal
  remain NOT_RUN for this patch. No commit, push, PR, tag rebuild or Actions
  dispatch has been performed as part of this repair.
