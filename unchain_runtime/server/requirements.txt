Flask==3.1.0
Werkzeug==3.1.3
httpx==0.28.1
mcp==1.26.0
openai==2.7.1
anthropic==0.83.0
qdrant-client==1.14.3
# computer_control ("hand") sidecar: screen capture + input injection.
# pynput is LGPL-3.0 and is used as a dynamic import only (never vendored).
mss==10.0.0
pynput==1.8.1
Pillow==10.2.0
# Outbound TLS trust (see server/net_tls.py). The frozen PyInstaller binary
# inherits the build machine's compiled-in OPENSSL cert path, which does not
# exist on a user's machine, so trust must be supplied explicitly.
# truststore -> OS trust store (also covers corporate MITM proxy roots).
# certifi is already pulled in transitively by httpx; pinned here because
# net_tls.py depends on it directly as the fallback bundle.
truststore==0.10.4
certifi==2026.2.25
